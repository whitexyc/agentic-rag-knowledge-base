- **更新 00-项目信息包（module-066~072 完成，08-13→08-19 复核）**：git log 验收链确认 7 个新 module——066 Agent 评估（36 条任务集/三层指标/1075）、067 MCP（6 只读工具/1102）、068 死锁修复（1116）、069 PDF 双栏+pymupdf4llm（1142）、070 记忆矛盾双判 dual P0.9412（1163）、071 幻觉 kappa 0.2981 不达标不改配置（1183）、072 意图路由 backlog 前三项（1225）。信息包更新：测试 1037→1225、模块 65→72、ADR 16→18、新增模块 9/10/11 条目、量化总表 8 个新数字、诚实边界改写条（开关双默认 true）、生成指令数字同步。**关键：module-072 恰好完成昨天深挖的意图路由 backlog ①上下文改写接入生产（双锚保真 0.867 vs 0.546，"为什么"raw_overlap 0.0→0.6）②WP-D 工具历史接线 ③开关默认开**——深挖内容直接变为简历卖点。
- **04/05 文档同步 module-072（用户确认"可以"）**：04 文档——①Backlog ①②③ 标 ✅ 完成（①上下文改写接入生产双锚 0.867 vs 0.546、"为什么"raw_overlap 0.0→0.6；②WP-D 工具历史接线 request_logs JOIN tool_call_logs + 三处 classify 传参 router.py 零改动；③开关评估四跑达标双默认 true）；②WP-C/D 表格行与状态说明更新（默认关→默认开、未接入→已接线）；③"多轮检索不带历史"短板小节改为"072 已修复"（原短板保留作为故事线，面试口径更新版）。05 文档——新增 2.3 节"module-072 上下文改写接入生产"（双锚保真/开关/测试数）+ 三处"默认关"过时表述修正 + 待续清单勾掉改写开启评估项。04 剩余 backlog：④L4 多轮重训 ⑤飞轮回流 ⑥图谱信号验证 ⑦threshold_scan ⑧消融 ⑨realtime ⑩简历弹药库（06/08 文档待核对）。
- **简历 v2 用 asu-resume-skill 重做（2026-08-19，用户要求简单易懂+少专业名词）**：skill 安装到 ~/.workbuddy/skills/（P2 安全审计通过：脚本零外部依赖、无危险操作、原则端正禁止伪造指标），建 managed venv 装 reportlab + pymupdf。生成两份简历——resume-simple（求职主用：标题"个人技术文档智能问答系统"、做了什么+成果 99%/39%/44%/0.6、行动与成果下方标题简单、少术语少括号）和 resume-dense（高密度展示：RRF/pgvector/Apache AGE/bge-m3/HHEM/Owner 0→1 端到端链路）。输出 6 个文件到 docs/简历/resume-output/（2 JSON + 2 MD + 2 HTML + 2 PDF）。修复两个 schema 不匹配：①projects 必须在 experience[].projects 下（render 脚本只渲染此处）→ 移动项目；②skills 字段是字符串列表不是 dict 列表（官方示例是 ["Python","Java"]）→ 转换为字符串。PDF 中文渲染用 reportlab UnicodeCIDFont（STSong-Light）正常，pypdf 文本提取确认 + PDF 转 PNG 视觉检查中文完整。**遗留：教育经历学校名待用户补充**（信息包未提供，用"（学校名称待补充）"占位，绝不编造）。
- **简历 v2 补充个人信息+项目二**（用户提供）：教育=赣南科技学院（公办全日制普通本科高校，官网核验）|物联网工程 2023.09-2027.06（**修正：之前误写软件工程，实际是物联网工程**）；联系方式=13170974384/1420632369@qq.com/22岁；headline 改"Java 后端开发 / AI Agent 应用工程"；荣誉 4 条（物联网设计竞赛省一/CET-4 544/专业前5%/奖学金3次）。项目二（AI 模拟面试平台，xyc-个人简历.md）加入两份简历——dense 版术语版（EnumMap 状态机/LiteFlow 规则链/Single-flight/FencingToken/讯飞 AST/TreeMap 增量去重/MongoSnapshot），simple 版简单化（"答题数据不丢失不重复"等）。validate 手机号提醒=隐私确认（求职简历预期内，用户明确要求放）。PDF 内容完整性 8 项检查全过。**遗留：xyc-个人简历.md 里还有"嵌入式芯片系统与设计大赛 国家二等奖"和"半程马拉松 1h37min"未加入**（用户本次只给 4 条荣誉，待确认是否加国二）。
- **简历 v2 补充个人信息+项目二**（用户提供）：教育=赣南科技学院（公办全日制普通本科高校，官网核验）|物联网工程 2023.09-2027.06（**修正：之前误写软件工程，实际是物联网工程**）；联系方式=13170974384/1420632369@qq.com/22岁；headline 改"Java 后端开发 / AI Agent 应用工程"；荣誉 4 条（物联网设计竞赛省一/CET-4 544/专业前5%/奖学金3次）。项目二（AI 模拟面试平台，xyc-个人简历.md）加入两份简历——dense 版术语版（EnumMap 状态机/LiteFlow 规则链/Single-flight/FencingToken/讯飞 AST/TreeMap 增量去重/MongoSnapshot），simple 版简单化（"答题数据不丢失不重复"等）。validate 手机号提醒=隐私确认（求职简历预期内，用户明确要求放）。PDF 内容完整性 8 项检查全过。**遗留：xyc-个人简历.md 里还有"嵌入式芯片系统与设计大赛 国家二等奖"和"半程马拉松 1h37min"未加入**（用户本次只给 4 条荣誉，待确认是否加国二）。
- **简历 v2 调整（用户反馈一轮）**：①项目二时间 2026.03-2026.06（已完结）；②教育删"公办全日制普通本科高校"Title（保留学校+学院+专业）；③技能区整体替换为用户提供的 7 组专业清单（Java 核心基础/数据库缓存/消息队列/后端框架/AI 应用集成/工具运维/AI 工具），dense 版压缩为 6 组（检索 RAG+Agent 合并）；④项目一加 GitHub 地址（experience 级 link）；⑤目标一页达成——simple/dense 都是 1 页、底部空白 18pt 基本填满（dense 曾超 2 页，压缩技能后回 1 页）；⑥skill 安装位置告知用户：~/.workbuddy/skills/asu-resume-skill/ 和 asu-resume-audit-skill/。**待确认：国二荣誉是否加入**。
- **简历荣誉定稿**（用户"抽取一些不要全加"）：最终 5 条——嵌入式芯片系统与设计大赛 国二 + 物联网设计竞赛省一 + CET-4 544 + 专业前5% + 奖学金3次；半程马拉松不加入（与求职无关）。两份 PDF 均 1 页，MD 同步。
- **简历 v2 精简轮**（用户反馈：技能太详细/括号太多/449 争议）：①技能区抽取为 Java/Python/AI 三类精选（simple 5 组、dense 6 组），去掉消息队列/冗长子项；②dense 项目一职责 8 条括号瘦身——删"449 条标注训练"（用户觉得昨日更新后存疑，实测 449=337+112 准确但简历去掉细节）、删"GIN 倒排/pgvector/Apache AGE/检索组7生成组4/2s轮询/30天1天"等细节，保留硬指标（44%/Accuracy 1.0/Precision 0.94/1225）；③两份 PDF 仍 1 页。
- **dense 版版式调大铺满一面**（用户要求）：扩展 render_resume.py 支持从 JSON["layout"] 读版式参数覆盖（fontSize/leading/spaceAfter/sectionFontSize/projectFontSize 等），只影响配 layout 的简历不影响其他；dense JSON 加 layout。二分调参：7.65→8.3(空 28pt)→8.6(超页)→8.45(空 17.6pt 1 页满)→定稿。行距 10.25→11.4，段距 1.6→2.4，标题 11.6→12.8，项目 8.8→9.6。视觉 PDF 确认：姓名/分区/正文/技能两列舒展铺满。**simple 版用户没要求，但发现正文后空白 103.7pt（约 1/4 页）偏大，待问用户是否也调。**
- **dense 版版式调大铺满一面**（用户要求）：扩展 render_resume.py 支持从 JSON["layout"] 读版式参数覆盖（fontSize/leading/spaceAfter/sectionFontSize/projectFontSize 等），只影响配 layout 的简历不影响其他；dense JSON 加 layout。二分调参：7.65→8.3(空 28pt)→8.6(超页)→8.45(空 17.6pt 1 页满)→定稿。行距 10.25→11.4，段距 1.6→2.4，标题 11.6→12.8，项目 8.8→9.6。视觉 PDF 确认：姓名/分区/正文/技能两列舒展铺满。**simple 版用户没要求，但发现正文后空白 103.7pt（约 1/4 页）偏大，待问用户是否也调。**
- **simple 版也铺满一页**（用户要求）：加 layout 配置，4 档二分调参——7.65(空 103.7)→8.3(空 58.8)→8.8(空 26.8)→8.9(空 21.1)→**9.0(空 17.0pt 1页满 定稿)**。simple 内容比 dense 少所以字号更大（9.0 vs 8.45），行距 12.2 / 段距 3.0 / 分区标题 13.5 / 项目 10.2。视觉 PDF 确认：姓名/教育/项目一二/技能双列/奖项一列全 1 页。**两份简历现在版式统一：1 页铺满、底部空白 17pt 左右**。
- **简历 v2 二轮修复（用户反馈：删 subtitle + 链接不能跳转）**：①删 simple 版两个 subtitle（"从零开发的全栈项目，已上线可用"/"后端开发，正在持续迭代"）——dense 版无这些字样保留；删后仍 1 页空白 17pt（标题行缩短不影响行数）。②**PDF 链接不可点击根因**：render_resume.py 原来只把 label 文本拼进 right_bits 无 href（HTML 版本来就是 a 标签，预览面板沙箱拦截外跳属面板限制）。修复：链接项构造 `<font color=#366F96><u><link href=url>label</link></u></font>` + 新增 _join_bits 辅助函数（富文本标签跳过 ptext 转义，普通文本仍转义），reportlab Paragraph 原生支持 link 生成 PDF 链接注解；pymupdf get_links 验证两份 PDF 均有 GitHub 注解。③MD 项目地址改 markdown 链接 [GitHub](url)。**踩坑：resume-simple.pdf 被预览面板锁定 PermissionError**——解决=渲染到 -new 临时文件 + PowerShell Move-Item -Force 替换成功（bash 里 cat 追加日志因内容含 PowerShell 字样被安全策略拦截，改用 Edit 工具）。
- **优化投递打招呼语（用户要求）**：用户粘贴原稿（500 字、65+ 模块/1037 测试/16 ADR 旧口径）。优化：①数字更新为 72 模块/1225 测试/18 ADR；②拆两版——精简版约 190 字（平台消息直接发，Java 后端 + AI Agent 双定位开头）+ 完整版约 360 字（求职宣言/面试自我介绍，保留评估闭环 4 个精选数字 0.9905/+0.24/44%/MRR 负）；③"不依赖框架"提为第一卖点；④落档 docs/简历/14-投递招呼语.md（含改动说明表）。
- **招呼语表述修正（用户纠正："参考框架内容并深入"）**：用户澄清项目实际是**参考 LangChain/LangGraph 等框架设计 + 深入理解后自研**（昨天深挖确认过的事实），"不依赖框架"过于绝对（且易被质疑"不会用框架"）。修正 14-投递招呼语.md 三处：精简版"核心链路全手写不依赖框架"→"参考框架设计、深入理解后自研"；完整版特点一"全手写不依赖"→"懂框架、不盲从框架 + LangGraph 对照实验佐证取舍"；改动说明表同步。**新卖点表述：比"不依赖"更强——既证明框架理解深度（能读源码对照取舍）又证明自研能力。dense 简历"手写 ReAct 循环/全自研"是事实表述保留不改。**
- **招呼语核心特点改大白话（用户问"需要再易懂些吗"）**：判断=需要，受众是 HR/初筛，ReAct/RRF/MRR/NLI/类型化衰减等术语等于白写。三特点改为"大白话标题 + 一句人话 + 保留技术锚点"：①懂框架不盲从（自研 vs 调包，判断力）②每一步改动有数据验证（0.9905/44% 提速/不吹牛）③让 AI 记住用户（记忆像人：沉淀/淡忘/纠错，多用户隔离）；技术细节留给面试展开。改动说明表补一行。
- **建 15-面试自我介绍.md（用户要求"含个人信息和第一个项目"）**：标准版约 350 字 1-1.5 分钟（身份+教育 → 项目一句总述 → 三个亮点：检索 99%/工程严谨 44%+数据验证/记忆像人 → 荣誉国二+省一 → 期望转正）+ 30 秒精简版 + **追问展开表**（RRF 三通道/+0.24/0.9905/250 字符/框架对照/NLI 0.94/1225 测试——每句都对应可展开的深挖点，面试官追问不慌）+ 注意事项（亮点顺序留钩子、荣誉只报最硬两条）。
- **建 12-工程化留档体系.md（用户问"决策、执行还有什么都有留档"）**：代码实测 specs/ 233 个 md——留档全景：**决策 18 ADR / 执行 39 组三件套（plan/changelog/test-report）+ 早期 12 task-brief / 数据 METRICS / 术语进度 CONTEXT / 代码 git / 知识沉淀项目深挖 / 日志 memory**。闭环：task-brief（要做什么）→ ADR（为什么）→ plan/changelog（怎么做）→ test-report+METRICS（结果）→ CONTEXT+深挖（怎么讲）。诚实边界：72 模块 vs 39 三件套（早期模块不齐，口径"主流模块有"）、无独立 review 文件（评审记在 changelog）。30 秒话术"18 份 ADR + 三件套 + METRICS 版本链，追问翻账本就能答"。**编号从 06 改 12（06 预留给三通道+RRF 融合）**，README 索引同步。
- **自我介绍融入留档体系（用户澄清"我的意思是加入到自我介绍中"）**：15-面试自我介绍.md 三处更新——①标准版"工程严谨"亮点扩写：对比实验 + "18 份架构决策文档记录每次为什么这么选、每模块计划/变更/测试三件套、任何数字可追溯"（350→400 字仍 1.5 分钟内）；②30 秒精简版加"18 份决策文档 + 每模块三件套留档，任何数字可追溯"；③追问展开表加一行（ADR 记录为什么选 A 不选 B：RRF vs 加权、图谱消融；三件套追溯）。
- **建 16-岗位问卷回答.md（AI 编程助手岗位问卷）**：5 题逐答，全部真实经历——①OpenClaw Skill：无，但带出 ToolRegistry（10 工具）+ FastMCP 集成（6 只读工具）同类实践；②参与最多项目=Agentic RAG 知识库全栈自研，负责全部（18 ADR/三通道/意图路由/幻觉检测/记忆/1225 测试）；③通用 Skill：未发布，但 ToolRegistry 与 FastMCP 是可复用设计；④Python/JS 有（golden 评测/阈值扫描/分类器训练脚本 + React 前端），Playwright/RPA/Computer Use 无；⑤测试 AI 代码三层：四角色评审验收 + 1225 测试（单测+golden 双防退化）+ 对比实验达标合入。**原则：未做过如实说无 + 带迁移点，不编造。**
- **建 06-三通道检索-RRF融合.md（深挖落档）**：代码实测 retriever.py:418-637——_execute_fusion 三路并行（FTS/向量/图谱各 fetch_k=top_k×2=20，gather + _timed_channel）+ _fuse_rrf（score=Σ1/(k+rank)，k=60，缺路不贡献退化、min-max 归一化 hybrid_score 兼容相对分过滤[module-035 教训 RRF 原始分与 0.6 绝对过滤不兼容]、双命中透传 abs_cosine 供 L3 反证）+ _fuse_weighted 对照（0.3/0.6/0.1）+ 降级链（单路失败缺路/三路全空[]/融合异常回退 hybrid/向量化失败 FTS+图谱）。数据：RRF 三通道 0.9905 vs 加权 0.9714（+0.0191）、向量单通道 0.9714 主力/图谱 0.7333（净增益+0.0096 实体关系）/FTS 0.0381 长尾、k 扫描 20-100 平坦 0.9810。业界：RRF 起源 Cormack 2009 k=60 成 ES/Azure/Qdrant 默认；2026 演进 weighted RRF（ES 9.2/Qdrant v1.17）/Weaviate RSF/Qdrant DBSF；2026 生产模式 RRF top-50→cross-encoder top-10（本项目同款 top-10→rerank top-5）；业界实测纯向量 78%/BM25 65%/RRF 91%。额外见解：k 平坦=特性非缺陷、图谱检索"查关系"vs 意图确认"点名"呼应 04、归一化是踩坑产物、abs_cosine 红线、降级链=全项目哲学。README 06 置 ✅。
- **06 文档补"7.3 归一化人话版"**（用户要求细说归一化）：min-max=汇率换算类比；RRF 原始分量纲 0~0.05（三路 top1≈0.049）；engine.py:986-989 过滤 `hybrid_score>=0.6`（0.6 是余弦尺子，RRF 原始分全 <0.6 → 检索空，module-035 踩坑）；归一化后 0.6="达到最高分 60%"相对分；**局限：min-max 只由最高/最低两点决定，不能跨查询比分数**（所以 L3 反证用 abs_cosine 原始绝对余弦）；面试答"RRF 分不能直接用，要归一化"。
- **06 文档补"7.4 评测指标人话版" + 公开基准语料核实**（用户追问）：①公开基准语料：C-MTEB EcomRetrieval 全量 100,902 篇（抽样 3,000）/ BEIR nfcorpus 3,633 篇（抽样 1,200）——抽样是 CPU 环境约束（全量嵌入 ~2h）；有效查询 26/292（正确答案不在抽样集内被"真跳过"）；②**真跑过核实**：eval_runs 表 id=44/45 真实落库（git_commit d78967cf + config_snapshot 10 参数 + scores），数据缓存目录 gitignored 被清但 DB 是硬证据——"数字 + 代码版本 + 配置快照三件套钉死"；③指标人话版：查询数=有效题数/Hit@5=前5有没有（找没找到）/MRR=第一个答案排第几（第1=1.0 第2=0.5）/nDCG@10=前10整体排序质量（相关×位置权重）——单一指标会骗人（Hit@5高+MRR低=找得到但排后；MRR高+nDCG低=第一好但整体乱），三合一="找得到排得前排得对"。**注意：config_snapshot 里 reranker_model=Qwen3-Reranker-0.6B 是历史生成式版本，当前生产是 bge-reranker-v2-m3（分类式 44% 提速），面试分清楚。**
- **公开基准扩题规划（用户要求"规划好让另一个 AI 执行"）**：用户嫌 ecom 有效查询 26 题太少。事实链：26=抽样 3,000/100,902（3%）的自然结果（相关文档不在抽样内无法判分被跳过），不是检索能力问题；扩 10,000 篇预计 ~100 题、5.5h CPU（bge-m3 Q8 ~2s/篇串行）、内存峰值 ~1.5GB（模型 634MB 为主，向量 40MB 不落盘）、磁盘=数据缓存几百 MB 可删+模型 634MB 必留。**模型文件缺失已修复**：从 .claude/worktrees/m8-knowledge-panel 复制 634MB 回主工作区（原来主工作区没模型）。规划落档 docs/公开基准扩题-执行规划.md（自包含：环境=hermes venv、命令 --corpus-sample 10000、固定种子 20260815、验收=新 eval id+n_queries 60-150+METRICS 更新、红线=勿删模型/勿动 worktree、故障处理）。HEAD=125e2dd 工作区干净，落库 git_commit 准确。用户决策：让另一个 AI 执行。
- **扩题规划 v2（用户要求"先试 GPU 能不能跑"）**：发现 embeddings.py `_lazy_load` 的 `Llama(...)` **没传 n_gpu_layers**（纯 CPU）→ 加 `-1` 可 GPU offload（RTX 4060 8GB 放 bge-m3 Q8 ~1.5GB 显存绰绰有余）。规划改为 GPU 优先/CPU 兜底：步骤 0 临时脚本 gpu_probe.py（30 篇计时，不碰生产代码）→ 分支决策（dt≤0.2s=≥10x→GPU 全量 --corpus-sample 0 1000 题 ~5.6h；dt 0.2-0.4s→全量仍值；dt>0.4s→回退 CPU 10000 篇）；GPU 全量与 CPU 兜底几乎同价但 10 倍题量（1000 vs 100）。陷阱：llama-cpp-python 可能没编译 CUDA（llama_supports_gpu_offload 检测，无则 pip 装 cu124 wheel）；评测脚本临时开 GPU 只改 _lazy_load 一行、跑完还原。
- **gpu_probe 简化（用户："先测一篇不行吗"）**：认同——"能不能"加载+1 篇即可判定，不需要 30 篇。规划改为：Llama 加载 + warmup 1 篇（排除加载时间）→ 计时 2 篇定加速比 → 分叉。全程 1-2 分钟。
- **ecom 数据已下载（用户："直接下载，修改文档"）**：实测全量三件仅 ~8.1MB（HEAD 验证：corpus 8.0MB+queries 26KB+qrels 14KB，parquet 压缩+电商短文本），此前估 100-500MB 高估 50 倍已更正。已下载到 eval/datasets/public/cmteb_ecomretrieval/（corpus 8224KB/queries 26KB/qrels 14KB，与 HEAD 一致），规划文档标注"数据已就位，download_dataset 检测存在即跳过下载"。
- **公开基准评测口径缺口 + 步骤 5 混合增益验证（用户关键质疑："不然怎么测三通道有没有用"）**：用户指出——公开集只跑了纯向量基线（0.9231/0.5685），三通道增益只在自建集验证过（+0.0191），公开集缺"融合有用"的证据。方案：扩展脚本加**内存 BM25 通道**（rank_bm25/jieba 中文分词）与向量 RRF 融合，同数据集同种子同有效查询过滤跑 a)纯向量 b)向量+BM25 对比；**图谱通道不适用公开集**（无实体，如实标注"图谱增益以自建集为准"）；业界参考纯向量 78%/BM25 65%/RRF 91%。已写进规划文档：步骤 5 + 验收清单 + **第七节完整可复制执行提示词**（GPU 验证→纯向量基线→混合对比→METRICS 更新→清理）。nfcorpus 低归因修正：领域(医学术语)>基准难度(BEIR qrels 稀疏)>语种(中文强一档但离英文 SOTA 仅 3-5pt)>评测配置(纯向量未用 bge-m3 稀疏混合,Recall@100 可 +12-18%)。
- **步骤 5 取消（用户决策："不能走我的 FTS，走自己的 BM25 没接入真实项目，先不处理"）**：内存 BM25 ≠ 生产 FTS（jieba+PG GIN 倒排+停用词表+search_tokens 列项目特有），测出的是陌生检索器增益、证明不了三通道有用；真验证要灌公开语料进 PG 建 FTS 索引（工程量+非知识库文档）不划算 → 取消。规划文档步骤 5 标 ❌ 取消 + 替代路径 backlog（灌 PG 跑真三通道 / 飞轮回流后验证）；公开集定位=测模型泛化（纯向量基线），三通道增益以自建集 +0.0191 为准。继续 07 语义重排。
- **建 07-语义重排.md（深挖落档）**：代码实测 reranker.py 238 行——模型演进（module-018 Qwen3-0.6B 生成式 6s/对 CPU 不可用 → bge-reranker-v2-m3 分类式 515ms/对 12x）；全流程=粗筛候选（rerank_max_candidates=6 按融合分截断，Hit@5 不降）→ 截断 200 字符（CrossEncoder batch 最长填充，超长父块 0.5s→200s 实测 2pair 201s）→ int8 动态量化（0.89→0.42s/对 2x，弱相关漂移排序保持，fail-open）→ threading.Lock 串行 to_thread → rerank_score 降序 top-5；缺权重明确报错不回退在线。截断来历：九档扫描 500→50（拐点 150 弱相关跌破 0.4）+ 精细扫描 240→150（160 漂移、中相关 170 崩塌）→ 200=分数语义+排序双稳安全下限。数据：单次 rerank 10.4-11.2→2.6-2.8s（3.8-4.1x）、单对 0.70→0.44s、E2E rerank 8.5-13.5→1.9-3.3s、TTFT 25.3→15.4s（-39%）；量化隔离实验强相关漂移≤0.025、干扰恒 0。业界 2026：三层格局（开源 cross-encoder=同款 bge-reranker-v2-m3 多语标准 / 托管 API Cohere+Voyage+Jina / LLM listwise RankGPT+jina-v3）；hybrid 0.612→+reranker +29.7% NDCG@10（本项目缺无重排 vs 有重排 ablation 诚实标注）；context rot（LLM 中间位置注意力衰退，重排对抗之）；ColBERT late-interaction（存储 50-100x，本项目量级不值）。README 07 置 ✅。
- **建 08-反思充分性.md（深挖落档）**：代码实测 reflector.py 699 行 + engine.py:389-429——五层方案（ADR-0005）：L0 golden_sufficiency 100 条标注集（充分50/不充分50 Accuracy 1.0000）；L1 零 LLM 硬闸门（文档数<2 或 top-1 abs_cosine<0.55，FTS 命中文档无字段跳过不误杀；阈值 module-047 实测 0.4 漏判 60% 不充分/0.55 F1=0.98）；L2 CoT 信息点比对 + few-shot + 自洽性检查（默认关，两温度不一致保守判充分）；L3 多信号融合（分数达标才问 LLM 判模糊地带，LLM 判不充分走 rewritten_query 不因分数高强制充分）；L4 sufficiency_clf（Acc 0.80 insufficient Recall 1.00，训练就绪未接入）。检索循环：最多 3 轮 + rewritten!=current_query 才继续 + 异常默认充分（防失控三件套，反业界 anti-pattern"不受控工具循环"）。**L3 反证 vs L1 闸门易混**：0.3 管路由误判（只观测标记 suspected_misclassify）vs 0.55 管文档够不够（触发二次检索），对象不同。业界对照：CRAG（检索评估器三分类 + web 兜底，本项目同款架构但兜底=如实告知非 web）、Self-RAG（reflection tokens 需微调，本项目 prompt 近似注释承认）、2026 共识 self-correction 非免费升级、基础 RAG 优化优先。反思延迟 1.3-2.6s。README 08 置 ✅。
- **08 文档补 2.6"文档数 → 走哪条路完整分支"**（用户追问"会不会只有一篇文档"后要求落档）：会——rerank 只裁剪不增补，检索召回 1 篇反思就收 1 篇（偏门查询/冷启动/检索差）。1 篇判不充分理由=单来源无法互相印证（新闻两信源类比）。分支：0 篇降级如实告知 / 1 篇闸门判不充分改查 / 2 篇交 LLM 判 / ≥3 篇跳过反思直接生成（_MIN_DOCS_SKIP_REFLECT=3 提前终止）——闸门下限 2 与 skip 3 一低一高围出"够用区"。面试话术补入。
- **08 文档 2.6 补"文档粒度"（用户追问"一篇文档指标题还是父块"）**：数量闸门的"文档" = **检索命中的子块（chunk ~300 字）**——链路顺序 engine.py:396-435：retrieve 返回子块 → rerank 按子块打分 → check_sufficiency 数子块 → **循环结束后**才 _expand_to_parents 映射父块（反思看不到父块）；title 只是附带元数据。判断依据=片段覆盖度（子块）、生成素材=完整章节（父块），各管各的。精确口径"1 篇文档 = 1 个子块命中"。
- **08 文档 2.6 分支图定稿（用户先要图后说"就用文本"）**：先改 mermaid 后用户明确要文本（环境不渲染 mermaid）→ 恢复为 ASCII 文本分支图（0/1/2/≥3 分流 + 1 篇改写重检索循环 + 改不出降级）。教训：用户文档渲染环境不支持 mermaid，后续文档插图默认用文本/ASCII。
- **08 文档 2.2 补"阈值 0.55 分布示意图"（用户："闸门怎么得出来的，图插入文档"）**：用户指的图 = 充分/不充分两座山 + 间隙 + 0.55 切点 ASCII 分布图（之前对话里画过）。已插入 2.2 L1 闸门"阈值来历"后：0.4 太靠左漏判 60% 不充分、0.55 切在间隙两峰都够不着 F1=0.98、"上缘"=间隙高分侧边缘保守方向。附一句话总结"扫描 100 条标注数据找两座山之间的空地切的"。
- **08 文档 2.2 补"F1 人话版"（用户"F1 什么意思解释并加入文档"）**：F1=精确率×召回率调和平均（2PR/(P+R)），偏科被惩罚。精确率=拦下的里多少真该拦（不冤枉充分）、召回率=该拦的里拦住多少（不放过不充分）。警察抓人类比。对应闸门：0.4 召回率崩漏 60%→F1 低；0.55 双高→F1=0.98。面试话术"定阈值=误杀 vs 漏网找平衡，F1 是量化标尺"。**顺手修复 2.2 阈值来历行的 HTML 实体乱码（&#x95F4; 混入导致"上缘--**间隙"错乱）**。
- **08 文档 2.7 外部兜底方案评估（用户"能不能接外部搜索/MCP 兜底"→"记录文档但不执行"）**：结论=能接架构已铺路但选择不执行（定位决策非技术问题）。三个插槽：ToolRegistry 注册 web_search 一行、Agent ReAct 多工具选项、MCP 基建（module-067 服务端已有/客户端同栈延伸）。两方案：A 外部搜索工具（CRAG 式，Tavily/SerpAPI/Bing）vs B MCP 客户端检索。三权衡：定位冲突（简历"离线零外部依赖"）、源治理（外部内容进答案链可信度/版权）、成本（API+延迟+降级链）。面试话术"有设计有理由没实施有 TODO"已写入 2.7 + 待续清单 web 兜底项勾掉标已评估不执行。
- **08 文档 5.1 补"Self-RAG 详解"（用户"Self-RAG 的反思什么意思"→"写入 08 文档了吗"）**：reflection token 人话版（[ISREL]/[ISSUP]/[ISUSE]/[检索] 自我监控按钮，训练才学会自发打标签）+ 项目"prompt 近似版"对照表（训练进权重 vs 提示词询问、可靠性高 vs 略低、锁模型版本 vs 零训练可换）+ 诚实边界（reflector.py 注释承认未训练）+ 面试一句话。另：用户问"项目定位文档在哪"→ ADR-0016-rag-architecture-positioning.md（权威锚点，面试/简历引用以此为准），注意文档里 250 截断是旧值现生产 200。
- **08 文档 2.3 补"CoT 人话版"（用户选中 CoT 问什么意思→写入文档）**：CoT=Chain of Thought 思维链=先展示推理过程再下结论；普通提问"够吗→够"拍脑袋 vs CoT 三步"列信息点→逐点比对→下结论"；为什么反思必须用=直接问 LLM 倾向偷懒答"够"，CoT 逼它认真核对（2023 CoT prompting 论文核心发现）；考试写解题过程类比。
