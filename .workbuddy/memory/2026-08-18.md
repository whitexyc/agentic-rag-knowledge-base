- **01 文档补 2.1.2 回退引擎选型（PyMuPDF vs pypdfium2，调研落档）**：PDF Oxide 3830 文档实测——文本提取 4.6ms vs 4.1ms、通过率 99.3% vs 99.2%、输出 99.5% 一致（几乎打平）；PDF-Bench 353 文档——pypdfium2 第1（78.3%）vs PyMuPDF 第4（77.3%）；关键差异：PyMuPDF 功能全（表格/图片/表单/渲染）但 **AGPL-3.0 传染性许可**（商业部署要开源服务端），pypdfium2 纯文本冠军但 **Apache-2.0 零风险**；Docling 社区验证 pypdfium2 双栏排序更好（2.2.1 P1 换引擎方案有实测支撑）。结论：回退路径当前 PyMuPDF 合理（功能全+质量无损），AGPL 是隐藏风险点（项目开源/商业化时换 pypdfium2）。
- **01 文档 2.2.1 补"P0 重组实现细节"**：用户问"怎么重组、没看到识别左右的标记"——关键认知：PDF 文本块自带坐标 (x0,y0)，x0<页面宽/2=左栏，坐标就是标记；重组必须在 get_text("blocks") 阶段做（AnyDoc Markdown 已丢坐标）。给完整代码（按中线分左右→左栏 y 序→右栏 y 序→左+右拼接）+ 现状核对（_parse_pdf_pymupdf 现用 get_text() 纯文本，P0 改动=换 blocks+排序）+ 三坑（单栏误切检测/跨栏图注/跨栏表格）+ 面试话术。
- **01 文档 2.2.1 补"为什么双栏问题不影响 AnyDoc 主力地位"**：用户问"AnyDoc 双栏用不了，那不是没用吗"——澄清三层：①内容层面可用（rag_survey 实测 111966 字符 100% 保留，只是排版失真）；②全行业难题（PDF-Bench 17 解析器结构保留平均仅 35%，非 AnyDoc 独有）；③场景覆盖极小（知识库 124 篇 MD 为主，双栏论文是长尾）。正确姿势=分层路由（双栏检测 → AnyDoc 主路径 or PyMuPDF 重组/MinerU），核心认知"主力+专精+检测器"。
- **01 文档 2.2.3 面试话术修正**：用户指出"双栏等复杂版面交给清洗层按中线重组规则（P0）"没写用什么实现 + "清洗层"表述错误——修正为"交给 PyMuPDF 路径按中线重组（P0）"，并补关键澄清：重组必须在坐标阶段 get_text("blocks") 做，AnyDoc Markdown 坐标已丢、清洗层拿到纯文本救不了。
- **01 文档 2.2.1 P0 代码修正 + 补"文本怎么拼"**：用户问"文本如何进行重组"——补三点：①修正原代码 bug：判断左栏必须用"右边缘 x1 ≤ mid_x"（原 x0 < mid_x 会把横跨两栏的标题误判成左栏）；②拼接顺序 = 横跨块（标题）→ 左栏 y 序 → 右栏 y 序，块间 \n 连接，段落边界交给清洗层（重组管顺序、清洗管格式）；③纯文本（无坐标）无法可靠重组，必须回到坐标阶段。
- **01 文档 2.1.2 补"能力面 vs 现状"澄清**：用户质疑"PyMuPDF 能转 MD 为什么之前说只能出裸文本"——认错：把项目现状（get_text() 裸文本）说成了库的全部能力。补 5 档 API 表（get_text 纯文本 / blocks 坐标 / dict 字号加粗 / html / pymupdf4llm 独立包直接出 MD），明确 pymupdf4llm ≠ PyMuPDF（官方生态独立包，发动机 vs 整车类比）。教训：讲库时要分"能力面"和"项目用法"两种口径。
- **01 文档 2.1.2 落档"回退路径结构化升级决策（B 档）"**：用户选 B（pymupdf4llm）。理由：官方 MD 生成器一行出 MD，比 A 档自写规则完整（标题层级/列表/表格），官方维护；落地要点：pip 装 pymupdf4llm、_parse_pdf_pymupdf 内替换 get_text、双栏仍先走 P0 重组（不赌）、输出仍过清洗层（它解决结构/清洗解决格式噪声）、PW_PDF_FALLBACK_MD 开关；诚实边界：AGPL 同 PyMuPDF（PDF Oxide 标 pymupdf4llm 也 AGPL-3.0）、55.5ms 性能（回退可接受但主链路必须 AnyDoc）。面试话术补充已写。
- **01 文档 2.3 清洗层五步代码级深挖 + 修正口径 bug**：用户逐问五步实现——①格式清理=Unicode 分类(控制符)+正则(页眉页码)+启发式兜底(剥装饰符纯数字≤4位)三件套，水印不做自动删(诚实声明)；②冗余过滤：**发现口径 bug**——清洗层实际只做规则级 _is_noise 三连判(空/纯标点/纯噪声符号)，哈希+语义去重其实在文档级 document_dedup.py(2.7 节)，已修 2.3 五步表；③结构恢复=断词合并三规则，单栏归清洗层/双栏归解析层 P0；⑤分块准备(_heading_prep)=标题表格前补空行+H1→H2 提升+合并连续空行("给 chunker 摆盘")。2.3 补"五步代码级实现细节"小节。
- **01 文档 2.3 补"② 业界方案对照 + 精确语义"**：用户问 _is_noise 是"出现标点就删"还是"成块出现才删"——精确语义：否定式判定（整段零文字内容才删，非出现标点就删）+ 第三条件噪声符号集合 + 段级判定 + 纯数字行由页码规则管（两套分工）。业界 4 档（搜索）：L1 规则初筛（Langchain-Chatchat：非文字字符占比>30%/长度<100/重复模式/句长分布）/ L2 统计（困惑度/熵）/ L3 MinHash 模糊去重 / L4 模型辅助（BERT/LayoutLM）。升级方向：P0 加非文字字符占比规则（抓 OCR 乱码，现纯符号判定会漏）；诚实边界：规则级抓不了"像话但没信息"段。
- **01 文档 2.3 补 Langchain-Chatchat 背景说明**：用户问"Chatchat 是什么，是框架的功能工具吗"——澄清：开源本地知识库问答成品应用（原 Langchain-ChatGLM），基于 LangChain 构建的完整应用（成品车）非工具（积木）；中文优化/可离线/Apache 免费商用。与项目对照：它开箱即用、本项目从 0 自研+评测闭环+三通道 RRF+幻觉检测+记忆进化；L1 非文字字符占比规则借鉴自它源码。面试话术：了解业界成品是拿它当参照系定位自己"多做了什么"。
- **新建专题文档：框架选型-自研vsLangChain**（docs/项目深挖/专题-框架选型-自研vsLangChain.md）：用户要求单独成篇讲透"为什么自研、调研了什么、最后选了什么"。骨架含九节：结论先行（核心编排手写/LangChain 仅工具层/LangGraph 实验对照）/ 大白话（乐高 vs 定制车，分界线=编排层）/ 代码实测（chunker+llm/client 工具层、langgraph_react 实验、react 手写）/ 自研四理由+单 Agent 记分卡 8 分 / 业界调研（LangChain vs LangGraph 区分、调试-70% 性能+40-60%、AgentExecutor 废弃、32% 不可靠调查、senior pattern 挑零件）/ 决策三句话 / 诚实边界（自研代价+切换信号）/ 面试话术 / 待细聊清单 6 项。README 索引加专题行。后续逐项细聊填充。
- **01 文档 2.3 补"L2/L3 具体实现详解"**：用户问困惑度/熵/MinHash 具体怎么实现——大白话+公式+实测参照落档：①困惑度=语言裁判看惊讶度（KenLM/GPT-2 级，CCNet 分三桶），坑=surprising≠bad 误伤术语；②信息熵=用词多样性（英文 4.5-5.2 bits/垃圾<4.0），语言自适应；③实词占比=废话词比例；④MinHash=5-gram shingle→Jaccard→128 哈希签名（签名相似≈Jaccard，误差1/√k）→LSH 分带分桶避开 O(N²)（datasketch 15 行，Dolma -30%）。本项目判断：124 篇两两比秒级不需 MinHash、困惑度误伤术语不推荐、熵可作 P1 补充；核心认知"语料级方案按规模选档"。
- **建 03-分块策略.md + 分块器盲区分析**：用户问"chunker 只认 ##/### 是不是有问题"——读 chunker.py 确认：有意的粒度设计（Obsidian 两级标题够用+粒度守恒），非 bug；但两个盲区：①H4+ 不切分（内容并入上层父块，边界丢失、粒度粗，>4000 段落兜底）；②隐式依赖清洗层 H1→H2 先跑（漏 clean() 的路径 H1 切不到）。改进选项 A 加 #### 层（牵动重灌评测）/B 清洗层降级/C 保持现状。03 文档已建（三级结构/盲区表/待续聊 6 项/面试话术），README 索引更新。
- **01 文档 2.7 补"坑① boilerplate 详解"**：用户问 boilerplate 先剥离什么意思——大白话：每篇文档都有的固定套话/包装纸（页脚/免责声明/导航/签名档）；骗人机制=相似度只看重合度，两篇不同主题文档带同一段套话会被判近似重复误删（出版社 logo 类比）；剥离方法 4 档（正则/位置裁首尾/⭐跨文档重复行检测——数据驱动不用预知内容/Boilerpipe 等专用库）；与清洗层关系两层分清：清洗层=格式噪声（已实现），boilerplate=内容级套话（去重前置）。
- **01 文档 2.3 补"RAGFlow DeepDoc 背景说明"**：用户问 DeepDoc 免费好用吗——调研落档：InfiniFlow 开源 RAGFlow 的内置解析引擎（表格 Faster R-CNN+Mask R-CNN 流水线 F1 92.8%/公式 94.3%/扫描件 85%/多栏 89%/LayoutLM 340M）；免费但门槛（Apache-2.0 模型免费，平台非库：五容器栈+GPU+6.2GB 内存）；本项目判断：124 篇 MD 场景杀鸡用牛刀，其价值=扫描件/复杂表格/多栏论文=MinerU 重炮路由定位；核心认知"解析工具按语料形态选档"。
- **检索指标口径深挖 + 文档补 105 有效口径（重要）**：用户问"0.9714/0.0381/0.7333 检索了多少文档、是最高最低还是什么"——读 golden_retrieval.py 确认：Hit@5 按题平均（每题检索 top-5 命中任意 golden 记 1/0，求和÷题数），非最高/最低。数学验证实锤：**统计分母 = 105 题有效**（112 题剔 7 题无覆盖，0.9905=104/105、0.9714=102/105、0.7333=77/105、0.0381=4/105 全部整除）——METRICS 标题只写"112 题"没写 105 口径（文档缺口，面试追问分母会穿帮）。已修：METRICS.md 头部补口径说明 + 02 文档检索表改"105 有效"并补注释。面试口径：主动交代"112 数据集/105 有效/自标注/按题平均"。
- **02 文档补"4.1 评测口径详解"完整小节**（用户确认写入）：三问答案表（检索范围=全库 top-5 / Hit@5 按题平均 / 112 题 105 有效）+ 判定逻辑（golden_retrieval.py:130-177）+ 数字还原表（104/105、102/105、77/105、4/105）+ 面试口径。METRICS.md 与 02 文档现已完全对齐 105 有效口径。
- **02 文档补"GGML_ASSERT 是什么"详解**：用户问 GGML_ASSERT 是什么——讲解落档：GGML=llama.cpp 底层 C 张量库（四层链路最底层）；GGML_ASSERT=C 断言宏，条件不满足直接 abort 杀进程（非异常，try/except 接不住）；并发触发机制（模型实例非线程安全→双线程踩内存→中间检查崩溃）；类比（两人同写一本账本）；面试金矿（自研独有经验）。
- **02 文档补"threading.Lock 详解"**（用户要求多讲）：本质=独木桥互斥；竞态条件（counter+=1 三步非原子）+ 关键认知 GIL 只防内存崩溃不防逻辑竞态、C 库执行时释放 GIL（锁是唯一防线）；用法 with/acquire(timeout)；四坑（死锁固定顺序/RLock 重入/asyncio.Lock 管不住线程/锁粒度）；项目三细节（懒加载锁内/整批持锁/归一化锁外）；PEP 703 free-threading 背景（3.13 禁用 GIL 后锁更刚需）。面试话术 30 秒已写。
- **02 文档补"L2 归一化详解"完整小节**（用户要求写清楚）：代码实现（embeddings.py:112-119 五步：转 numpy→L2 范数→防除零 1e-9→逐元素除→tolist）+ 公式（v'=v/‖v‖）+ 手算例子 + 调用链（锁内 create_embedding → 锁外 _normalize）+ 三业务意义（点积=余弦→pgvector 余弦距离成立 / 跨查询可比→动态 K 绝对阈值前提（module-035 min-max 相对分套绝对阈值恒 K=1 的坑）/ 与云端 normalize_embeddings=True 一致）。面试话术含两个细节：module-035 踩坑 + 锁外归一化。
- **03 文档补"small-to-big 检索映射详解"**（用户深挖）：engine.py:923-1021 实测——入库（父块 parent_id=NULL 无向量、子块带向量只查子块）+ 映射（多子块命中→父块分数=子块最高分，abs_cosine 透传，is_canonical 检索抑制，去重排序）+ 核心决策（取最高分非求和——防长父块占优/平均抹掉对口信号，考整本书类比）+ 价值（检索小保精准/返回大保完整）+ 细节（旧格式兼容/abs_cosine 透传 WP2b/防扎堆升级落点）。03 待续聊清单第一项完成。
- **03 文档补"300/50/4000 参数来历"**（待续聊清单第二项完成）：全查证——三参数全是经验值：300（ADR-0006"300 是经验值"，四档消融 200/300/400/500 规划未执行，METRICS/eval/ADR 三处查无结果）、50（module-017 设计理由 + 业界 2026"overlap 零收益"新发现需消融未做）、4000（工程经验值，module-031 重建 >8000 父块 68→0 验证上限生效但阈值无实测）。三实锤：消融没跑/验收是现状验证非最优验证/chunk size 拐点实验未做过。面试口径：经验值+消融 TODO 诚实交代比假装最优可信。
- **03 文档补"拐点实验详解"**（用户问 256/512/1024 拐点实验是什么）：chunk size 是超参数（切小=语义碎/切大=分数稀释跨主题污染）；实验 5 步（固定一切→只动 chunk_size→每档重灌→每档评测→画曲线找拐点）；拐点=性价比最高点（先升后平/降，左边爬坡右边掉分）；类比切西瓜块大小/钓鱼网眼；为什么没做（每档全量重灌 CPU 2h×4 档+牵动四层+已达标）；缺基建（无参数扫描脚本，golden_retrieval 参数固定）；300 落在业界甜点区（256-512）有合理性但无实测。
- **03 文档补"防扎堆详解"**（待续聊清单第三项完成）：用户记忆修正——扎堆发生在候选阶段（子块并集 ≤20 名额被同父块刷屏→其他文档子块被挤出→映射后其他文档父块进不了 top-5）；映射后去重合并，所以"top5 全同一个父块"不发生，真实后果是"同篇文档不同 section 霸占"。拼标题会加剧（同父块子块带相同标题集体高分），故防扎堆是 WP-A 必配。两方案：每父块限 2 命中（简单）/ 聚合式（选定：按 parent_id 分组→父块分=组内最高子块分→附带组内全部命中子块，名额公平内容完整）。类比选秀海选名额分配。
- **03 文档补全三个话题的面试话术**（用户指出"上面两个的面试怎么讲没加进去"）：参数来历小节补 30 秒话术（替换"面试口径"后的空引用）、拐点实验小节补 30 秒话术、防扎堆小节补 30 秒话术（替换"见上"）、文末"四、面试话术"汇总区补 5 段（分块总述/small-to-big/参数来历/拐点/防扎堆）。教训：落档内容必须连"面试话术"一起写全，不留"见上/见文末"空引用。
- **03 文档补漏（用户指出）**：small-to-big 小节的"面试话术：见文末"空引用也补全为完整 30 秒话术（取最高分 vs 求和决策版）。最终检查：4 个小节各有独立"面试话术（30 秒）"完整段落 + 文末汇总 5 段，空引用清零。
- **03 文档补"候选→重排→父块映射完整链路"**（用户确认"简单，记一下就行"）：engine.py:176-192 实测——① retrieve(top_k×2) 粗召回候选 → ② reranker.rerank 精排裁剪回 top_k → ③ _expand_to_parents 子块映射父块。为什么 2 倍候选（engine.py:176-177 注释：rerank 更准但慢，先粗筛再精排留裁剪空间）。关键认知：被挤出候选集的文档 rerank 救不回来，故防扎堆必须在候选阶段解决（海选/决赛评委类比）。小节带完整 30 秒话术 + 文末汇总区补第 6 段"检索链路"。待续清单加 [x] 项。
- **03 文档三通道 vs 两通道口径修正**（用户问"怎么没有三通道"）：查证发现我链路表格写岔——默认**三通道**（config.py:79 `retrieval_fusion_mode="rrf"` 默认值，FTS+向量+图谱三路并行 `_execute_fusion`，retriever.py:427）；"两通道 hybrid"只在①显式配置 hybrid ②某路失败缺路降级 ③融合异常回退 `_execute` 时出现；chat 路径 round 1/2 单路混合无图谱。候选数修正：058 brief 的"每路 10、并集 ≤20"是旧口径，代码实测 `fetch_k = top_k×2`（search 默认 top_k=5 → 每路 20、三路并集 ≤60）；放大是两层 2 倍（engine 5→10 → retriever 10→20 每路，共 4 倍）。防扎堆小节/类比/话术/链路表格/汇总话术全部同步修正。教训：写链路数字必须从配置默认值 + fetch_k 公式推导，不能沿用 brief 设计稿口径。
- **03 文档"每路 10"误记更正**（用户指出"代码没改"）：读 058 brief task-brief.md:14 原文——"FTS/向量各 `fetch_k=top_k*2` 候选 + 图谱 `search_related(top_k)` → 三路 RRF（k=60）融合 → 取 top_k"，**与代码完全一致**；"每路 10、并集 ≤20"是我早前对话的误记，根本不是 brief 原文，代码从未改过。另核实图谱通道实际也取 fetch_k（retriever.py:256 `search_related(entities, top_k=top_k)` 传的 top_k 参数=fetch_k=20），三路并集 ≤60。文档已改为"误记非 brief 原文"表述。教训：引"XX 文档写的"必须先回原文核对再下结论，不能凭对话记忆给设计稿安出处。
- **03 文档补"hybrid 类名是历史遗留"**（用户问"第一行是混合检索还是三通道"）：`hybrid_retriever.retrieve()` 的 hybrid 是**类名**（`class HybridRetriever`），早期两通道混合时命名，module-053 加图谱后未改名；真正决定通道数的是 `retrieval_fusion_mode`（默认 rrf → `_execute_fusion` 三通道；显式 hybrid/融合异常 → `_execute` 两通道）。retriever.py:138-142 分支证据已写入文档，防面试被类名误导。
- **03 文档防扎堆/拼标题"均未实施"修正（重要，用户确认）**：用户澄清防扎堆和拼标题**都没做**、二者是配套设计要一起做。拼标题未做原因：成本（标题向量化=重嵌入全部子块+重灌牵动四层）+ 精度已达标（Hit@5 0.99/0.97，105 有效口径）；防扎堆是拼标题必配项，拼标题没做则单独做防扎堆收益低，一起推迟。文档全面修正：小节标题改"防扎堆+拼标题（WP-A 配套设计，均未实施）"、加实施状态声明（设计方案 vs 已实施功能区分）、"聚合式（选定）"改"聚合式（规划选定）"、两处面试话术改诚实版（"规划了但没实施 + 理由"）。待续清单：与 ADR-0006 对照、重灌流程两项关闭（改参数成本大不改）；拼标题保留 TODO 标注未实施。教训：设计稿方案 ≠ 已实施功能，落档时必须区分，否则面试话术会吹成做过的。
- **建 04-意图路由.md（深挖落档）**：代码实测 router.py（RouterAgent 239-669）+ intent_classifier.py（L4）。三层架构：①L4 小分类器（bge-m3 冻结特征 1024 维 + LogisticRegression，module-056 默认启用，337 条人造集训练，Accuracy 1.0，延迟 15-94ms，失败回退 LLM）；②L2 确定性确认（intent≠knowledge 无条件触发，FTS 术语/图谱实体/规则表三信号，零 LLM，golden 50 条误确认 0，规则表 module-055 数据驱动补 20 噪声词）；③保守兜底（任何异常→knowledge，宁多检不漏检）。多轮路由（module-063）：短句继承（<6 字符零 LLM）+ 工具历史信号 + 会话级上下文。业界对照：LLM-as-Router（原方案）→ semantic-router（embedding 余弦，与 L4 最像）→ vLLM ModernBERT（小分类器共识）→ 模型路由（RouteLLM 等，未来方向）。差异化亮点：L2 安全网是业界开源方案没有的"误判后拉回"。诚实边界：训练数据人造集未接飞轮、realtime 未实现、多轮拼接模型维度不匹配待重训。README 索引 04 置 ✅。
- **04 文档补"L2 信号命中率 + 概率阈值"（用户深挖，九节）**：①**概率阈值答案**：L4 分类器 argmax 无阈值（router.py:314 `max(probs, key=probs.get)`），LLM 路径也无 confidence 阈值——module-055 起 L2 无条件触发；0.5 阈值是历史产物（module-043 低置信触发 → module-047 threshold_scan 校准实测 golden 100 条 LLM 零漏判、confidence 全 >0.7、无法校准保持现状 → module-054 E2E 暴露高置信也漏检 → module-055 无条件触发废弃）；"用安全网取代阈值"（设阈值反而漏检：knowledge 0.45 被 0.5 拒掉）。threshold_scan.py 模拟逻辑已过时未更新。②**L2 信号命中率**：无专项统计（诚实标注待环境）——现有证据：module-055 golden 50 条非 knowledge 误确认 0、module-047 当时 L2 零触发、图谱实体"判别力最强 0 误命中"；定性分工：rule_veto 防误杀、FTS 救回（依赖语料重合度）、图谱判别力最强。现场统计未跑成（conda 三环境+base 均缺 langchain_anthropic + DB/AGE 需在线），标注待环境不伪造数字。③**⚠️ 简历弹药库过时**：06/08 文档意图路由是 module-043 旧快照（"L4 默认未启用"、"L2 低置信触发"已过时），待用户确认后更新。教训：简历文档要定期对照 module 演进核对版本。
- **04 文档补"端到端流程全景 + 用户理解纠错"**：用户复述业务理解，发现两处偏差并纠正——①❌"闲聊会去检索知识库，查到走知识库/查不到闲聊"：实际 L2 确认发生在路由决策阶段（检索之前），产出是改判意图非检索文档；闲聊路径（engine.py:286-296）一行检索不跑直接聊天回答；②❌"完全不相信 LLM 判断"：准确是"模型提议（argmax 定类）→ 规则复核（L2，仅非知识库判定）→ 保守兜底（异常走 knowledge）"，模型仍是决策主体，不信的是自报置信度。已落档 04 文档流程图 + 误区清单。
- **04 文档收尾（用户要求"将上述内容写入文档"）**：通读核对——argmax 无阈值、0.5 阈值历史（043→047→054→055）、L4 概率=置信度仅记录不决策、L2 信号命中率（待环境）、端到端流程全景、信号确认 vs 完整检索、模型提议/规则复核/保守兜底、检索无结果不改回意图，均已落档。补缺：六、面试话术区新增第 4 段"端到端流程 + 信号确认 vs 完整检索"（可背诵版）。意图路由深挖全部收口。
- **04 文档补"十、多轮对话路由详解 + L4 训练集构成"**（用户回聊两个遗留项）：①多轮路由（module-063/ADR-0015）四层机制 WP-A~D：A 会话级路由（LLM prompt 拼最近 4 轮上下文，含义完整句按字面判断防话题漂移）；B 短句意图继承（去语气词 <6 字符 + 无新特征 → 继承上一轮 intent，零 LLM，深度 ≤3，无状态从 history 推演）；C 改写喂路由（分诊式改写提前到路由前，同时喂路由+检索，FTS 术语命中短路 knowledge 省一次 LLM 调用）；D 工具历史信号（上轮 tool_calls 含 search_knowledge/generate_answer → 短 query 强制 knowledge）。设计细节：空 history 零回归、历史只取 6 轮（LLM 上下文 4 轮）、话题漂移双向防护、语气词表 8 个不追全、改写保守防 over-rewriting（业界第一翻车原因）、两条路径同改。评测：golden_multi_turn 12 条对三指标（自包含清晰度/意图保持/检索提升，检索用 prev 锚点代理口径如实声明）。②L4 训练集实测：337 条 = knowledge 132/casual 105/realtime 100；note 构成 = 基础 241 + 专有术语 40 + 边界易混 31 + 口语化 24 + E2E bug 1；+ golden.json knowledge 112 题；训练/评测分离防泄漏。待续清单两项勾掉，仅剩 realtime 实现方案。
- **04 文档 2.5 补"信号执行顺序（串行 + 优先级）"**（用户追问并行还是串行 + 信号打架裁决）：_deterministic_confirm 三信号**串行非并行**——①规则表（纯 Python 零 DB）命中直接 rule_veto 保持原判、②③不执行；②FTS（DB）命中后 ③图谱不查（if not fts_hit）；③图谱（Cypher）仅 FTS 未命中才跑。信号打架裁决：**规则表一票否决**（规则表说闲聊→FTS/图谱再命中也不转，因规则词几乎不会出现在知识库问题中、判别力最强）；module-055 把规则表提前到首位短路省 DB。两类信号方向相反：规则表=防误杀闸门、FTS/图谱=救回信号，防误杀优先级最高。
- **L2 信号命中率实测跑通（用户提供 hermes venv 环境）**：环境 = `C:/Users/white/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe`（项目实际环境，conda/managed 均缺依赖）。golden 100 条 × _deterministic_confirm 实测：**fts_term 50（knowledge 50 条全部命中=救回主力）/ rule_veto 24（闲聊 14+实时 10）/ no_signal 26 / graph_entity 0 / error 0**；非 knowledge 50 条零误确认（module-055 复现 ✓）。**重要发现：图谱实体命中 0**——实体表实测有货（哈希环/跳表/握手流程等 30+），根因=子串匹配要求实体名逐字在 query（"握手流程"⊄"三次握手"），判别力最强但召回极低，意图确认救回全靠 FTS。04 文档 9.2 从"待环境"更新为"已实测"，补信号分布表 + 图谱 0 命中根因 + 运行环境。
- **04 文档 10.2 补"多轮路由 L4 vs LLM 分工"**（用户追问多轮是否用分类器）：config.py:187/195 实测——intent_classifier_enabled=True（L4 默认启用）、intent_classifier_multi_turn=False（多轮拼接默认关）。多轮三层分工：短句（<6 字）规则继承零 LLM 零分类器（省成本主力）；长句 L4 分类器按字面 1024 维判断（无多轮上下文）；LLM 仅回退路径但唯一携带多轮上下文（prompt 拼最近 4 轮）。2048 维拼接预留未启用（需 multi_turn=True + 多轮重训模型，当前维度不匹配 fail-open 回退 LLM）。结论：多轮用上了分类器但不只靠 LLM。
- **04 文档 10.3 补"短句继承两个机制细节"**（用户追问 6 字符依据 + 上一轮意图怎么知道）：①6 字符=经验值无消融（task-brief"<6 字符或<3 词"业界惯例），省略句本质=短；**先去语气词再判长度**（"今天天气呀"5 字→去"呀"7 字不继承防话题漂移）。②上一轮 intent = **无状态推演**：_last_user_turn 从 history 逆向找最近 user 消息 → _classify_prev 重新路由（非短句走 _classify_core L4/LLM）→ 继承；权衡=无状态幂等 vs 多一次路由调用（task-brief 评估后选无状态）；链式继承递归深度 ≤3。
- **04 文档 10.3 补"走一遍为什么场景"完整走查**（用户要求落档）：场景 A 单轮省略句全流程（classify → 截断 → _short_inherit 三条件 → _last_user_turn → _classify_prev(depth=1) → _classify_core → 继承 knowledge）；场景 B 链式 4 连为什么到 depth=3 追溯链断裂（router.py:444-445 返回 None → 轮3 正常路由判 casual → 轮4 继承 casual）——深度上限已知代价。上限 3 三理由：防无限递归/收益递减/正常路由兜底。
- **04 文档 10.2 补 WP-C/D 实际状态（重要修正，用户深挖"改写喂路由和工具历史信号"）**：代码实测——WP-C 改写喂路由：engine.py:250-269 分诊命中 FTS 术语→短路路由省一次 LLM 调用、改写 query 同时喂路由+检索，**但 query_rewrite_enabled=False 默认关（config.py:216）**；WP-D 工具历史信号：router.py:408-411 分支已实现，但**所有生产 classify 调用点（engine.py/main.py/graph.py）均未传 tool_history → 恒 None → 永不触发，接口预留未接入**（task-brief"若不可得则跳过"）。文档 10.2 表格修正：C/D 标注"能力已备、默认不生效"，面试口径"设计已落地、开关默认关/待接入"。
- **04 文档 10.3 补"多轮检索 query 不带历史（诚实短板）"**（用户追问"检索时会不会把两轮都带上"）：不会——engine.py:324 retrieve(current_query) 只有当前句；短句继承只改 intent 不改 query；reflector.check_sufficiency(query, docs) 签名无 history；WP-C 单句改写无历史。真实流程：意图对 → 检索"为什么"空（停用词 FTS 无词可查）→ 反思 rewritten==query → break → 降级"暂无相关信息"。**对话上下文化改写只在 eval 有（golden_multi_turn contextual_rewrite 标注 eval-only 不接生产）**——module-063 已知缺口，backlog。面试口径"意图对了检索落空，走降级/反思兜底，缺口已列 backlog"。
- **04 文档补"十一、待优化 Backlog"（用户要求汇总优化点）**：10 项按优先级——🔴高：①对话上下文化改写接入生产（多轮为什么检索落空，contextual_rewrite 仅 eval）；🟠中：②WP-D tool_history 接入（低成本即生效）、③WP-C 开关评估、④L4 多轮重训 2048 维、⑤飞轮回流、⑩简历弹药库更新（面试前必须）；🟡低：⑥图谱信号真实数据验证、⑦threshold_scan 过时、⑧6 字符/语气词表消融、⑨realtime 实现。
- **04 文档五节补"工业界产品实测方案"**（用户要求搜意图路由业界方案）：①Dify 工作流=Python 短语匹配硬编码（greeting_phrases 表，检索前拦截寒暄，检索空走闲聊 LLM 兜底）；踩坑"怎么样"短词包含误伤→手动收紧（4 字以上包含/短词相等）——本项目同款问题用 golden 扫描数据驱动修，方法论对比亮点。②RAGFlow=Retrieval 前置 + Categorize LLM 分类（RAG 增强分类：静态定义+动态检索条目注入分类 prompt），UserFillUp 循环回检索防话题漂移——本项目相反：LLM 非分类主体（L4 小分类器），L2 拉回 + 规则表 veto 防漂移更省。③Dify 内置轻量分类器（知识库查询 vs 闲聊 vs 指令）与 L4 同定位但无校准概率无 L2。对比亮点：本项目=规则匹配超集 + 数据驱动修规则 + 零 LLM 分类主体 + L2 安全网，业界独有。
- **04 文档五节补"工业界多轮意图路由对比"**（用户问"他们有多人对话意图路由吗"）：业界多轮=每轮重做——RAGFlow UserFillUp 循环重新检索+分类（省略句"为什么"检索意图库匹配不到→落 OTHER，对省略句也弱）；Dify 会话历史只给 LLM（生成侧）、意图节点无状态（每轮独立短语匹配）。本项目短句继承=业界少有的零 LLM 跨轮意图状态（从 history 推演+规则继承），差异化亮点已补 RAGFlow 行。
- **04 文档五节补"5.1 工业界产品意图路由详解"**（用户要求业界做法落档）：完整小节——①Dify 规则匹配派（greeting_phrases 硬编码、踩坑"怎么样"包含误伤手动收紧 vs 本项目 golden 扫描数据驱动修、检索空切闲聊 LLM 兜底 vs 本项目如实告知防幻觉）；②RAGFlow RAG 增强分类派（Retrieval 前置+LLM 分类、UserFillUp 循环每轮重做、省略句弱、不用每意图一库理由）；③Dify 内置分类器（同 L4 定位无安全网）；④多轮现状对比表（业界每轮重做 vs 本项目短句继承零 LLM 跨轮）+ 面试话术业界对比版。
- **建 05-查询改写-HyDE.md（开篇落档）**：HyDE 大白话（考试脑补参考答案、假文档有错没关系只认语义方向）+ 代码实测（_HYDE_PROMPT engine.py:52-56、_hyde_expand 649-695 缓存 rag:hyde:sha256 TTL300s 降级原 query、**只在流式 _retrieve 用 chat 不用**）+ 查询改写（query_rewrite.py 分诊/改写/保真0.6/并行择优，query_rewrite_enabled=False 默认关）。数据：HyDE 2.5s 首次；**无单独消融**。业界（2026）：HyDE 论文 Gao 2022 零样本；2025 降温——延迟 +25-40%（Gemma 1B/4B）、事实域幻觉风险、原版仅 +2% ACC@1（query dilution，query-based re-ranking 72%）、hybrid 条件启用是改进方向；复杂开放问题值得、简单事实问题直接 RAG。README 05 置 ✅。
- **05 文档补"HyDE 调用链 + prompt 质量分析"**（用户质疑"没看到调用"+"prompt 太简单"）：①调用链核实：main.py:496 流式端点 → _retrieve → _hyde_expand(771) → _HYDE_PROMPT(677) ——HyDE 确实被调用，只在流式路径（chat 主路径无）；②prompt 简单质疑成立：无 few-shot 样例无领域指令（对比 _REWRITE_PROMPT 有要求列表）；为什么可能仍有效=HyDE 只要语义方向对不要求风格逐字一致（论文原版也简单）；业界 2025 改进=领域 prompt+few-shot（医学 +4.9% NDCG）；改进方向=知识库抽样例/领域指令/消融验证（无数据不盲改）。
