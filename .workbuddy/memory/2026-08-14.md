- **向量存储选型讨论 + 并入 WP-A 规划**：用户问"向量还能用什么存、为什么选 pgvector"。查证+搜业界（53AI/glukhov/cnblogs 选型框架）：本场景约 5000-10000 条向量（124 篇×300 字子块），业界 <10 万条 = pgvector/Chroma 档；pgvector 优势 = 零新增部署 + 同库事务/join（向量是 documents 表一列）+ 运维零成本；Milvus 杀鸡用牛刀（etcd+MinIO 重运维）、FAISS 无持久化无 CRUD。已把"向量存储选型确认"小节写入 specs/module-058-retrieval-chain-opt/task-brief.md 的 WP-A（拼标题 reindex 前置决策）：结论维持 pgvector + 执行时核对 2 件事（pgvector 索引已建 HNSW/ivfflat、向量维数 1024 与 bge-m3 一致）+ 迁移触发条件（百万级再评估 Qdrant）+ 面试口径。
