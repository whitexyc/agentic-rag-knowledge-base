# 2026-08-13 工作日志

## 058 编号来源查证（用户问"为什么变成058"）
- 结论：不是跳号。08-12 一天内完成 module-054（检索降级修复+NLI 复测）→ 055（提示词评估优化，含 rrf 切默认 config.py:79）→ 056（L4 意图分类器上岗，默认启用）→ 057（数据验证批 5 硬数字：图谱消融 +0.2381、改写 +0.0096、RRF k 扫描平坦、NLI v2 kappa 0.4311、飞轮冒烟）→ 全量测试 667→688→699→700→740。
- 记录源：memory/agent-activity-log.md + file-index.md + project-context.md（均已同步，但 specs 目录只有 052/053/058——054-057 的 specs 文档在并行 worktree/.claude/worktrees 执行线里，主工作区未落 specs 目录）。

## ADR-0012 工具数口径与代码不符（重要事实）
- 当前代码实测：tool_registry.py 只注册 **7 个**工具（search_knowledge/search_fts/search_vector/search_graph/extract_entities/recall_memory/generate_answer），react.py:196 每轮全量 to_llm_schemas() 暴露，无阶段状态机。
- ADR-0012 声称 10 个（含 verify_answer/re_search/note_to_self）——grep 全 ai_service 无注册：verify 是固定流水线 HHEM 裁判方法（非 ReAct 工具），re_search/note_to_self 查无此物。又一起"文档≠代码"。
- main.py 有两条 ReAct 路径：react_loop（:518）+ langgraph_react_loop（:579，module-030），方案 A 需同时覆盖。

## Q1-Q3 结论（工具治理 P1 实施前）
- Q1 阶段判定：推荐"以是否调用过 generate_answer 为界 + 允许回退"的状态机（ctx.generated 标志），非"docs 非空"（会切断补检能力）。
- Q2 归组：verify_answer 只放生成组合理；re_search 应双组可见（反思不充分时生成阶段要重检），硬切会阉割"验证→重检→再生成"闭环。
- Q3 现在做：同意用户"现在做"倾向，但动机修正为"结构性防误调 + 省 schema token + 面试叙事"，非防退化（7 个远在阈值内）。建议按真实 7 工具重写 brief 并顺带修 ADR-0012。

## 落档完成（用户确认"可以"）
- 新建 `specs/module-059-tool-phase-split/task-brief.md`：工具治理 P1 阶段切分（WP-A 状态机半天 / WP-B 回归+E2E / WP-C 文档收口；纪律：两循环同改、单向前进、开关 PW_TOOL_PHASE_SPLIT、不新增不删工具）。
- 更新 `specs/adr/0012-tool-governance.md`：工具口径 10→7（verify/re_search/note_to_self 非工具）+ 状态行 P1 已立项 + 方案 A 状态机定稿（ctx.phase、补检口 search_knowledge、单向前进）+ 面试话术 7 工具版。
- 更新 `CONTEXT.md`：ADR-0012 行 + 新增 058/059 索引行。

## 同步远端代码后复核（重要修正）
- 用户同步 worktree-m8-knowledge-panel（merge b42da5d），**代码实为 10 个工具**：verify_answer（module-039，需 docs+answer）/ re_search（module-040，需已有 docs，本质反思-重检工具）/ note_to_self（module-041，scratchpad）均已注册——**我上一轮"7 个工具、ADR 口径不符"结论作废**（基于同步前代码）；react.py:213 / langgraph_react.py:89 全量暴露。
- **059 brief + ADR-0012 已按 10 工具重写**：检索组 7（含 re_search）/ 生成组 4（generate_answer+verify_answer+note_to_self+re_search 双组）；阶段判定 = 已调 generate_answer 或 verify_answer 为界；Q2 用户直觉被代码证实（re_search 实现要求 docs 非空 → 双组）。
- **CONTEXT.md 被远端版本覆盖**（174 行维护版 → 24 行旧术语表），索引行丢失；已重建索引（ADR-0010/11/12 + 052~059 全量）。
- specs 目录仍无 module-054~057 文档目录（代码合入、specs 未合）。
- 遗留：058 brief 基线 0.9905(id=18) 等事实未逐一复核（同步后测试数未知，可能 >740）；下轮如需可跑定向测试。

## CONTEXT.md 恢复与口径统一
- 用户恢复了 CONTEXT.md（177 行同步前维护版，历史领域术语全回来）；恢复版中 ADR-0012 行/059 行/工具治理术语段仍是"10→7 已修正 / 检索组 6+search_knowledge 补检口 / 生成组 3"旧口径——已按 10 工具复核结论修正 3 处 + 补 054~057 索引行。

## 用户执行决策落档（文档已同步）
- **059 并入 058 一起执行**：058 brief 头部加合并说明 + 新增 WP-E（工具治理要点，引用 059 brief）；059 brief 头部标"已并入 module-058 执行"；ADR-0012 状态行改"P1 已立项，并入 module-058 执行"；CONTEXT.md 三行同步。
- **WP-A 拼标题推迟**（用户决策：三通道 RRF Hit@5 0.9905 基线饱和）：058 brief WP-A 标 ⏸️ 推迟，注明后续做时须三口径对比（Hit@5+Recall@5+MRR，057 改写 MRR -0.0353 提示精排面仍有空间）+ 防扎堆聚合式必配；058 交付物 WP-A 划推迟。
- B 方案评估结论（用户问"直接做 B 不做 A"）：B1（intent 分流 casual 零工具）已是现状（main.py:403 / engine.py:245）；B2（两级路由）10 工具=过度设计违背 ADR 量级判据；A 是 B 的前置，路线不变。

## 058 brief 用户审阅修正（2026-08-13 深夜）
- WP-D 验收残留"golden 对比表（拼标题 vs 0.9905）"与 WP-A 推迟矛盾 → 已标"WP-A 推迟无此项"。
- 加固 1：WP-E 通过标准补 conftest autouse fixture 钉住 PW_TOOL_PHASE_SPLIT=false（对齐 module-056 开关模式），新测试显式开 true——默认 true 会漂移 react 层存量 agent 测试。
- 加固 2：WP-C request_logs 建表走 init_db 自愈幂等 DDL（对齐 module-048 feedback 表，不另起迁移脚本）。
- 059 brief WP-A 通过标准同步补第 ⑦ 条 conftest 钉住。

## 记忆机制 visio 图（用户要求"记忆机制也画一张"）
- 产出 `visio/Memory-Mechanism-v1.vsdx`（3 页）+ `visio/memory-mechanism.json`（spec，可改可重生成）。
- 风格对齐 Agentic-RAG-Flow-v3：00 说明页【大白话/逻辑/面试】三段式 12 块 + 颜色图例；01 三层结构总览（读写主链路）；02 短期进化生命周期 + 去重。
- 内容基于代码实测：三层 source / extract_facts importance≥0.6 / 语义去重 0.85 / 动态 K 绝对余弦 / 衰减 0.5^(age/3)+提及加权 / 30 天硬上限 / 升级幂等 / 身份隔离双保险 / 会话摘要 MemGPT 公式 / 已知局限 4 条。
- 流程复用：visio-skill → spec JSON → `New-VisioDiagram.ps1`（需 dangerouslyDisableSandbox 因 Visio COM）。
- **踩坑**：① PowerShell 工具 stdout 被吞 → 用 Out-File 写日志再读；② ConvertFrom-Json 报"应为:或}"——根因是 text 值内裸双引号「防"检索一次即永生"」，改中文引号「」修复；③ connection 目标引用跨页节点会 throw——02 页 e6→long 改本页 e6t。

## 记忆纠错/淘汰方案落档 ADR-0007（用户"放到07"）
- 用户追问"≥2 次/7 天升级是否太死板、记错了怎么办"→ 调研业界（Mem0 四操作 ADD/UPDATE/DELETE/NOOP、Zep Graphiti 双时态 SUPERSEDED +18.5%、A-MAC 类型化衰减 偏好0.5%/天·动作20%/天、TiMem hot buffer probation、Mem0 Memory Decay 0.3x-1.5x、Ghost Context 写路径消解）→ 落档。
- ADR-0007 v2：状态"暂不实施"→"部分已实施（module-034/046：会话摘要+短期进化）+ 纠错/淘汰待实施"；现状基线同步 30 天硬上限/半衰期/提及加权/升级已落地；新增问题 4（三软肋：升级单向、去重追加非替换 _merge_duplicate 拼接、阈值一刀切）+ P0-P4 方案（撤销/冲突消解复用 mDeBERTa NLI/类型化/降权/反馈闭环）；对照表 9 行更新；面试话术 v2。
- CONTEXT.md ADR-0007 行同步。

## 求职 + README 更新（10:26-12:45）
- 求职任务：收到两家 HR 的 6 问（职业规划/为什么投/岗位匹配/优缺点/实习期望/作品链接）——① 金舟软件（callmysoft.com，EzClaw 本地 AI Agent + AI 写作/绘画/语音转文字/翻译）；② 厦门某 DC 公司（2027 届、业务域客服/HR/运营 AI 提效、Base 厦门、演武草堂/人才公租房福利）。生成两版回复（长版+400 字短版），3 个缺口待用户确认（DC 公司全称/2027 届身份/厦门 Base）。
- 项目链接实测：GitHub 主页 https://github.com/whitexyc ，主项目 https://github.com/whitexyc/personal-resume-website （git remote 确认）。
- README.md 重写：对齐当前代码（RRF 默认 0.9905 / 10 工具 / L4 分类器 / 记忆进化 30 天+半衰期+升级 / HHEM 幻觉裁判 / 740+ 测试 / 12 份 ADR / 飞轮），补 PW_RETRIEVAL_FUSION_MODE 等环境变量；工具治理标"规划中"（未实施不写死）。

## 同步后复核（12:59）：module-058/060/061 全部落地，README 二轮更新
- git merge 6de3794：module-058（检索链优化+工具治理 WP-B/C/E）/ 060（verify 异步化，ADR-0013）/ 061（记忆纠错，ADR-0007 P0+P1）完成。
- **工具治理已实施**：tool_registry group（检索组 7/生成组 4，re_search 双组）+ react.py ctx.phase 状态机 + langgraph 同步 + PW_TOOL_PHASE_SPLIT 默认 true + conftest 钉住 false。
- **可观测性已实施**：src/observability.py（trace_id 贯穿日志 TraceIdFilter + 阶段计时 + usage 供应商桶 + 缓存计数）+ request_logs 表（init_db 幂等）。
- **verify 异步化已实施**（060）：verify_results 表 + 轮询端点 + 前端 2s/30 次轮询，PW_VERIFY_ASYNC 默认 true。
- **记忆纠错已实施**（061）：P0 升级不删短期副本（后悔药）+ superseded 标记 + 召回过滤；P1 NLI 冲突消解（mDeBERTa 557MB，_merge_duplicate 分流）——评测 contradiction Recall 0.5<0.8 未达门槛 → PW_MEMORY_CONFLICT 默认 false（诚实）。
- **最终测试数 824/0**（46 个测试文件）；README 更新：测试数 740→824、工具治理✅、幻觉检测异步化、记忆纠错、工程实践加可观测性、环境变量表 +PW_TOOL_PHASE_SPLIT/PW_VERIFY_ASYNC/PW_REQUEST_LOGS。
- CONTEXT.md 索引修正 3 行过时（ADR-0012"10→7 已修正"→✅已实施；058"📋"→✅ WP-B/C/E 实施 WP-A 推迟；059"检索组 6 旧分组"→✅ 随 058 完成）+ 补 module-061 行。

## 重排截断口径修正（用户纠错：250 不是 500）
- 用户问"重排不是 250 字符吗"——核实 `reranker.py:64 _MAX_PAIR_CHARS = 250`（module-044 九档实测采纳），**README 上一轮写的"截断 500"是抄 CONTEXT 旧口径的错误**（又一起"文档≠代码"，教训：改 README 必须读代码，不能抄文档）。
- 九档数据（2026-08-09，6 pair）：2000→45.4s / 1000→19.7s / 500→8.9s(1.48s/对) / **250→5.0s(0.83s/对) 采纳** / 200→4.2s 同档安全 / 150→3.5s 分数拐点(弱相关跌破0.4) / 100→2.6s / 75→2.3s 主文档掉分 / 50→2.0s 主文档崩溃。250 相对 500 提速 44%、相对 2000 提速 89%，250→200 差≤0.003、6/6 一致。
- 修正 4 文件：README（截断 250 + module-044 标注）/ CONTEXT（:16 改 250、:49 重写九档表、:51 TODO 标已完成）/ 08 文档 1.4 节（标题、截断原因、九档选数表、TODO、content[:250]、面试话术、L29 表格）。

## README 面向读者重写（用户批评：内部术语/实施状态对读者无意义）
- 用户指出："工具阶段切分已实施""L4"这类话 README 不该说——README 是给别人看的，应描述功能而非实施进度。
- 重写原则：① 删光"已实施/已立项/module-0xx/ADR-00xx/ctx.phase/SUPERSEDED/NLI/449 条/Acc 1.0000"等内部语；② 内部术语换通俗（L4 分类器→本地轻量分类器、RRF→融合排序、small-to-big→小块检索大块回答、MemGPT 摘要→长对话自动摘要）；③ "开关默认关待评测"等未完成痕迹不出现；④ 环境变量表保留 PW_ 前缀（本就是环境变量名）。
- 功能条目改为"是什么 + 用户得到什么"句式（如"按执行阶段分组暴露——检索阶段只见检索工具、生成阶段才见生成工具，避免误调用、节省 token"）。
- 教训沉淀：README = 对外文档，用功能语言；内部进度/术语只留在 CONTEXT/ADR/日志。

## 简历更新（用户："看不出细节，去简历改"；README 保持对外简洁）
- 用户指出 README 看不出项目细节——技术细节应进简历（精简版 + 详细版），README 保持对外口径。
- **07-项目经历-精简版.md**：① 补工具阶段分组暴露（检索组 7/生成组 4 结构性防误调省 token）② 补验证异步化（答案先回、后台验证、前端轮询）③ 449 条训练（337 人造 + golden 112 并入）④ 记忆纠错（升级留底 + SUPERSEDED 标记）⑤ 反馈闭环扩为"反馈+可观测性"（verify_results 落库 + trace_id/request_logs）⑥ 测试 740→824、模块 57→61。
- **01-项目经历简历.md（详细版 v2.1）**：① 技术栈 pytest 740→824 ② ①Agent 小节补工具阶段切分 + verify 异步化 + 小白解释 7→10 工具（新增"工具阶段分组"条目）③ ②检索小节：加权融合 0.9714/105 有效口径 → **RRF 三通道 0.9905/112 题全量**（小白解释补 RRF 原理 + 消融 +0.24，删 105 有效口径、0.91→0.97 旧史）④ ④记忆小节：TTL→进化机制 + 纠错 ⑤ 成效表 0.97→0.99、740→824、57→61 ⑥ 速记：614→824、7 工具→10 工具分组、数字复现改 RRF 命令。
- **⚠️ 提醒用户**：02-简略版/03-完整版/05-修订版 数字也全过时（0.9714/57/740），未动（用户只指定精简+详细版），需确认是否一并更新。
- 教训：简历文档（01/02/03/05/07）是面试主材料，改动后数字必须与 activity log 最新口径一致（824/61/0.9905/250/10 工具）。

## 多格式文档解析方案落档（用户场景：只处理 md、要扩多格式 + 数据清洗 + PDF 图片）
- 场景澄清：前端 UploadPanel.tsx:149 只开 md/txt；后端 main.py:911 upload 只认 PDF（PyMuPDF）——**前后端割裂**（有 PDF 端点前端没接）；Word/Excel/PPT 全无。
- 调研：AnyDoc（firecrawl/anydoc，14.1k★）Rust 14 格式统一转 GFM MD，中位 4.4ms（vs pandoc 102ms/LibreOffice 1129ms/markitdown 135ms），LLM 盲评 81 分全格式第一，零依赖无模型，Python 绑定释放 GIL；**边界：扫描版 PDF 不 OCR、PDF 路径比 Office 弱**。MinerU = 复杂版面重炮（版面/公式 LaTeX），用户嫌重 → 只路由不默认启用。Docling 管线（占位符+VLM 描述）为图片方案参考；Dify 已内置 MinerU/MarkItDown 验证方向。
- 落档两份（module-062 已被记忆优化占用 → 编号 063）：
  - **ADR-0014** 文档解析与清洗策略：决策 1 AnyDoc 主通道（选型对比表）/ 决策 2 清洗五步（格式清理/冗余/结构恢复/语义修复/分块准备）+ 白名单哲学（按块类型作用）/ 决策 3 嵌入前归一化边界 / 决策 4 PDF 图片三层（PaddleOCR + 本地 VLM 描述 + MinerU 路由，默认关）/ 决策 5 原始文件留存；面试话术。
  - **module-063** 任务简报：WP-A 上传链路扩展 / WP-B AnyDoc 接入（rag/parsing.py + 错误变体映射 + PyMuPDF 回退）/ WP-C 清洗五步（rag/cleaning.py）/ WP-D 图片通道默认关 / WP-E MinerU 路由默认关 / WP-F original_path 落盘 / WP-G 验收（824 基线 + 多格式 fixture + E2E）；纪律：下游零改动、默认路径零新增依赖、不假成功。
- CONTEXT.md 补 ADR-0014 + module-063 索引行。

## 文档去重方案补入 ADR-0014（用户"帮我记入文档中"，v2）
- 用户追问"很多高度相似文档如何取舍"，调研（webclaw 三分类 / app-lab 去重粒度 / ai-rng canonical + boilerplate / tsight 算法演进）→ 大白话解释（双胞胎指纹/人脸识别/换衣服类比）→ 落档。
- ADR-0014 新增决策 6：三级分类（完全相同→哈希直接丢 / 近似→SimHash/MinHash-LSH 聚类选择性保留 / 语义→embedding 余弦 ≥0.95 不直接删）+ canonical 取舍策略（留最新/结构最完整/元数据全，其余 duplicate_cluster_id 关联检索抑制）+ 粒度两级（文档级先、块级后，省 30-50%）+ 三坑（boilerplate 先剥离 / embedding 不跨源折叠 / identity 与 content 分离保版本化）。
- 规模判据：124 篇 O(N²) 本地秒级够用；几千+ 再上 MinHash-LSH（datasketch，>0.8 阈值 99% 准确率）。
- module-063 WP-C 补"文档级去重"实现段（L0 content_hash 复用 / L1 结构指纹 / L2 余弦 ≥0.95 + canonical + duplicate_cluster_id + 检索抑制 + boilerplate 剥离）；CONTEXT.md ADR-0014 行同步。

## 07 简历 ATS 化改造（用户：投递用 07，要有亮点+量化+ATS 友好）
- 用户痛点：简历看不出亮点、普通；企业用 ATS 筛（搜索确认 2026 规则：单栏/标准标题/Summary 埋关键词/bullet=动词+任务+关键词+量化/表格和 emoji 会被 ATS 跳过）。
- 07 改造：① 顶部加"核心优势"段（一句话卖点：从 0 自研 Agentic RAG 全栈 61 模块 824 测试全绿 AI 协作 + 4 条量化 bullet：0.99/824/61/3ms/124 篇）② 量化指标表 → 纯文本 bullet（ATS 友好）③ 补教育背景/专业技能（精简版原来只有项目）④ 核心工作保留 5 点深度但首句卖点化。投递用 07 不是 05；05/02/03 仍数字过时待用户决定。
- 亮点定位（给用户的）：深度（全链路自研+12 份 ADR）+ 工程化（61 模块/824 测试/评估回归）+ 量化（0.9905 带基线对比 +0.24）+ 时代感（AI 协作开发）——同龄人稀缺组合。

## 07 简历定稿 + 旧版归档（17:24）
- 用户反馈迭代：量化融入核心工作（不要单独量化表）→ 07 定稿（5 点核心工作每点 = 特点 + 技术 + 量化内嵌；删量化指标表；824/61/14 ADR 收在可靠性第 5 点）。
- 简历目录归档：02 简略版 / 03 完整版 / 05 修订版（数字过时 0.9714/57/740 且与 07 重复）改名加"-旧版归档"后缀（可逆，未删除）；01 详细版 / 04 评审 / 05 速记卡 / 06 弹药 / 08 深挖 / 优秀样例保留。
- 简历现状：投递主版 = 07（精简，量化内嵌）；面试深挖 = 01（详细）+ 06（弹药）+ 08（逐词深挖）。

## 07 二轮精简（17:29，用户：内容太多/不突出重点/要简洁清晰低理解难度）
- 每条核心工作压到 1-2 行（动作 + 最能打的数字），全文一页扫完；砍掉次级细节（449 条/HHEM 110M/jieba/父子分块细节/30 天/≥2 次/15s/14 ADR）——移交 01/08 面试深挖。
- 07 保留数字：0.9905（+0.24 消融确认）/ 3-5s / 3ms / 824 测试 / 61 模块 / 112 题。
- 简历精简原则沉淀：简历 = 动作 + 1 个数字；细节/口径/深挖全部留给 01 详细版 + 06 弹药 + 08 逐词深挖。

## 07/09 双版本简历定稿（17:31，用户：上一版也要保留）
- 07-项目经历-精简版 = **极简投递版**（每条核心工作 1-2 行，动作+数字，30 秒扫完）。
- 09-项目经历-完整版 = **上一版保留**（量化融入核心工作完整叙述版，17:27 删中文 FTS 句后的版本，含 449 条/HHEM 100 对/30 天/≥2 次/14 ADR 细节）。
- 简历文件体系：07 投递极简 / 09 完整叙述 / 01 详细(小白解释) / 08 逐词深挖 / 06 弹药；02/03/05 旧版归档。

## 10-项目经历-结构化版（17:50，用户贴【】版修正后入库）
- 用户贴一版【】标题结构简历，评审发现 5 处问题并修正：① Milvus/Neo4j → pgvector/Apache AGE（项目 1 技术栈写错，穿帮点）② "Schema Token 省 30%" 无实测 → 删数字 ③ "+2.4%" 混口径（0.9714→0.9905 实为 +1.96%；+0.24 是图谱单通道绝对增量）→ 拆分表述 ④ "P50/P95 看板"夸大 → 可查询 ⑤ "成本精确量化" → 可估算。
- 修正版存 10-项目经历-结构化版.md（【】标题 + 6 维度 + 因果逻辑，结构最优）；07/09 保留不动。
- 教训：简历数字必须对口径（+0.24 是单通道消融 delta，0.9714→0.9905 是两通道→三通道差 0.0191），写错会被追问穿帮。

## 00-项目信息包（18:04，用户要"把项目信息给 AI 让它生成简历"）
- 用户反馈 10 版是"AI 只看 07 生成的"所以不准（缺技术栈准确清单→AI 自补 Milvus/Neo4j；缺口径→算错 +2.4%；缺边界→编 30%）。
- 新建 docs/简历/00-项目信息包-供AI生成简历.md：9 节（项目身份/技术栈准确清单/一句话定位/痛点规模/核心模块逐条=动作+技术+量化+因果/量化总表带口径/诚实边界/项目2⚠️未核实/给 AI 的生成指令模板）。含防穿帮设计：技术栈白名单（禁 Milvus/Neo4j）、数字口径表（+0.24 是单通道绝对增量）、📌设计收益勿写数值、⚠️未启用/未核实标注。
- 用途：用户把整份文件丢给任意 AI + 指令模板 → 生成不穿帮简历；07/09/10 都是它的派生。


- **module-062 完成（记忆进化 2，897/0 全绿）**：WP1 类型判断双方案（clf 1.0000 / LLM 1.0000 同分取 clf，零成本确定性离线）+ WP2 类型化衰减（documents.type：preference 30 天 / event 1 天 / 其余 3 天零回归，PW_MEMORY_TYPE_DECAY 默认 true）+ WP3 冷记忆降权（last_recalled_at 距今天数 ×0.3-1.0 不删除 + 召回刷新升温，PW_MEMORY_COLD_DECAY 默认 true）+ WP4 矛盾检测启用（clf P 0.9048/R 0.95 vs mDeBERTa P 1.0/R 0.5 同集对比，按"Precision≥0.8 启用、双达标取 Precision 高者"→ nli 启用 PW_MEMORY_CONFLICT=true + JUDGE=nli，clf 可切换；覆盖 module-061 默认关）。Reviewer MAJOR-1 修复：_apply_cold_decay naive/aware 时间差（冷降权对被召回过记忆恒不生效）→ _cold_ref_time 补 replace(tzinfo=utc)，+2 测试。
- **文档同步（062 完成后）**：00 信息包（记忆条目补类型化衰减/冷降权/冲突消解已启用——推翻了"NLI 默认关勿写"旧口径！测试 824→897、模块 61→62）+ 07/09/10/01 简历（824→897、61→62，记忆描述补 NLI 判别 Precision 1.0）+ README（897 项/47 文件、14 份 ADR、记忆行补类型化衰减/冷降权/冲突判别、补 PW_MEMORY_* 三个环境变量）+ CONTEXT.md（修 ADR-0012/058/059 三行过时口径 + 补 0014/061/062 索引行 + 061 领域段落补"已过时以上条为准"衔接）。
- **文档管理三层架构落地（渐进式披露）**：盘点全项目 1502 个 md（specs 218 个）；用户索引文档 = memory/file-index.md（166 行，文件级索引）+ CONTEXT.md（221 行，知识级索引）+ project-context.md（147 行）三份分工明确但缺"阅读协议"。
- **CLAUDE.md 10.4 更新**：新 Agent 入场阅读顺序重构为 L0 常驻（project-context/CLAUDE/CONTEXT）→ L1 按需（file-index/模块目录/activity-log）→ L2 深度（ADR/METRICS/简历信息包）三层渐进式披露，明确"不要全量读文档（context rot）"原则。源文件：C:/Users/white/.claude/skills/vibe-coding-workflow/CLAUDE.md。
- **file-index.md 修数字**：module-061 补"062 后已启用"衔接 + module-062 895→897（Tester 验收含 2 修复测试）。
- **00 信息包丰富（三层按需，非全扫）**：量化总表补记忆类型分类器 1.0（120 条）、矛盾检测 Precision 1.0（mDeBERTa 30 条实测）、充分性 1.0（100 题）；新增"核心模块 8：记忆进化 v2"（双方案对比取 clf + 矛盾启用决策规则 + 诚实边界）；生成指令模板数字清单同步。
- **00-信息包丰富（按缺口补，非全扫）**：①检索模块补 RRF vs 加权选型因果（图离散计数量纲难校准）+ HyDE（独立缓存键 300s + 失败降级）+ 反思充分性判断（不足自动改写重查最多 3 轮）+ 分块 4000 上限细节；②Agent 模块补手写 ReAct vs LangGraph 取舍 + 并发安全（GGML_ASSERT 坑/asyncpg session）+ 16 路并发一致；③可靠性补测试口径（897 = 单测+集成，eval 分家）；④量化总表补 16 路并发/HyDE 缓存零额外 LLM；⑤08 深挖同步修正多处过时口径（详见下）。
- **08 深挖文档过时口径大修（面试防御材料，过时会穿帮）**：2.4 "默认仍 hybrid" → module-055 已切默认 rrf（config.py:79 实测）+ 面试话术 3 处同步；2.7 标题 0.96 → 0.9905 全新口径（112 题/105 有效、图谱 +0.24 绝对增量勿写 2.4%）；2.6 评测集 30 题 → 112 题；3.1 记忆"可设计未实现"→ 全部已实施（module-034/046/061/062，强化/衰减/升级/冷降权/纠错）+ 面试话术同步；弹药 Q10 355→897、Q11"反馈没落库"→ 已落库（/ai/feedback module-048）；顶部对照表 0.97/30 题/TTL 7 天/614/42 模块/RRF 行全修；风险清单 6 行重写。
- **多轮意图路由方案 + ADR-0015 落档**：问题——engine.py:237 classify(request.query) 只传当前句，历史没进路由，省略句/指代句（"为什么""那图谱呢"）误判闲聊。方案三步：①会话级路由 classify(query,history[-4:])（半天，历史窗口 3-8 轮业界共识）；②短句意图继承（规则层：去语气词 <6 字符且无 FTS/图谱特征 → 继承上轮 intent，中文先去语气词"为什么呀"→"为什么"）；③复用现成分诊式改写喂路由（现有 Hybrid 改写=业界标准形态，一次改写路由+检索两头受益）；④工具历史规则信号（可选）。业界数据：百度千帆实测 召回 62.5→89.2% / 准确率 58→87.5% / 轮次 3.2→1.8；三大坑：历史不全塞/改写保守防 over-rewriting/继承防话题漂移。ADR-0015 已写（含验收标准），CONTEXT 索引已加。
- **module-064 task-brief 落地（多轮意图路由，ADR-0015 执行层）**：文件 specs/module-064-multi-turn-intent-routing/task-brief.md，5 WP：A 会话级路由（classify(query,history=None) 空历史零回归，取最近 4-6 轮，L4 分类器拼接最近一轮向量，memory_conflict_clf 双拼接先例）/ B 短句继承（去语气词表哦呢呀啦请问那个，<6 字符且无 FTS/图谱特征 → 继承上轮；状态取 history 推演无状态优先）/ C 改写喂路由（现成分诊式改写=Hybrid 标准形态，改写结果同时供路由+检索，分诊命中术语可短路路由省一次 LLM）/ D 工具历史信号 + golden 多轮 10 条三指标（自包含/意图保持/检索不降）/ E 回归（897 基线 + 新增测试，两路径同改 chat+stream，不改意图类型）。CONTEXT 已加 064 行 + ADR-0015 状态改"已立项 module-064"。
- **063/064 编号对调（用户想先做路由）**：module-064-multi-turn-intent-routing → module-063-multi-turn-intent-routing（多轮意图路由，先做）；module-063-multi-format-ingestion → module-064-multi-format-ingestion（数据清洗入库，后做）。已同步：两份 task-brief 标题编号、CONTEXT.md 索引（ADR-0014→064 / ADR-0015→063 + 两条 module 行）、ADR-0014 元信息两处引用。
- **补漏：ADR-0015 状态行未随对调更新**——用户指出后修正："方案已定，待实施（无需单独模块简报）" → "已立项 module-063（执行简报 specs/module-063-multi-turn-intent-routing/task-brief.md，编号对调后为 063）" + 关联行补 module-063。全局复查：ADR-0014→064、ADR-0015→063、CONTEXT 4 行、specs 目录 2 个全部一致。
