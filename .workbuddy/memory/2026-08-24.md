# 2026-08-24 工作日志

## 闭环构想 + ai-meeting-project 对接调研
- 用户构想：自动化任务(类似 WorkBuddy 自动化) + 审查改写机制 + RAG→SAG + 知识库/面试系统接码形成"检索-学习-面试"闭环
- 调研：Zleap SAG（arXiv 2606.15971）= SQL-Retrieval Augmented Generation，离线抽事件+实体(11类)，查询时 SQL join 动态连边，append-only 增量；省图谱构建钱，不省嵌入钱；MuSiQue Recall@5 80%
- CodeGraph 索引 ai-meeting-project：567文件/11531节点/21043边，init 3.8s（mcp.json 里 disabled=true，但 CLI 可用）
- 读源码确认：
  - 出题核心 = InterviewQuestionExtractionService.extractInterviewQuestions（InterviewQuestionExtractionService.java:47），第75行 callAiSyncWithFile 是注入知识库上下文的精确插入点
  - 已有 OkHttp3 客户端（讯飞 XunfeiAudioService）、@Scheduled（InterviewTurnRepairService）、星辰大模型 XingChenAIClient、BusinessAgentScene 场景配置
  - 落档：docs/项目深挖/13-知识库与面试系统闭环对接.md（架构拍板，未改代码）
- 下一步待用户拍板：SAG 完整版 vs 增量append；数据源白名单；知识库 FastAPI 是否已跑/端口

## 用户拍板（18:50）
- SAG 先做增量 append 不重建（SQL 连边留演进）；数据源白名单扩大
- 用 CodeGraph 双向查 Agentic RAG 项目：确认 `/ai/rag/search` 已存在（main.py:452，端口8001，uvicorn.run port=8001），检索核心 retriever.py:93 retrieve(mode="hybrid")；SearchRequest/Response 在 rag/schemas.py:8/13
- 更新 13 文档：真实接口契约（Java 零后端改动，只新建 OkHttp client 调 8001）+ 扩大数据源白名单（官方文档优先/掘金/思否/SO/GitHub Issues/arXiv/B站预习 + 黑名单）+ SAG 决策更新
- 下一步待用户指令开工阶段1代码（KnowledgeBaseClient + 出题注入）
