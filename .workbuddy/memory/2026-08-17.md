# 2026-08-17 工作日志

## Agent 级评估 + MCP 集成立项（ADR-0017/0018 + module-066/067）

用户选择做两个对齐"AI Agent 工程师"岗位的方向，完成查证 + 业界调研 + 落档：

### 查证（代码实测）
- request_logs 只记阶段耗时（timings/usage JSONB），**无工具调用明细**——Agent 评估最大缺口
- ToolRegistry 已是 MCP 雏形（AgentTool 含 name/description/args_schema/group，to_llm_schemas 现成）
- **撞号发现**：执行线 08-15 已完成 module-065（ingest-perf-and-e2e，1037/0）——我初始分配 065/066 撞号，修正为 **066（Agent 评估）+ 067（MCP 集成）**，目录/标题/ADR 状态行全部同步

### 落档
- **ADR-0017 Agent 级评估体系**：三层指标（Outcome pass^1/pass^3 对齐 τ-bench 可靠性思想 / Trajectory 工具调用正确率 / System 成本步数）+ tool_call_logs 表（react.py 执行处落库，PW_TOOL_CALL_LOGS 默认 true）+ 任务级评测集 30-50 条（静态、不引 simulated user）+ 确定性判定不用 LLM judge + 验收标准 5 条
- **ADR-0018 MCP 集成**：官方 FastMCP 动态遍历 ToolRegistry 注册（单一事实源不双份维护）+ stdio/Streamable HTTP 双传输（挂载 FastAPI /ai/mcp）+ 只暴露 6 只读检索工具 + PW_MCP_TOKEN fail-closed + MCP 边界（管道不是判断，治理/验证/审计在管道之外）+ 验收标准 5 条
- **module-066/067 task-brief**：自包含可扔执行 AI（WP 划分 + 通过标准 + 纪律项）
- CONTEXT.md 补 ADR-0017/0018 + module-066/067 索引行

### 面试价值
- Agent 评估：补"检索有评测、Agent 行为无评测"的盲区，直接对应业界"Agent 不可靠是最大障碍"痛点
- MCP：岗位直击（AI 编程助手公司核心工作就是接工具进 Agent），"ToolRegistry 本身就是 MCP 雏形"叙事 + "懂 MCP 且超越 MCP"（治理/验证/审计三层）
- **简历全量数字更新（按事实口径）**：用户要求"把事实写上去"——修正 4 类过时数字到 11 个文件：①延迟 3-5s → **E2E 首 token 15.4s（module-065 实测，优化 -39%；完整 18.9s -33%），缓存 3ms 保留（缓存层口径非端到端）**；②测试 897 → **1037**；③模块 62 → **65**；④ADR 15 → **16**；⑤GitHub 链接 personal-resume-website → **agentic-rag-knowledge-base**（git remote 实测）。改动文件：00 信息包（源头 6 处）/ 07 / 09 / 10 / 11 / 12 / 13 / xyc-个人简历（含时间 2026.02-05→07-至今、200万字→124篇156万字、0.99→0.9905）/ 01 / 06（弹药表改为拆分口径：意图15-109ms+检索三通道+HyDE 2.5s+反思1.4-2.6s+rerank 1.9-3.3s+生成4.5-14s外部波动）+ 提前终止句 / 08（对照表+弹药Q10+风险清单）。04 评审报告=历史记录不动、优秀样例/建议=参考不动。08 残留两处 3-5s 为 rerank 对比表/图片说明非端到端，保留。
- **发现（重要）**：xyc-个人简历项目二"AI 模拟面试平台"含**讯飞 AST 实时 ASR 链路**（分段增量去重 + TreeMap+seg_id 有序重建）——用户其实有 ASR 经验！之前给算法岗招呼写"音频没做过"需改口。
- **项目深挖系列启动（面试深化）**：用户要求按流水线顺序逐个环节深挖（用户提问→意图路由→改写→检索/重排→反思→幻觉检测→流式输出→记忆机制，记忆独立于流程特定时刻注入）。新建 `docs/项目深挖/` 目录：README.md（索引 + 统一深挖框架：大白话→代码实测→为什么有/没有会怎样→真实数据→业界对照→面试话术→额外见解）+ 01-文档向量化嵌入.md（已聊完落档）。
- **01 嵌入深挖核心内容**：bge-m3 GGUF Q8（605MB/1024 维/CLS pooling）+ llama-cpp；L2 手动归一化（点积=余弦，动态K前提）；threading.Lock 防 GGML_ASSERT 崩溃（module-027）；批量整批持锁；全局单一事实源（记忆/去重/类型分类/冲突判别全复用 embed_text）；真实数据：向量单通道 0.9714、检索 52ms、批量/多进程证伪（module-065）；业界：Qwen3-Embedding-0.6B 中文更强是升级方向、bge-m3 MIT 可商用 vs OpenAI 26 倍成本差；面试话术 30 秒版已写。
- **深挖 01 文档解析与数据清洗落档（重编号）**：用户指出清洗在向量化之前——01 重编号：01=解析与清洗（新写），02=向量化嵌入（原 01 改名），README 索引更新（11 个话题：01 解析清洗 ✅ / 02 嵌入 ✅ / 03 分块 / 04 意图路由 / 05 改写 / 06 检索RRF / 07 重排 / 08 反思 / 09 幻觉 / 10 流式可观测 / 11 记忆）。内容基于 module-064 changelog 实测：AnyDoc（firecrawl-anydoc==0.1.8，PyPI 包名踩坑）/ 字节魔数识别 / PyMuPDF 回退 / 五步清洗白名单哲学（⟦N⟧ 占位符非控制符坑）/ NFKC 保留行首缩进 / PDF 图片三层默认关 fail-open / 原件留存 original_path / 去重三级（sha256 直丢 + 余弦 0.95 标簇检索抑制 + SimHash 预留）；真实数据 1036/0 + 56/56 前端。
- **01 深挖文档补 2.2 真实解析问题 + 业界印证（2026-08-17/18 跨零点）**：实测 2 份真实 PDF（rag_survey 英文 21 页 0.095s/111966 字符 + 汉斯出版社中文综述 12 页 0.061s/30020 字符，anydoc 原生 API），保存原 PDF + 完整 MD 到 docs/项目深挖/（anydoc原生-rag_survey.md / anydoc原生-中文综述.md，删了截断版）。问题清单：中文（标题层级子比父高/公式打乱/ì引号乱码/词间空格/页眉重复/DOI残留/HTML标签/英文断词）+ 英文（标题截断错位 LARGE→ARGE+#Lable/页眉转表格/作者行乱序/链接残渣/断词/句号粘连）。业界印证：官方承认 PDF 是最弱路径（pdf-inspector 独立引擎）、OCR 留在收费 Parse、公式/复杂版面交重型工具、CSV 无签名；官方基准 anydoc 94 分 vs Pandoc 34/Docling 21。01 文档重编号：2.2 新增（真实问题+业界印证）、原 2.2-2.6 → 2.3-2.7。
