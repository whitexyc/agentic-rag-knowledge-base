# 2026-08-12 工作日志

## NLI 矛盾扫描前置决策（module-052 任务简报）
- 用户复盘 ADR-0010 P1-③ 矛盾扫描方案，提出 5 个疑问，并问"哪个最重要 / 或按 ADR 直接实施"。
- 我的判断：**疑问 1（NLI 中文未验证）最重要，且与疑问 2（HHEM vs NLI 双轨还是替换）是一个决策的两面，必须打包成前置决策**——不解决就实施会方向摇摆/白做。
- 处置：疑问 3（先 HHEM 校准，低成本 threshold_scan，守 kappa>0.7 原则）；疑问 4（重生成 P2 ⊂ 异步化之后，标注依赖）；疑问 5（矛盾扫描限制范围：top-5 未引用块 × 每条 claim 5 对 = 25 对上限）；小疑问（40% 阈值纳入校准；文档两套叙事要统一）。
- 中文备选模型调研（联网）：**mDeBERTa-v3 多语言 NLI（MoritzLaurer）为首选**——训练 26 语言含中文、XNLI 中文 86.4%、~1.1GB、MIT 开箱即用；DeBERTa-large-mnli 纯英文不可直接上（MiniCheck 前车之鉴）；RoBERTa-wwm/ERNIE 3.0 中文更强但要自己微调。
- 产出：`specs/module-052-nli-contradiction-scan/task-brief.md`（自包含任务简报：背景/已知事实/两前置 WP+通过标准/决策树/纪律项/交付物，可直接扔给另一个 AI 执行）。

## task-brief v2 修订（用户补充 5 问题全消化）
- ① 数据源口径：主数据源=SUFFICIENCY_DATASET（module-050 同源代理度量，选型对比必须同口径）；真实数据（golden 112 检索结果）为可选增强（依赖 DB 可用，不可用如实标注不硬造）。
- ② 指标口径：**kappa 为主**（校正随机一致：HHEM 二分类基线 50% vs NLI 三分类基线 33%，比 Accuracy 不公平）；Acc 注口径或 NLI 二值化（entailment vs 其他）对齐。
- ③ 标注一套两用：人工标三分类（entailment/contradiction/neutral），HHEM 支持度从三分类映射（entail→supported/contradict→unsupported/neutral→inferred）——省一半标注成本、标签天然对齐。
- ④ 替换成本（决策树"是"分支必含）：三态映射定义（⚠️neutral→inferred 会让"存疑"并入"推断"，前端色标语义漂移需确认）+ golden_factcheck kappa 复测 + 阈值重新校准（0.7/0.3 是 HHEM 的）。
- ⑤ 环境前置（新增 WP-0）：mDeBERTa 下载照 module-050（curl resolve 直链 + -C - 续传，hf-mirror 308 坑）；transformers 5.x 兼容性离线验证（分数与 HF README 核对，防 embed_tokens 类坑）；实测峰值内存 + 25 对批量 CPU 耗时（防超 15s）；全机模型账 ~5GB 余量确认。
- 小问题：未引用块排序实施时定（建议检索分数 top-5），brief 只约束 25 对上限。
- 纪律 4 新增：口径声明不可省（代理度量 + kappa 主指标），产出可审计数字。

## 检索融合升级讨论 + module-053 任务简报（RRF 三通道消融）
- 用户问"图谱检索和混合检索分开的吗？为什么混合不乘图谱分"→ 代码确认（engine.py:725-755）：两路并行（hybrid_retriever vs graph_store.search_related），按 doc id 合并去重（混合优先、图谱追加），分数互不乘除；合并后统一交 rerank 精排。
- 用户问"能改三通道混合吗？哪个更好？"→ 联网调研：业界多路融合标准 = **RRF**（Σ1/(k+rank)，k=60，不看分数只看排名，零调参最稳健）；三方案（RRF/归一化加权/学习排序）；硬数据 100 万中文文档：+图谱 RRF Recall 90%→94%、MRR +0.06；生产推荐 = RRF 粗排 → CrossEncoder 精排。
- 代码实测：**无 RRF**（全代码搜零结果）；当前 = min-max 归一化 + alpha 加权（retriever.py hybrid，alpha=0.3）+ 图谱按 id 追加；重排是 CrossEncoder 不是 RRF（粗排 vs 细排两层，可共存）。
- 产出：`specs/module-053-rrf-fusion/task-brief.md`——WP-A 基线复测（0.9714 锚点）→ WP-B RRF 三通道原型（retrieval_fusion_mode 开关，默认 hybrid 零回归）→ WP-C 加权三通道对照 → 放行决策；纪律：不破坏现状/不改重排层/保留 abs_cosine 存档（L3 依赖）/评估同口径/图谱 round 0 语义交代。

## 工具治理讨论 + ADR-0012 落档
- 用户问：工具多少个/何时调用/prompt engineering vs function calling → 代码确认 10 工具（tool_registry.py register_builtin_tools）+ react.py:213 全量暴露 + ReAct 循环调用。
- 用户问：能不能走二级（不一次全给）→ 联网调研：工具数量退化硬数据（~50 个选对率 84-95%/200 个 41-83%/740 个近 0 + Lost in the Middle）；业界分层工具选择（Hierarchical Tool Selection）为标准生产模式。
- 三档方案：A 阶段切分（检索组 7/生成组 3，防误调 generate_answer，半天可做）；B 分组路由（ToolRouter 先类别后工具，可复用现有 intent 路由；类别语义最大区分+失败 fallback）；C 动态工具检索（description 向量化进 pgvector 复用 RAG 基建；语义路由 86.4% vs 全量 <50%；MCP-Zero token -98%；AutoTool 成本 -30%）。
- function calling vs prompt：互补非替代——工具调用走原生 FC（结构化 tool_calls），语义判断走 prompt JSON+白名单；工具 description 是关键 prompt（情境导向，准确率 +30%）。
- 落档：`specs/adr/0012-tool-governance.md`（📋 **待实施**，含实施顺序 P1 A→P2 B→P3 C）；08 文档 1.8 节补「工具治理」小节+追问话术；CONTEXT.md 文件索引加 ADR-0012 + 术语表新增「工具治理领域」节 + module-052/053 task-brief 入索引。
- 状态口径更新：用户要求 ADR-0012 及对应文档标注从"📋 暂不实施"改为"📋 **待实施**"（这是用户要真做的优化项，不是储备方案），三处（ADR/08 文档/CONTEXT）已统一。

## 用户已实施 module-052/053，扫描代码确认状态并同步文档
- 用户说"昨天那两个模块我都做了"→ 扫描代码确认：
  - **module-053（RRF 三通道）✅ 已实施放行**：changelog 放行决策表（基线 hybrid 0.9714 / **RRF 0.9905（+0.0191，0 回退）** / 加权两组=基线）；否决加权（图通道离散计数，加权需先校准量纲，RRF 只依赖排名天然适配）；645 passed；默认 hybrid 零回归（AC 契约），上线 `PW_RETRIEVAL_FUSION_MODE=rrf` 一键开启；retriever `_execute_fusion` + `round_num` 参数（融合仅 round 0），engine 三通道图谱可排 top-1；口径声明（评估=retriever 直调，引擎层图追加不在评估路径）。
  - **module-052（NLI 矛盾扫描）📋 复测进行中（module-057 v2）**：`eval/retest_nli.py` 阈值扫描+句级拆解+kappa；`contradiction_dataset.json` 86 条（contradiction 53 / internal 23 含 8 条多句混合）；claim 用真实 LLM 答案+DB 真实检索片段；门槛 kappa≥0.7 放行替换，未达降级双轨——放行判定未定论。
  - 测试数 614→740（新增 rrf_fusion/nli_improve/benchmark_rrf_k 等）；specs 目录有 module-053 全套（acceptance/plan/review/test/changelog），无 module-054-057 目录（代码注释有引用）。
- 文档同步：module-053/052 task-brief 头部加状态（✅/📋）；CONTEXT 索引更新；08 文档 2.7 节已是新版（用户已同步 RRF 消融表+话术，无需改）。

## 检索链深挖 + module-058 任务简报（检索链优化 + 可观测性）
- 用户连续深挖检索链细节（代码逐项确认）：
  - **分块**：三级结构（MarkdownHeader 父块 ≤4000 → 递归子块 300 软上限/重叠 50/切点对齐分隔符，非段落级优先）；子块只对 content 向量化，title 未进向量。
  - **检索**：**默认已切 rrf 三通道**（config.py:79 `retrieval_fusion_mode="rrf"`，之前我讲"默认 hybrid"是过时表述——教训：核对 config 当前值而非 changelog 旧记录）；fetch_k=top_k×2 是"每路各 10、并集最多 20"（非总共 10）；图谱串联=实体命中数（query 实体∪一跳邻居→文档经共同实体间接关联，非文档直连）。
  - **top_k**：默认 5（答案单元数，可调 1-50），重检 20。
  - **反馈落库 ✅ 已做**（/ai/feedback + 表）；**验证异步化 ❌ 没做**（engine.py:352 / main.py:530 仍串行 await）。
- 讨论方案：分块拼标题（embed_text(title\ncontent)，召回 +35-50%）+ **防扎堆**（每父块限命中 vs 聚合式——当前整块返回下限 1 够、窗口返回才需 2）；prompt 顺序（docs 前移→前缀缓存，当前 query 挡 docs 前堵死缓存）；可观测性（trace_id/阶段耗时/token/缓存命中，业界 P50/P95/TTFT）。
- 产出：`specs/module-058-retrieval-chain-opt/task-brief.md`——WP-A 拼标题+防扎堆（基线 0.9905 id=18，golden 对比，有提升才留）→ WP-B prompt 顺序（verify token 对比）→ WP-C 可观测性（trace_id+阶段耗时+token+request_logs 表）→ WP-D 验收（740 测试全绿）。纪律：默认 rrf 不动/评估同口径/防扎堆必配/reindex 先 dry-run/观测不引新依赖。
