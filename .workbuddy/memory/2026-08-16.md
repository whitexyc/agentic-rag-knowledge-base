- **README 加端到端流程章节**：10 步主链路（①用户提问→②输入校验→③意图识别（多轮省略句继承）→④查询优化（分诊式改写+HyDE）→⑤三通道并行检索→⑥RRF 融合排序 0.9905→⑦语义重排→⑧反思检查（重检≤3轮）→⑨流式生成+引用溯源→⑩HHEM 幻觉检测异步化），全部大白话表述（无内部术语），与代码核实一致。链接两张 Visio 图：visio/Agentic-RAG-Flow-v3.vsdx（端到端10步）+ visio/Memory-Mechanism-v1.vsdx（记忆3层+进化）。
- **README 补充 4 块内容**：①关键指标快照表（0.9905/MRR 0.9341/消融/记忆矛盾 P1.0/3-5s）；②知识库管理（多格式上传 .md/.txt/.pdf/.docx/.xlsx/.pptx/.epub/.csv + 文档去重 + 原件留存——核实 main.py:937）；③项目目录结构树；④开发工作流（四角色 AI 协作闭环 + 60+ 模块 + 16 ADR）。修正 ADR 数不一致（14→16，目录实测 specs/adr 16 份）。tagline 补"从0自研/全本地/中文深耕/评估驱动"。
- **docs/ 目录解除 git 跟踪**：.gitignore 第 40 行本来就有 `docs/`，但 6 个文件（docs/adr/adr-001、memory-maintenance-guide、project-experience、rag-flow、rag-optimization-plan、vibe-coding-workflow-reuse-guide）是早期被跟踪的（gitignore 不作用于已跟踪文件）→ `git rm -r --cached docs/` 解除跟踪（本地文件全部保留，简历/面试材料不再进 git/推送远端）。当前 git 状态：6 个 D（待 commit 生效）+ README M + worktree 子模块 m。
