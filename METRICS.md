# 📊 评估指标总览

> Agentic RAG 知识库系统的量化指标。全部来自 `eval_runs` 表版本化评测记录（git_commit + 配置快照落库），数字可复现。
> 更新日期：2026-08-15（module-065：E2E 复测 15.4s/18.9s + 公开基准首跑 + 写入侧批量/多进程证伪；全量 1037 测试全绿）

## 检索质量（golden 评测集 112 题，Hit@5 / MRR / Recall@5）

| 配置 | Hit@5 | MRR | Recall@5 | 来源 |
|------|-------|-----|----------|------|
| **三通道融合（RRF k=60，生产默认）** | **0.9905** | 0.9341 | 0.9762 | eval id=18/28 |
| 三通道融合（加权 0.3/0.6/0.1） | 0.9714 | 0.9270 | 0.9571 | id=19 |
| 两通道基线（加权） | 0.9714 | 0.9270 | 0.9571 | id=13/17 |

### 三通道各自消融（112 题，2026-08-13 补跑）

| 单通道 | Hit@5 | MRR | Recall@5 | 来源 |
|--------|-------|-----|----------|------|
| **向量单通道** | **0.9714** | 0.9286 | 0.9571 | id=37 |
| **图谱单通道** | **0.7333** | 0.6752 | 0.7286 | id=27 |
| **FTS 单通道** | **0.0381** | 0.0333 | 0.0381 | id=36 |

| 融合增益 | 值 |
|----------|-----|
| RRF 三通道 vs 加权 | **+0.0191**（id=18 vs 19） |
| 图谱净增益（三通道 vs 两通道） | **+0.0096**（id=30） |
| RRF k 扫描（k=20-100） | 全平坦 0.9810（k=60 保持，id=30） |

**结论**：向量是主力召回通道（单通道 0.9714）；图谱提供补充（单通道 0.7333，融合净增益 +0.0096——图谱命中的独特实体/关系路径）；FTS 单通道极弱（0.0381，jieba 分词对技术问题的全文匹配差）但融合提供长尾覆盖。**三通道融合（RRF）0.9905 = 每通道的互补叠加**，印证多路召回 + 排名融合的价值。

## 意图识别（100 题）

| 方案 | Accuracy | 每类 P/R/F1 |
|------|----------|-------------|
| LLM 分类 | **1.0000** | 全 1.0（knowledge 50 / casual 30 / realtime 20） |
| 分类器（bge-m3 + 逻辑回归） | **1.0000** | 全 1.0（同集对比，id=24） |

## 记忆类型判断（30 条，module-062 双方案对比）

| 方案 | Accuracy | 说明 |
|------|----------|------|
| 分类器（bge-m3+LR，人造 120 条训练） | **1.0000** | 取为生产（零成本确定性，id=32） |
| LLM（few-shot） | **1.0000** | 同集对比（id=33） |

> 生产注入 `memory_type_mode=clf`（分类器，失败自动回退 LLM type→fact）。

## 充分性判断（100 题）

| Accuracy | 1.0000（sufficient 50 / insufficient 50） |

## 幻觉检测（HHEM 裁判，50 题）

| 三态 kappa | 二值 kappa | Accuracy |
|-----------|-----------|----------|
| 0.3252（<0.7 未达标） | 0.3220 | 0.56 |

> 诚实标注：kappa 未达 0.7 门槛（中文分数压缩 + inferred 标注口径），裁判已接入（HHEM→LLM→空降级链），阈值校准留待标注集扩充。

## NLI 矛盾扫描（mDeBERTa）

| kappa 三分类 | kappa 二值 | Accuracy 二值 |
|-------------|-----------|---------------|
| 0.5167（<0.7 未达标） | 0.6176 | 0.8375 |

### 记忆矛盾检测（70 条，module-070 三方案对比后双判共识启用）

| 方案 | Precision | Recall | F1 | tp/fp/fn | 来源 |
|------|-----------|--------|-----|----------|------|
| **dual（双判共识，生产默认）** | **0.9412** | 0.4000 | 0.5614 | 16/1/24 | id=48 |
| mDeBERTa NLI（单判） | 0.9167 | 0.5500 | 0.6875 | 22/2/18 | id=46 |
| 分类器（bge-m3+LR，单判） | 0.8158 | **0.7750** | 0.7949 | 31/7/9 | id=47 |

> 对照（module-062 30 条旧口径）：nli id=35 Precision 1.0000/Recall 0.5000（**70 条口径证实 1.0 是"窄而准"假象，跌至 0.9167**）、clf id=34 0.9048/0.9500（**人造分布缩水，跌至 0.8158/0.7750**）。
> 决策（module-070 WP-A 数据驱动，用户 2026-08-18 "两个裁判数字都不可信 → 双判共识"）：**`PW_MEMORY_CONFLICT_JUDGE` 默认 `dual`**——双确认才标 superseded（fp=1 三方案最少，Precision 最高，符合"宁漏检也不错标"哲学）；单判矛盾 → conflict_hint 新旧并存；clf 缺失自动回退 nli 单判（fail-open 零回归）。dual Recall 0.4 为 AND 共识数学性质（≤ min 单判），漏判 = 无害降级（拼接共存）；Recall 优先场景可 `PW_MEMORY_CONFLICT_JUDGE=clf` 一键切换。

## Query 改写（104 题，rrf 口径）

| 指标 | 原始 | 改写 | 增量 |
|------|------|------|------|
| Hit@5 | 0.9808 | 0.9904 | **+0.0096** |
| MRR | 0.9287 | 0.8934 | -0.0353 |
| Recall@5 | 0.9712 | 0.9760 | +0.0048 |

**结论**：改写无净收益（MRR 下降），生产保留改写作为护栏（不默认启用），提升空间在精排面。

## 忠实度（faithfulness 双判实验，15 条 dataset 问题，2026-08-14）

| 判分器 | 平均 | 中位 | P10 | P90 |
|--------|------|------|-----|-----|
| LLM-as-Judge（DeepSeek t=0） | 0.607 | 0.60 | 0.20 | 0.80 |
| HHEM（逐句三态聚合 supported/total） | 0.167 | 0.00 | 0.00 | 0.38 |

**LLM vs HHEM 相关系数 0.093（几乎不相关）**——两判分器对同一批答案系统性分歧（LLM 偏宽松、HHEM 偏严且部分拆句失败=0）。诚实结论：**faithfulness 判分器未定**，分歧原因待分析（HHEM 中文偏严为 module-051 已知 + LLM 拆句失败致 HHEM 无法判分），不能草率替换。历史：早期 5 样本 LLM 判分 faithfulness 0.78 / relevancy 0.90（id=10）。

### E2E 实测延迟（"什么是G1 GC"，6 次，优化前后对照）

| 指标 | 优化前（2026-08-14）平均 / 中位 / 最小 / 最大 | 优化后（2026-08-15 module-065 复测）平均 / 中位 / 最小 / 最大 | 变化 |
|------|------|------|------|
| 首 token（TTFT） | 25.3 / 23.0 / 19.8 / 38.3 s | **15.4 / 14.0 / 10.2 / 20.9 s** | **-39%** |
| 完整回答 | 28.1 / 25.8 / 22.6 / 40.9 s | **18.9 / 16.8 / 13.1 / 28.0 s** | **-33%** |

> 优化后 TTFT 15.4s 落在 reranker 优化批预估区间（15-19s）；verify 异步后台不阻塞主链路。
> 实测环境：Windows 本机 CPU，deepseek 外部 API 波动 ±5s（生成段 4.5-14s）。

**首 token 分段构成（优化后 6 次实测，request_logs）**：

| 阶段 | 优化前耗时 | 优化后耗时 | 说明 |
|---|---|---|---|
| 意图路由 | 15-94 ms | 15-109 ms | L4 本地分类器，飞快 |
| 检索三通道 | 向量 52ms + FTS 5ms + 图谱 1.2-2.2s | 向量 52ms + FTS 5ms + 图谱 0.9-1.4s | 图谱最慢 |
| HyDE | 2.5s（仅首次） | 2.5s（仅首次，走缓存） | LLM 生成假设回答 |
| 反思/充分性 | 1.3-2.0s | 1.4-2.6s | |
| **rerank（重排）** | **8.5-13.5s（最大头）** | **1.9-3.3s（平均 ~2.4s）** | **P0-P3 优化（int8 量化+粗筛+200 截断+预热），~4-5x 提速** |
| 生成（LLM） | 4.5-12s | 4.5-14s | 含首 token，外部波动 |

**结论**：TTFT 25.3s → 15.4s（-39%），rerank 从最大头 8.5-13.5s 降为 1.9-3.3s；剩余大头 = LLM 生成（4.5-14s，外部 API 波动）+ 图谱检索（~1s）+ 反思（~1.5s）。

### verify 幻觉检测实测（6 次，后台异步）

置信度：1.0 / 0.875 / 0.625 / 0.625 / 0.5 / 0.0（一次 LLM 拆句 15s 超时兜底）；verify 耗时 12-16s，全部后台不阻塞回答。

## 可观测性（request_logs 实时可查，非快照）

| 类别 | 内容 |
|------|------|
| 阶段耗时 | intent / 检索（FTS·向量·图谱三通道各自）/ rerank / reflection / generate / verify |
| token 用量 | 按供应商（deepseek / qwen / zhipu） |
| 延迟分布 | P50 / P95（聚合查询） |
| 单问题成本 | 可算（usage 按供应商） |

## 分类器训练指标

| 分类器 | Accuracy / 指标 | 说明 |
|--------|-----------------|------|
| intent_clf | Accuracy **1.0000** | module-056 人造 337 条重训 |
| sufficiency_clf | Acc 0.80（insufficient Recall **1.00**） | module-045 训练 |
| memory_type_clf | Accuracy **1.0000** | module-062 人造 120 条训练（生产注入） |
| memory_conflict_clf | Precision **0.9048** / Recall 0.95 | module-062 人造 142 条训练（备选，可一键切换） |

## 用户反馈

| feedback 表 | 👍👎 落库（rating ±1 + comment + identity），飞轮再训练数据源 |

## 2026-08-14 性能优化与真实文档实测批

### reranker 优化 P0-P3 实测效果（真实模型 + 知识库真实候选，5 题 × 3 次中位）

| 指标 | 优化前（fp32+250+15候选全精排） | 优化后（int8+200+粗筛6） | 倍率 |
|------|-------------------------------|------------------------|------|
| 单次 rerank | 10.4-11.2 s | **2.6-2.8 s** | **3.8-4.1x** |
| 单对打分 | 0.70 s/对 | 0.44 s/对（量化+截短 1.6x） | |
| 模型加载 | fp32 4.0s | int8 含量化 12.6-13.8s（P3 预热消除） | |
| 截断 250→200 | 排序零影响（20 文档扫描：250→170 全一致，160 漂移，中相关 170 以下崩塌） | | |

**排序安全性（量化隔离实验，固定 250，fp32 vs int8）**：强相关 doc-a 全主题稳居第一（漂移 ≤0.025，0.965-0.999→0.943-0.997）；top-2 "不一致"仅发生在 0.01-0.16 弱相关内部（doc-b/doc-c 互换，真实链路不进 top-5）；干扰文档恒 0.0 不误抬。**P0 量化安全**。
**E2E 换算**：TTFT 25.3s 中 rerank 8.5-13.5s → 预计 2.6-3.5s，TTFT 预计 15-19s（待重启服务实测）。

### bge-m3 嵌入吞吐（Q8 GGUF 本地，llama-cpp-python）

| 指标 | 耗时 | 说明 |
|------|------|------|
| 冷加载 | 1.7 s | 605MB Q8 |
| 单条嵌入 | 230 ms | |
| 批量 10/50/100 条 | 3.1 / 23.5 / 26.9 s（~270-470ms/条） | **无批量加速**（逐条调用） |
| 查询侧（request_logs） | 52 ms/次 | 短文本 |

### 真实文档插入实测（module-064 补强，2026-08-14）

源：arxiv RAG Survey 论文（PDF 21 页）/ Redis 官方 llms.txt / datahub 城市数据集（CSV）。

| 文档 | 解析 | 清洗/归一化 | 入库总耗时 | chunks | 质量（结构保留） | 二次插入去重 |
|------|------|-----------|-----------|--------|-----------------|-------------|
| rag_survey.pdf（1.6MB） | 992ms（anydoc, 21页） | 68/43ms | 136.4s | 568 | 111,612 字符 / 118 表格行 / 26 URL | exact 命中（489ms）✓ |
| redis_docs.txt（45.8KB） | 6ms | 25/21ms | 76.3s | 246 | 46,579 字符 / 199 URL | exact（27ms）✓ |
| world_cities.csv（1.3MB） | 124ms | 493/545ms | **3002s ⚠️** | 7231 | 1.66M 字符 / 34,067 表格行 | exact（1.2s）✓ |

- **L2 语义去重实测**：截断 80%+改序变体 → dup_kind=semantic、标簇、canonical=False（43.6s 含文档级嵌入）✓
- **⚠️ CSV 瓶颈**：1.3MB 数据集 7231 块 × 逐块嵌入 ~0.4s ≈ 50 分钟——**写入侧无批量嵌入**（与嵌入基准"无批量加速"印证），大数据集入库性能优化点（批量嵌入可提数倍，待办）
  - **补充（2026-08-15 module-065 WP1 证伪）**：上行"批量嵌入可提数倍"为 064 记录时的乐观估计，原文字保留。实测：llama.cpp `create_embedding(List[str])` 内部逐条解码无批量加速（10 条 0.8x / 50 条 1.3x / 200 条 0.9x），多进程并行 Windows spawn 负优化（0.3-0.4x）——**现有引擎路径已证伪，不做投机改动**；真 batch 需换引擎（sentence-transformers/ONNX），GPU 上才显著、CPU 未验证，暂缓。若后续优化入库耗时，更现实方向是断点续传而非批量加速（复跑验证：`python -m eval.benchmarks.benchmark_embed_write`）
- 测试文档本地留存（uploads/realdoc-test-2026-08-14/ + 知识库 source=realdoc_test:2026-08-14）

### 真实文档检索验证（bge-m3 向量通道，6 题）

| 问题 | 结果 | sim |
|------|------|-----|
| What are the three paradigms of RAG? | ✅ 论文 chunks 3/3 | 0.66-0.71 |
| What is Advanced RAG...? | ✅ 论文 2/3 + 存量 AdvancedRAG 中文文档 | 0.70-0.74 |
| 检索增强生成如何解决幻觉（中文） | 存量中文 RAG 文档 3/3（跨语言弱，论文未命中） | 0.64-0.67 |
| Redis persistence RDB and AOF | 存量 Redis 持久化 0.75 第一 + 测试 redis_docs 2/3 | 0.73-0.75 |
| 欧洲城市 Munich 在哪个国家 | ✅ 测试 CSV 3/3 | 0.54 |

**结论**：三份真实文档全部可检索；同语种强、跨语种弱（bge-m3 双语边界，如实标注）。

> **sim 列说明**：sim = similarity = 查询与文档块的**余弦相似度**。查询和文档各被 bge-m3 嵌入成 1024 维向量，sim = cos(两向量夹角) ∈ [-1,1]（pgvector `1 - (embedding <=> 查询向量)` 计算，即余弦距离取反）。0.7+ 强相关（论文题命中论文段落）、0.5-0.7 中等、<0.4 弱/噪声。向量检索按 sim 降序取 top-K。

## 公开基准检索评测（2026-08-15，module-065 WP3，测通用泛化）

> 自建 golden 集（112 题）是"自己考自己"——文档来自本项目知识库，命中率高
> 存在乐观偏差。用公开标准数据集（BEIR / C-MTEB）测 bge-m3 + 余弦暴力检索的
> 通用泛化。数据源探测（2026-08-15）：huggingface.co 502 不可达，hf-mirror.com
> 可达（下载走 mirror 直链 resolve URL）；脚本 `eval/benchmarks/benchmark_public_retrieval.py`
> （数据落 eval/datasets/public/ 缓存 gitignored，可复跑）。

| 数据集 | 语种 | 口径 | 有效查询（跳过） | Hit@5 | MRR | nDCG@10 | 来源 |
|--------|------|------|------------------|-------|-----|---------|------|
| C-MTEB EcomRetrieval（电商检索） | 中文 | **corpus 抽样 3,000/100,902**（固定种子，代理口径） | 26（跳过 974） | 0.9231 | 0.9231 | 0.9231 | eval id=44 |
| BEIR nfcorpus（医学检索） | 英文 | **corpus 抽样 1,200/3,633**（固定种子，代理口径） | 292（跳过 31） | 0.5685 | 0.4630 | 0.4853 | eval id=45 |

> **口径演进（2026-08-15 三轮修正，旧记录已落库标注）**：
> id=40/41 = 点积口径（嵌入未 L2 归一化，Review MAJOR-1 修复）；id=42/43 =
> 余弦但全查询计入（相关文档不在抽样内的查询按 0 分稀释，Tester 修复轮）；
> **id=44/45 = 余弦 + 真跳过语义**（有效查询 = 相关文档 ∩ 抽样 corpus 非空者，
> 空者跳过不计）。**注意：各 id 口径不同不可直接比**；本表以 id=44/45 为准。

**解读（诚实）**：
- **ecom-zh**：1,000 查询中仅 26 条（2.6%）的相关文档落在 3% 抽样内（qrels
  每查询仅 1 条相关，非假设的 5 条）——**有效查询仅 26 条，统计证据薄**；
  该 26 条上 0.9231 说明"答案在 corpus 内时 bge-m3 找回能力高"，但**不能
  与官方 leaderboard 直接比**（抽样 + 小基数双代理）；
- **nfcorpus**：292/323 有效（跳过 31），nDCG@10 0.4853 **落在官方全量口径
  bge-m3 参考值（~0.45-0.5）区间内**——抽样代理下与官方水平一致，是三个
  数字中最有信息量的；
- 两公开集与自建集（Hit@5 0.9905）差异（ecom 0.92 基数薄 / nfcorpus 0.57）
  印证自建集"自己考自己"的乐观偏差存在（语料/口径不同：自建集 RRF 三通道
  +重排且文档即源文档，公开集单通道余弦暴力检索，仅方向性对比）；精确泛化
  水平需全量 corpus（GPU/集群环境 `--corpus-sample 0` 可跑，脚本入口已就绪）。

> 自建集 vs 公开集对比仅为方向性结论：自建集 Hit@5 0.9905（RRF 三通道 + 重排，
> 文档即源文档）；公开集为单通道余弦暴力检索 + 抽样代理口径，两数不可直接比。
> 实测解读：nfcorpus 有效 292 条 nDCG@10 0.4853（与官方 bge-m3 全量参考同量级，
> 英文泛化能力相当）；ecom-zh 有效 26 条 0.9231 高但样本极小（97.4% 查询相关
> 文档落抽样外被跳过），不代表全量水平——泛化能力需全量语料（`--corpus-sample 0`）
> 在 GPU 环境补跑。

## 待补跑/待优化（诚实标注）

1. ~~记忆提取真实 baseline~~（2026-08-16 完成，标注集已扩 28→60 条）：**P 0.66-0.69 / R 0.68-0.70 / F1 0.67-0.69**（两次 LLM 跑分波动 ±0.02 非确定性；`python -m eval.golden.golden_memory`）。关键发现：初版匹配口径（_fact_match 双向包含）把 0.69 误杀成 0.2353——修尺子（+字符重合率 ≥0.6 兜底）后翻近 3 倍，**评测口径修正收益 >> 提取器改进**；新增 A 类多轮样本（省略指代/跨轮补充/修正语义）全部通过——提取器真实会用 history；tp=0 仅剩阈值边界案例（LLM 简化提取重合率 0.53<0.6）。诚实边界：落库失败（DB 网络不可达，分数有效未入库）；0.6 阈值对"简化/缩写提取"仍有误杀边角；LLM 判分（语义级匹配）为未来选项
2. 幻觉检测 kappa 校准（<0.7 未达标）
3. ~~矛盾检测 Recall 提升~~（2026-08-18 module-070 完成，目标按用户决策重定义为双判共识）：评测集 30→70 条（真实分布打底 + 4 条边界陷阱），**nli 1.0000 Precision 假象证实（70 条 0.9167）/ clf 人造分布缩水（0.8158/0.7750）→ 双判共识默认 `dual`**（Precision 0.9412 fp=1 最高，AND 共识 Recall 0.4 为数学性质 = 无害漏判拼接共存；Recall 优先可 `PW_MEMORY_CONFLICT_JUDGE=clf` 切换），eval_runs id=46/47/48（scores 含 judge 字段区分）
4. 类型判断 1.0 含同分布偏乐观成分（真实泛化以飞轮重训为准）
5. **公开基准检索评测**（module-065 WP3 三轮修正后定稿）：ecom-zh 中文抽样 3,000/1,000 查询（有效 26）+ nfcorpus 英文抽样 1,200/323 查询（有效 292），见"公开基准检索评测"段（最终口径 = 余弦 + 真跳过，id=44/45；40-43 为中间口径已标注）——测通用泛化，补自建集"自己考自己"的乐观偏差（Hit@5 0.9905 偏宽口径，MRR 0.9341 已有）；**注意**：C-MTEB 各子集 corpus 均 ~10 万篇，本机 CPU 单篇 1.7-2.2s 全量嵌入 ~2h 不现实，抽样为环境约束（有 GPU 可 --corpus-sample 0 跑全量）
6. ~~写入侧批量嵌入~~（2026-08-15 探路实测**证伪**，module-065 WP1）：`create_embedding(List[str])` 无批量加速（10 条 1.0x / 50 条 0.9x / 200 条 1.0x vs 循环）+ 多进程并行嵌入负优化（2 进程 0.4x / 4 进程 0.3x vs 串行，Windows spawn 重 import + 内存带宽竞争）——写入吞吐 ~110-210ms/块（bge-m3 Q8 单核推理，随文本长度波动：探路短文本 110-160 / 复测 141 字符文本 173-211）为**固有成本**，无廉价优化路径，生产代码不改（`eval/benchmarks/benchmark_embed_write.py` 可复跑验证，module-065）
7. ~~优化后 E2E 复测~~（2026-08-15 module-065 完成）：TTFT 25.3 → **15.4s**（-39%），完整 28.1 → **18.9s**（-33%），rerank 8.5-13.5 → **1.9-3.3s**（平均 ~2.4s），实测落在预估区间 15-19s
8. ~~意图分类器多轮重训~~（2026-08-16 架构评估**降级不做**）：L4 多轮拼接（2048 维 = 当前句 1024 + 上一轮 1024）想"让省略句/指代句走便宜分类器"，但能力已被 LLM 路径覆盖（module-063 多轮省略句实测 12/12 全对，分类器传 prev 维度不匹配自动回退 LLM——**现状即合理架构，零改动**）；且线性模型拼向量学不到指代关系（天花板已看清）、配对样本稀缺（golden_multi_turn 仅 12 对）、省略句真实占比未数据化——收益依赖未知占比、成本要造数据+改训练契约+重训，性价比不足。数据驱动决策：飞轮 👍👎 数据积累后统计省略句占比，占比高再评估（替代路径：WP-B 规则层短句继承已覆盖大部分，去语气词后 <6 字符直接走 LLM 兜底）
