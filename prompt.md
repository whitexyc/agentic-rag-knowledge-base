# 个人简历展示与 Agentic RAG 知识库系统（双语言架构）

请根据以下需求，帮我搭建一个融合简历展示与AI知识库问答的个人网站系统。

## 一、 系统整体架构目标
采用“双语言微服务架构”：
1. **前端**：Vue3 / React 搭建极简双栏或卡片式简历展示页面。
2. **后端业务层（Java）**：Spring Boot。负责用户管理、PDF/文档文件上传与元数据管理、查询统计、对接前端的RESTful API网关。
3. **AI推理层（Python）**：FastAPI + LangChain / LlamaIndex。负责对接大模型、向量数据库、Agent路由调度，并向Java端提供标准的HTTP接口（API调用）。

## 二、 核心业务功能开发要求
### （一）简历展示模块
- 静态展示个人简历（数据结构/路由直接返回JSON，前端渲染）。
- 简历内容包含：1次半程马拉松（1小时37分）、校卫队安保/迎新/消防巡查经历、春运志愿者、多次奖学金、英语四级544分、全国大学生物联网/嵌入式竞赛国奖（曾任7人组长）。

### （二）Agentic RAG 知识库模块（**该模块需要“多做”高级功能**）
实现一个企业级知识库智能问答助手，包含以下高阶特征：
1. **多源混合检索**：实现 BM25（关键词倒排）与 向量检索 的加权混合召回。
2. **Rerank 重排**：引入 Cohere 或 BGE 重排模型，对 Top 20 召回结果重排，输出最精准的 Top 5。
3. **意图识别路由（Router Agent）**：Agent 先判断用户问题类型（如：查知识库、查实时数据、闲聊），分别路由到不同管道，避免无效检索。
4. **自我反思与纠错（Self-Reflection）**：Agent 在给出答案前，自动检查检索内容是否充分，如果不充分，自动改写 Query 进行二次检索，保障回答可靠度。
5. **精准引用溯源**：每次回答必须带上检索到的原文片段、文档ID和页码，并可推给前端做“点击查看原文出处”的溯源展示。
6. **多模态/复杂文档解析**：解析 PDF/图片时，利用 Unstructured 或 PaddleOCR 进行版面分析，将表格转为 Markdown 存入向量库。

## 三、 工程架构与防坑要求
- **前后端及AI层必须彻底解耦**：Java 只负责业务和请求转发，Python 独立迭代 AI 逻辑；Java 端需实现熔断降级机制（Python 服务超时后，Java 端返回“系统繁忙”或走纯 LLM 兜底）。
- **需包含代码脚手架**：包括 Spring Boot 项目结构、FastAPI 项目结构、LangChain 的 RAG 核心链路代码骨架、Docker Compose 编排（包含 MySQL 和向量数据库容器）。

