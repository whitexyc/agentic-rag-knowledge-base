# 下一阶段任务规划（Roadmap）

> Planner | 2026-08-11 | 状态：📋 规划完成，**待用户确认后执行**（用户指示：完成后等通知再执行）

---

## 背景（截至 module-051 的状态）

- 测试：**614 passed / 0 failed**（module-049 分诊改写 → 050 HHEM 对比+目录细分 → 051 HHEM 裁判接入）
- 核心能力：Agentic RAG（分诊式改写 / 三通道检索 Hit@5 0.9714 / 记忆进化 / HHEM 专职幻觉裁判）
- 简历 07/06 停在 module-049（567/49 模块），**050/051 成果未同步**
- 已知短板：HHEM kappa 0.3252 < 0.7 未达门槛；验证交叉对延迟 ~9s；图谱消融/改写真实增益等"待环境"数字

---

## 任务清单（按性价比排序）

### T1 · 简历同步（低风险，优先，可立即做）
- **内容**：07 简历更新为 **614 测试 / 51 模块**，核心工作补"HHEM 专职幻觉裁判（LLM 拆句 + 专职模型判分，evidence 取 max 分文档）"；06 弹药补 HHEM 钩子（含 kappa 0.3252 未达门槛的诚实口径 + 与 MiniCheck 对比数据 0.77 vs 0.51）
- **验收**：07/06 同步主 checkout hash 一致；数据口径与 changelog 一致
- **依赖**：无

### T2 · kappa 校准（半天，质检员"合格"化）
- **内容**：module-051 结论——kappa 0.3252 根因是 0.7 上界偏严（中文分数压缩，9/20 supported 落 0.33-0.67）+ HHEM 对"部分覆盖"判一致偏乐观。校准 = ① 高阈值下调至 0.5 附近（PW_VERIFY_HHEM_THRESHOLD_* 配置改）② inferred 标注口径重设计（"部分覆盖"样例重标注或拆分）③ 扩充标注集（50→100+ 条）后重跑 `python -m eval.golden_factcheck`
- **验收**：kappa 提升可量化；阈值改动有数据证据；如实报告新数字（不达标继续标）
- **依赖**：人工标注扩充（时间成本）

### T3 · 矛盾扫描（P1-③，半天-1 天，补最大漏洞）
- **内容**：ADR-0010 P1-③——把**未引用的文档片段**也做 HHEM 判定（"这段和 claim 矛盾吗"），防"挑樱桃"（AI 只引支持自己的 chunk 忽略矛盾）。构造 5 条"文档有矛盾但答案挑樱桃"用例验证
- **验收**：构造用例全部检出；无矛盾时不误报；全量 pytest 保持全绿
- **依赖**：HHEM 裁判（module-051 已就绪）

### T4 · 验证异步化（P2，1-2 天，体验提升）
- **内容**：ADR-0010 串行阻塞解法①——非流式路径"答案先返回 + verified 后台算 + 前端轮询/SSE 推送"，用户感知延迟 = 生成时间（当前 5 claims×5 docs ≈ 9s 干等）；或先做低成本版：验证期间与记忆写入/会话持久化 `asyncio.gather` 并行 + 抽样验证（10%）
- **验收**：非流式响应延迟显著下降；verified 后置到达前端正常渲染；降级（推送失败）不影响聊天
- **依赖**：接口协议改动（前后端协调），改动面较大——按性价比可拆为两个小步

### T5 · 环境补跑（待 DB+LLM 环境就绪）
- **内容**：
  1. **本地库 schema 迁移**（module-046 列 ALTER TABLE）→ 解锁图谱表/记忆列
  2. **图谱消融**：`python -m eval.golden_retrieval --ablate --top-k 5` → Hit@5 差值（+X.X）→ 简历补"图谱提升了 X%"（有数据才写）
  3. **改写真实增益**：`python -m eval.golden_query_rewrite`（真实模式）→ Recall@K 前后对比
  4. **记忆提取 baseline**：`python -m eval.golden_memory`（真实模式）
- **验收**：每个数字如实记录（含 skipped）；环境不可用继续标"待环境"不伪造
- **依赖**：DB schema 迁移 + LLM 环境

### T6 · 飞轮数据消费（待数据积累，长期）
- **内容**：feedback 表攒够数据 → message_id 关联 query/answer 生成标注集 → 增量重训 intent/sufficiency 分类器（module-045 脚本已就绪）+ sufficiency_clf 推理路径接入生产
- **验收**：重训后 Accuracy/Recall 对比；推理路径接入后全绿
- **依赖**：数据量（时间）

---

## 执行建议

- **立即批次**（无需环境）：T1 → T2 → T3 → T4
- **环境批次**：T5（环境就绪信号触发）
- **数据批次**：T6（数据量触发）
- 每项按 vibe-coding-workflow 走完整闭环（Planner specs → Workflow 一次派发 → 单次提交）
- 简历/弹药更新（T1）可随任意模块完成后合并执行，或单独快速批次

## 待用户确认

- [ ] T1 简历同步现在做？
- [ ] T2-T4 执行顺序与拆批方式？
- [ ] 有无新增优先级？
