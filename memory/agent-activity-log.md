# Agent 活动日志索引

> 日志文件按 Agent 角色 + 日期存储于 `memory/logs/<role>/YYYY-MM-DD.md`
> 维护规则：Agent 完成有意义动作后，在对应角色当日日志追加记录。

## 日志文件索引

| 日期 | 角色 | 文件路径 | 主要活动摘要 |
|------|------|----------|-------------|
| 2026-07-29 | Planner | [planner/2026-07-29.md](logs/planner/2026-07-29.md) | 项目初始化、技术栈配置、模块规划 |

## 阶段汇总（2026-07-29 ~ 2026-08-02，模块 001-030）

> 各模块详细产出见 `specs/module-0XX-*/`（plan/acceptance/changelog/review-report/test-report）。

### 基础期（module-001 ~ 017，07-29 ~ 07-31）
- 项目脚手架、简历数据/API/前端、AI 层基础、RAG 核心、Chat UI、知识库面板、会话持久化、RAG UI、RAGAS、HyDE、Redis 缓存、Graph RAG、父子分块。

### 优化期（module-018 ~ 030，08-01 ~ 08-02）
| 日期 | 模块 | 角色 | 摘要 |
|------|------|------|------|
| 08-01 | module-018 | 全角色 | Rerank 修复（Qwen3-Reranker）→ 评审/测试通过 |
| 08-01 | module-019 | 全角色 | 评估闭环（golden 集 + Hit@k/MRR + 版本化） |
| 08-01 | module-020 | 全角色 | 中文 FTS（jieba）+ 本地嵌入（bge-m3 GGUF） |
| 08-01 | module-021 | 全角色 | 图分数归一化 |
| 08-01 | module-022 | 全角色 | 检索缓存修复（key 参数化 + 失效） |
| 08-01 | module-023 | 全角色 | 长期记忆（测试发现 date 类型 bug → 修复） |
| 08-01 | module-024 | 全角色 | 检索延迟优化（预算 + HyDE 缓存） |
| 08-01 | module-025 | 全角色 | 流式记忆接入 |
| 08-01 | module-026 | 全角色 | 检索并发修复 + Reflector 低温度 |
| 08-02 | module-027 | 全角色 | 嵌入并发修复 + backlog 收敛 |
| 08-02 | module-028 | 全角色 | Agent 工具化（ToolRegistry + ReAct，8 轮充分迭代） |
| 08-02 | module-029 | 全角色 | 前端增强（工具轨迹 + 降级链动态调序） |
| 08-02 | module-030 | 全角色 | 重排优化（bge）+ LangGraph 实验端点 |

### 2026-08-04（module-030 修复 + module-031 知识库重建）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-030 修复 | Developer | 实机诊断：库里 45 篇全为旧版"整篇 1 父+1 子"大块（平均 2.1 万字符）→ rerank 200-641s + 同步阻塞事件循环冻结服务；reranker 截断 500 字符 + to_thread 修复；降级链恢复 deepseek 优先（提交 78fc9a0） |
| module-031 | Planner | 分块规则讨论 → 用户拍板 Option C（## + ### + 父块 4000 上限 + 子块 300）；plan.md / acceptance-criteria.md |
| module-031 | Developer | chunker Option C 实现 + tests 8/8；reindex_knowledge_base.py（幂等/--dry-run/--no-graph/--skip-import）；全量重建 58 文件 → 1136 父 / 6370 子，父块 >4000 = 0 |
| module-031 | Reviewer | 审查发现 cleanup_orphans `r.t` bug（SQLAlchemy 2.0.19 Row 具名属性陷阱）→ 修复 + --skip-import 恢复模式；review-report.md |
| module-031 | Tester | 单测 8/8 + graph_store 12/12；全量回归 **195 passed / 0 failed**（含 async 债务修复）；库内统计达标；图谱 1423 实体；E2E G1/Redis 检索质量恢复；test-report.md |

### 2026-08-05（module-032 JWT 登录体系）| 模块 | 角色 | 摘要 |
|------|------|------|
| module-032 | Planner | 记忆架构讨论（三层：长期/短期/会话，参考 llm-push/19-Agent记忆管理.md）→ 用户定 3 模块方案（032 JWT登录 / 033 长期记忆 / 034 短期+会话）；plan.md + acceptance-criteria.md（跨栈契约：JWT HS256 + 共享 secret + 匿名降级 IP）；三记忆文件更新 |
| module-032 | Developer(backend) | Workflow 并行派发：users 表 V032 + UserEntity/Repository + JwtUtil(HS256) + AuthService(BCrypt) + AuthController(register/login/me) + AuthInterceptor + application.yml/pom；mvn 37/0（新 17）；changelog-backend.md |
| module-032 | Developer(frontend) | LoginPage + AuthContext(localStorage) + api/client.ts(统一附 Bearer) + AppLayout 登录入口 + 3 service 复用；vitest 17 新过 + build ✓；changelog-frontend.md |
| module-032 | Developer(python) | src/identity.py(parse_jwt/resolve_identity) + 中间件注入 user_id + memory source→memory:\<identity\>: + engine 身份化；pytest 215/0（新 20）；changelog-python.md |
| module-032 | Reviewer | 三栈全量审查：跨栈契约专项核对（HS256 payload / APP_JWT_SECRET=PW_JWT_SECRET 按值对齐 / CommonResult.msg vs message / memory:\<identity\>: / 匿名降级 IP）；安全审查（BCrypt、secret 不进仓库、LIKE 注入双保险、日志无敏感）；独立复现 mvn 37/0 ✓ + vitest 17 ✓ + pytest identity/memory 49 ✓ + frontend build ✓；结论：⚠️ 有条件通过（契约/安全无阻塞；建议项见 review-report.md）|
| module-032 | Tester | 三栈回归全过（mvn 37/0、pytest 215/0、vitest 31+3 基线）+ 真实 E2E：**发现核心跨栈缺陷**——Java `signWith(key)` 对 64 字节 secret 自动签 HS512，Python 仅验 HS256 → token 被拒、记忆不按 user_id 隔离（B 召回 A 记忆）。结论 ❌ 不通过，验收 32/40 |
| module-032 | Developer(team-lead 修复) | 修复 JwtUtil 显式 `signWith(key, Jwts.SIG.HS256)` + 64 字节密钥回归测试（shouldSignHS256WithLongSecret）；mvn 38/0。重跑真实 E2E：token alg=HS256、A 记忆 memory:3:、B 召回 0（隔离成功）、匿名降级零回归 → **复验通过 40/40**。E2E 测试数据已清理 |

### 2026-08-05（module-033 长期记忆自动写入）| 模块 | 角色 | 摘要 |
|------|------|------|
| module-033 | Planner | 长期记忆自动写入方案（对话结束异步 LLM 提取 + 语义去重>0.95 + 动态K召回 + 格式化注入 + fire-and-forget）；plan.md + acceptance-criteria.md（38 项）；三记忆文件更新 |
| module-033 | Developer | [CODE] 实现：memory_extractor.py（extract_facts LLM 提取 + importance>=0.6 过滤 + 失败/超时降级[]）+ memory.py（save dedup=True 语义去重 cosine>0.95→更新旧父块追加合并、条数不涨 / recall 动态K 0.85/0.75/宁缺毋滥 / format_memory_line '[长期记忆 - 日期]：内容' / _expand_to_parents 增 created_at）+ engine.py（_persist_memory fire-and-forget + _schedule_persist + chat knowledge 路径异步触发 + _recall_memory 新格式）+ main.py（chat_stream accumulate token 后 schedule_stream_persist，casual/realtime 跳过）+ config.py（阈值可配置）；changelog.md；技能：test-driven-development / systematic-debugging（test_empty_answer_skips_extract 失败归因 → 补 _persist_memory 空答案守卫）；pytest 254/0（215 基线 + 新 39） |
| module-033 | Reviewer | [REVIEW] 全量审查：契约核对（source 格式 / save-recall 签名兼容 / 匿名降级 / fire-and-forget 不阻塞 / 提取只对 knowledge 路径）；安全（去重只查本身份 LIKE 转义+校验双保险 / 提取失败降级不影响对话 / 嵌入 L2 归一化验证点积=cosine）；独立复现 test_memory_extractor 39/0 + 全量 pytest 254/0 + identity 20/0 + py_compile OK；验收 38 项中 32 项经代码/单测核验 ✅、2 项 E2E 留 Tester、4 项文档 ✅。结论：⚠️ 有条件通过（无阻塞；6 项非阻塞建议：①动态K阈值作用于 min-max 相对分而非绝对相似度（retriever._normalize 跨查询不可比），真实语义与 19-Agent记忆管理 参考设计有偏差 ②去重合并后不重建子块向量，新事实检索可能 miss ③chat 路径 _recall_memory top_k=3 使 K=5 档不可达 ④新生产代码约 438 行略超 plan 400 预算 ⑤agent 端点未接自动写入 ⑥worktree 既有 application.yml 硬编码 JWT 默认值提示）；review-report.md 已产出；技能：understand-diff / verification-before-completion / requesting-code-review |
| module-033 | team-lead | [FIX] 安全阻塞：application.yml `jwt.secret` 硬编码默认密钥（${APP_JWT_SECRET:64位hex}）→ 移除默认值恢复 ${APP_JWT_SECRET} fail-fast（Reviewer 阻塞 #1：env 缺失时回退公开密钥可伪造 token）。注：Reviewer 审查时打印 .env PW_JWT_SECRET 到 transcript（凭证泄露警告），建议用户轮换该值 |

### 2026-08-06（module-033 收尾 + module-034 规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-033 | Tester | [TEST] **验收通过（40/40，4 项附条件非阻塞）**。① 全量回归 **254 passed / 0 failed**（215 基线 + 39 新增，3 个既有 Redis setex 弃用 warning 与模块无关）；② 新增单测 **39/39**（test_memory_extractor.py：提取/importance 过滤/失败降级/JSON 结构/去重（>0.95 更新、不同新增、失败降级）/动态K 三档+空候选/格式化注入/触发链路 chat+chat_stream）；③ 身份回归 20/20、memory+identity+stream 54/54、py_compile 5 文件 OK；④ **半真实 E2E**（真实 PG + 真实本地 bge-m3 + 真实 DeepSeek HTTP 200）：`_persist_memory` 自动写入链路真实跑通（extract_facts 返回事实 → save 落库 → recall 召回 → 格式化注入）、匿名 client_ip 两身份（9.9.9.9/8.8.8.8）隔离 PASS（含自动写入路径）、去重机制（identical content cosine 1.0→updated）✅；⑤ **Reviewer 关注项复核**：#1 动态K 真实 min-max 相对分 avg 恒<0.75 → 恒 K=1（K=5/3 档实际不可达，确认语义偏差）、#2 去重合并无脏数据/崩溃（子块向量不重建是已知取舍）、#3 chat 路径 top_k=3 截断 K=5 档（无逻辑错误）；⑥ **去重阈值校准观察**：真实 bge-m3 同义改写 cosine≈0.88<0.95 不触发去重（完全一致 1.0 触发），LLM 提取措辞不稳定 → 「二次同义对话→不膨胀」在措辞变化时不成立；机制正确属阈值校准问题，建议 module-034 下调阈值（约 0.85）或改绝对相似度口径；⑦ 时区观察：created_at 为 PG UTC（08-05）vs 标题本地日期（08-06），同天差 8 小时（环境既有非模块缺陷）；⑧ 验收项核对发现原统计 38 实际复选框 40（17/5/6/8/4），按实际 40 签署并修正统计表。test-report.md 已产出；技能：verification-before-completion / systematic-debugging（观察归因） / test-driven-development |
| module-034 | Planner | 短期记忆+会话记忆方案（source 三层分层 long/short/session + 短期 TTL 7天 + 会话摘要异步生成 + 会话持久化复用 documents + IP_SESSION_MESSAGES 升级）；plan.md + acceptance-criteria.md（34 项）；三记忆文件更新 |
| module-034 | Developer | [CODE] 实现：memory.py（三层 source 分层 `_memory_source`/`_layer_pattern`，长期检索由 `memory:<id>:%` 改**精确匹配**防跨层误命中（零回归）+ `save_short`/`recall_short`（动态K + TTL 7天过滤）+ `format_memory_line` label 参数）+ session_memory.py（`save_session_messages` content_hash 幂等 + 每 identity 上限滚动 / `get_session_messages` 隔离恢复）+ engine.py（`_recall_memory` 长/短合并注入"最近上下文"段 / `_persist_memory` 同批 facts 沉淀长+短 / `_resolve_session_history` 优先持久化会话 / `_schedule_session_persist`）+ main.py（chat/chat_stream 会话持久化接入 + Step 5 会话恢复 + IP_SESSION_MESSAGES 降级兜底缓存）+ config.py（memory_short_ttl_days / session max+history limit）；changelog.md；技能：test-driven-development / systematic-debugging（fake session 按语句类型路由修正）；pytest **278/0**（254 基线 + 新 24） |
| module-034 | Reviewer | [REVIEW] **结论：❌ 不通过（1 项阻塞）**。全量审查：① 契约核对全过（三层 source 精确匹配互不混淆 / save-recall 签名兼容 / chat-stream 端点签名不变 / 匿名降级不变 / fire-and-forget 不阻塞 / 会话复用 documents 不泄漏进检索）；② 安全全过（LIKE 注入双保险保留、TTL fail-open 合理、日志无敏感）；③ 独立复现与 Developer 一致：新单测 53/0 + 全量 **278/0** + 身份 20/0 + py_compile OK；④ **阻塞项**：`/ai/rag/chat` 每轮 knowledge 请求会话持久化**双重调度**（engine.chat L254/L268 + main.py L333），mock 计数实证 schedule_session_persist=2；两 fire-and-forget 任务独立 session（连接池 pool_size=5 并发）check-then-insert 均读旧态，`content_hash` 无唯一约束 → 每轮会话消息确定性重复落库（4 行/轮）、恢复历史每轮重复、50 上限减半；修复：删除 main.py L333 冗余调度（或删 engine.chat 两处，二选一，只留一个调度点）；⑤ 9 项非阻塞建议（短期去重合并不刷新 created_at、content_hash 仅 content 不含 role、TTL 惰性物理不清理、_save/save_session_messages 超 50 行、持久化会话可能丢弃更新鲜 request.history、3s 会话恢复延迟、UTC/本地时区差、retriever docstring 过期、agent 端点未接会话/短期记忆）；⑥ 验收按实际复选框 36 项核对：32 项代码/单测核验通过 + 3 项真实 E2E 留 Tester + 1 项代码长度附条件。report：`specs/module-034-short-session-memory/review-report.md`；技能：verification-before-completion / requesting-code-review（原则应用，完整阅读全部变更文件 + 独立复现 + 阻塞实证，未显式调用 Skill 工具） |
| module-034 | team-lead | [FIX] Reviewer 阻塞 #1（双重调度）修复：`main.py` L333 删除重复的 `rag_engine._schedule_session_persist(...)`，只保留 `engine.chat` 内部（no-docs L254 / docs L268）自包含调度。全量回归 **278 passed / 0 failed** 通过（无回归）。已通知 Tester 真实 E2E 复验每轮会话不再重复落库（预期 2 行/轮而非 4 行） |
| module-034 | Tester | [TEST] **验收通过（36/36）**。① 全量回归 **278 passed / 0 failed**（254 基线 + 24 新增，3 个既有 Redis setex 弃用 warning 与模块无关）；② 新增单测 **24/24**（test_session_memory.py 11 + test_memory.py short 13）；③ 身份回归 20/20、memory+extractor 81、stream 5、py_compile 5 文件 OK；④ **真实 E2E（Java 8081 + AI 8001 + 真实 PG/Redis/bge-m3/DeepSeek）**：注册/登录、带 token knowledge 对话 → 短期摘要写入（memory:10:short: 落库）、recall_short 真实召回、匿名双 IP 隔离 PASS、会话持久化恢复优先核验；⑤ **Reviewer 阻塞 #1 复现 + 复验**：首跑修复前代码真实复现 4 行/轮重复落库 → team-lead 修复 main.py 后重跑 **2 行/轮、duplicated_contents=[]**（PASS）；⑥ 摘要失败降级真实验证（uid=11 facts=0 不落库）；⑦ 测试数据已清理（user 10/11、ip 71/72/90 全删，残留校验 0）。test-report.md + acceptance 36/36 签署 + 三记忆文件更新；未 git commit。技能：verification-before-completion / systematic-debugging（观察归因） / test-driven-development |

### 2026-08-06（module-035 分数口径校准）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-035 | Planner | 分数口径校准方案（根因：相对分/绝对分用错尺子 → 动态K 改绝对余弦 + 去重阈值 0.95→0.85 + min_score 校准 + 可选三通道 RRF A/B）；score-issues.md（问题分析）+ plan.md + acceptance-criteria.md（33 项）；三记忆文件更新 |
| module-035 | Developer | [CODE] 分数口径校准实现：① 动态K 改**绝对余弦**（P1 核心）——memory.py 新增 `_cosine`/`_child_embeddings`（按候选子块 id IN 查询读库 embedding，存时已 L2 归一化点积=cosine）/`_absolute_cosine_avg`（query 嵌入 + 每条绝对余弦 + 低分过滤 `abs_cosine<0.4` 丢弃 + 绝对余弦降序 + 均值判定档位 >0.85→5/0.75-0.85→3/<0.75→1），recall/recall_short 改走该口径，`_expand_to_parents` 优先取 `abs_cosine`；**K=5/3 档真实可达不再恒 1**；query 嵌入/embedding 读取失败降级用原 hybrid_score（不回退失败）。② 去重阈值校准（P5）——config `memory_dedup_threshold` 0.95→**0.85**（真实 bge-m3 同义改写 cosine≈0.88 可触发，记忆不膨胀）。③ min_score 校准（P2）——main.py chat_stream 移除失真 `MIN_SCORE=0.3`（作用于 min-max 相对分，语义失真），`relevant_count=retrieval_count`（仅统计展示）。④ 新增 config `memory_recall_min_score=0.4`（低分过滤阈值，绝对余弦口径）。⑤ **P3 三通道 RRF：评估后决定不采纳**（保持 min-max 加权）——硬阻塞：RRF `1/(60+rank)` 得分为 0.017-0.033，而 `engine._retrieve` 默认 `min_score=0.6` 过滤 `hybrid_score`（engine.py:655）→ 聊天路径全部文档被过滤恒空，采纳须级联重设该过滤语义（超出 P3 声明范围）；golden A/B 直连 retriever 不经该过滤无法暴露此回归。changelog.md 已产出；技能：test-driven-development（先更新/新增动态K 绝对口径断言再实现）/ systematic-debugging（测试 parent_id=0 被 `if parent_id` 视为空导致 _expand_to_parents 跳过取父→改测试 id 从 1 起；P3 分数量纲耦合归因）；pytest **292/0**（278 基线 + 新 14） |
| module-035 | Reviewer | [REVIEW] **结论：✅ 通过（无阻塞，4 项非阻塞建议）**。完整阅读全部变更文件（memory.py / config.py / main.py / test_memory.py / test_memory_extractor.py）+ 独立复现：① 核心四项核对全过——动态K **真用绝对余弦**（query embed_text L2 归一化 + 候选子块库 embedding L2 归一化 → 点积=cosine，`_absolute_cosine_avg`），**K=5/3 档真实可达不再恒 1**；低分过滤 `abs_cosine<memory_recall_min_score(0.4)` 丢弃防"本批相对高但绝对烂"注入；去重阈值 0.85 与动态 K **同绝对余弦口径**（真实同义改写 0.88 触发/0.80 不同事实）；embedding 失败（query 嵌入或 embedding 读取）**降级 hybrid_score 不回退失败**；min_score 移除失真阈值（plan 允许），relevant_count=retrieval_count 仅供 UI 展示。② 契约全过：save/recall 签名、recall 返回格式（content/score/title/created_at）、chat/stream 端点、三层 source、匿名降级均不变；下游 engine._recall_memory/tool_registry 仅用 content+created_at 不依赖 score 值零回归。③ 安全全过：`_child_embeddings` SQLAlchemy 参数化 IN 查询无新注入面、identity 隔离双保险保留、日志无敏感、无新依赖。④ 独立复现与 Developer 一致：记忆单测 **95/0**（81 基线 + 新 14）+ 全量 **292/0** + 身份 **20/0** + 引擎/流/会话 **18/0** + py_compile OK。⑤ 验收按实际复选框 **35 项**核对（acceptance 汇总表记 33 实际 35：功能 12 vs 11、代码质量 6 vs 5，建议签署时修正）：30 项代码/单测核验 ✅ + 3 项 P3 可选不适用 + 3 项真实 E2E 留 Tester。⑥ P3 RRF 不采纳决策复核通过（分数量纲与 engine._retrieve min_score=0.6 过滤硬阻塞 + A/B 不经该过滤无法暴露回归），记录 backlog。4 项非阻塞建议（#1 `docs.sort` 硬键 `d["abs_cosine"]` 在 min_score≤0 配置下可 KeyError→建议改 `.get`；#2 无 embedding 候选被静默丢弃→建议补 debug 日志；#3 chat 路径 `_recall_memory` top_k=3 使 K=5 档 chat 不可达（module-033 review #3 既有）；#4 acceptance 汇总表 33 vs 实际 35 修正）。review-report.md 已产出；技能：understand-anything:understand-diff / verification-before-completion / requesting-code-review（完整阅读 + 独立复现 + 实证，未显式调用 Skill 工具） |

### 2026-08-07（评估脚本修复 + module-036 规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-035 收尾 | team-lead | golden_retrieval 评估脚本修复：`compute_metrics` 标题匹配由精确等值改**容忍层级前缀**（golden 文档名 `X` 匹配检索标题 `X > 小节`，split 取最左段）。**根因**：module-031 分块重建后检索返回父块层级标题，旧精确匹配致 Hit@5 误报 0.13。**修复后真实质量**：hybrid Hit@5=**0.9565** / Recall@5=0.9130 / MRR=0.9130（23 题，7 无 gold 跳过）——**优于 module-024 的 0.9130 基线**，分块重建 + 分数校准有效。分类：java_gc/concurrency/kafka/comprehensive 1.0、ai_llm 0.8（KV Cache/RAG 1 题未进 top5） |
| module-036 | Planner | Agent 端点接入会话记忆方案（现状：agent/agent-lg 缺会话恢复 _resolve_session_history + 会话保存 _schedule_session_persist，普通 chat/stream 已有；复用现成函数 + 可选 ReactContext.client_ip→identity 命名修正）；plan.md + acceptance-criteria.md（29 项）；三记忆文件更新 |
| module-036 | Developer | [CODE] 实现：main.py 两 agent 端点（/ai/rag/chat/agent + /ai/rag/chat/agent-lg）——构造 ReactContext 前先 `await rag_engine._resolve_session_history(identity, request.history)` 会话恢复（无持久化回退 request.history 零回归，与 chat_stream Step 5 一致）；react_loop/langgraph_react_loop 结束后 `rag_engine._schedule_session_persist(identity, query, answer)` 会话保存（fire-and-forget 不阻塞 SSE）。命名修正（功能3）：ReactContext.client_ip→identity（字段+构造参数+docstring）+ tool_registry._recall_memory 用 ctx.identity + react_agent/langgraph_react_agent 参数同步；grep 全量核对无遗留 client_ip 记忆用途（ratelimit/IP 缓存语义正确不动）。changelog.md 已产出；技能：test-driven-development / verification-before-completion；pytest **298/0**（292 基线 + 新 6：TestAgentSessionMemory 4 + TestReactContextIdentity 2） |
| module-036 | Reviewer | [REVIEW] **结论：✅ 通过（无阻塞，4 项非阻塞建议）**。完整阅读全部变更文件（main.py agent/agent-lg 端点 / react.py / langgraph_react.py / tool_registry.py / test_agent_tools.py）+ 独立复现：① 核心四项核对全过——agent/agent-lg 真走 `_resolve_session_history`（main.py L516/L583，与 chat_stream L460 一致，直接 request.history 旧路径已移除）；循环结束后 `_schedule_session_persist`（L546/L613）fire-and-forget 不阻塞 SSE + 空 answer 守卫 + **单调度点无双重调度风险**（agent 端点不经 engine.chat，react_loop/工具/反射器均不调用，module-034 阻塞专项复核通过）；client_ip→identity 命名修正 grep 零残留（agent 目录 0 处，ratelimit/IP 缓存语义正确未误改，调用方全位置传参零破坏）；无会话零回归（`_resolve_session_history` 回退 request.history）。② 契约全过：端点签名 + SSE 事件格式不变、recall_memory 工具行为不变、会话 source `memory:<identity>:session:` 不变、匿名降级（user_id 否则 client_ip）不变。③ 安全全过：无新注入面（仅调 module-034 已审函数）、会话按身份隔离、日志无敏感、无新依赖。④ 独立复现与 Developer 一致：test_agent_tools **27 passed** + test_rerank_langgraph **18 passed** + 全量 **298 passed / 0 failed**（3 个既有 Redis setex 弃用 warning）+ py_compile OK。⑤ 验收按实际复选框 **29 项**核对（汇总表总数 29 一致，分项出入：代码质量记 5 实际 6、测试记 8 实际 7，建议 Tester 签署时修正）：27 项代码/单测核验 ✅ + 2 项真实 E2E（4.3-1/2）留 Tester。review-report.md 已产出；技能：verification-before-completion / requesting-code-review（完整阅读 + 独立复现 + 实证，未显式调用 Skill 工具） |
| module-036 | Reviewer | [REVIEW] **结论：✅ 通过（无阻塞，4 项 🟢 非阻塞建议）**。完整阅读全部变更文件（main.py / react.py / langgraph_react.py / tool_registry.py / test_agent_tools.py）+ 独立复现：① 核心四项核对全过——agent/agent-lg 真走 `_resolve_session_history`（main.py L516/L583，先解析再构造 ctx，与 chat_stream Step 5 一致，直接 request.history 旧路径已移除）；会话保存 `_schedule_session_persist` 在 react_loop/langgraph_react_loop **结束后**、最终 done 事件前触发（L546/L613，asyncio.create_task 只调度不 await 不阻塞 SSE）；client_ip→identity 命名修正 grep 全量 0 残留（`ctx.client_ip` 0 处、`ReactContext(client_ip=` 0 处，agent 代码 client_ip 仅 docstring 与合法限流/IP 缓存用途；调用方全位置传参零破坏）；无会话零回归（`_resolve_session_history` 空/失败/无身份回退 request.history，单测实证）。② 契约全过：agent/agent-lg 端点签名 + SSE 事件格式不变、recall_memory 工具行为不变（仅字段名改值相同）、会话 source `memory:<identity>:session:` 不变（复用 module-034 函数）、匿名降级不变。③ 安全全过：无新注入面（只调用现成参数化函数）、会话按身份隔离、无新依赖。④ 独立复现与 Developer 一致：agent 单测 **27/0**（21 基线 + 新 6）+ 全量 **298/0**（292 基线 + 6 新增）+ py_compile OK。⑤ 验收按实际复选框 **30 项**核对（汇总表记 29 实际 30：代码质量 5 vs 6，建议签署时修正）：28 项代码/单测核验 ✅ + 2 项真实 E2E（4.3-1/2）留 Tester。4 项非阻塞建议（#1 acceptance 汇总表 29 vs 实际 30 修正 #2 chat_agent 端点本体 ~57 行超 50 为 module-028/030 既有非本模块回归 #3 LLM 全失败时 persist 走 error 分支不触发可接受 #4 会话恢复最坏 +3s 与 chat 对齐）。review-report.md 已产出；技能：understand-anything:understand-diff / verification-before-completion / requesting-code-review |
| module-036 | Tester | [TEST] **验收通过（29/29）**。① 全量回归 **298 passed / 0 failed**（292 基线 + 6 新增，3 个既有 Redis setex 弃用 warning 与模块无关）；② agent 单测 **27/27**（21 基线 + 6 新增：TestAgentSessionMemory 4 恢复持久化/无持久化回退 request.history/保存触发参数断言/agent-lg 恢复+保存 + TestReactContextIdentity 2 无 client_ip 残留/recall 按 identity）；③ 相关回归 54/54（LangGraph 18 + 会话 11 + 流式 5 + 身份 20）、py_compile 5 文件 OK、grep `ctx.client_ip`/`ReactContext(client_ip=` 0 残留；④ **真实 E2E（uvicorn 8001 真实 HTTP 栈 + 真实 PG/Redis/DeepSeek，匿名 XFF 注入）**：**4.3-1 Agent 对话→会话落库→新对话恢复**——XFF=203.0.113.55 agent 对话 SSE tool_call×4/tool_result×4/token×2/done（answer 355 字）落库 2 行 source=`memory:203.0.113.55:session:`（user+assistant）；二次对话（history=[]）恢复持久化会话，answer 连贯（1208 字引用上轮主题）且新轮次追加落库（2+2=4 行无重复）；**4.3-2 匿名隔离**——IP_A agent 4 行 / IP_B agent-lg 仅自己 2 行，source 精确隔离无跨身份泄漏；⑤ **Reviewer 建议复核**：#1 acceptance 汇总表分项（代码质量 5 vs 6、测试 8 vs 7）已按实际 29 项修正、#2 chat_agent 端点本体超 50 行为 module-028/030 既有非本模块回归、#3 LLM 全失败 persist 走 error 分支不触发（与 chat 一致，行为合理）、#4 会话恢复 +3s 为 module-034 既有设计；⑥ 测试数据已清理（203.0.113.55/56 残留 0 行；观察库内 module-034 遗留 3 行 memory:203.0.113.71:session: 非本模块写入）。test-report.md + acceptance-criteria.md（29/29 签署）已产出；技能：verification-before-completion / systematic-debugging（E2E 落库归因）/ test-driven-development |

### 2026-08-07（评估脚本修复 + 质量确认）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-035 收尾 | team-lead | golden_retrieval 评估脚本修复：`compute_metrics` 标题匹配由精确等值改**容忍层级前缀**（golden 文档名 `X` 匹配检索标题 `X > 小节`，split 取最左段）。**根因**：module-031 分块重建后检索返回父块层级标题，旧精确匹配致 Hit@5 误报 0.13。**修复后真实质量**：hybrid Hit@5=**0.9565** / Recall@5=0.9130 / MRR=0.9130（23 题，7 无 gold 跳过）——**优于 module-024 的 0.9130 基线**，分块重建 + 分数校准有效。分类：java_gc/concurrency/kafka/comprehensive 1.0、ai_llm 0.8（KV Cache/RAG 1 题未进 top5） |
| module-035 | Tester | [TEST] **验收通过（35 项：32 通过 + 3 P3 不适用）**。① 全量回归 **292 passed / 0 failed**（278 基线 + 14 新增，3 个既有 Redis setex 弃用 warning 与模块无关）；② 记忆单测 **95/95**（81 基线 + 14 新增：TestRecallDynamicKAbsCosine 7 三档真实可达+低分过滤+空候选+嵌入失败降级 / TestRecallShortAbsCosine 1 / TestChildEmbeddings 3 / TestDedupThreshold035 2 / TestConfig035 1）；③ 身份回归 20/20、下游消费者（engine/stream/session）18/18、py_compile 5 文件 OK、无未使用 import；④ **真实 E2E（uvicorn 8001 真实 HTTP 栈 + 真实 PG/Redis/bge-m3/DeepSeek + JWT token 注入）**：**4.3-1 多档召回**——K=5 真实可达（5 条近义候选 dedup=False 构造，abs_cosine [1.0,0.903,0.867,0.824,0.748] 均值 0.868>0.85 → 5 条）；K=3 真实可达（5 条中质候选 [0.796,0.791,0.775,0.763,0.755] 均值 0.776∈[0.75,0.85) → 3 条，确认档位判定非候选截断）；K=1 宁缺毋滥（混质候选均值 0.713<0.75 → 仅最高 1 条）；**登录用户 E2E（JWT user_id=9001）**——5 次同义保存被 0.85 去重合并为 3 父块（不膨胀），recall 返回 3 条多档（不再恒 K=1）；user 9002 同 query recall 0 条（隔离）；**4.3-2 去重**——同义改写二次 save status=**updated**（parents 1→1 不膨胀）、不同事实 saved（1→2 正常新增）；**4.3-3 低分不注入**——追加无关记忆后 recall 主题 query 0 条注入；**chat_stream 真实 SSE**——检索 step count=2 relevant=2（MIN_SCORE 失真阈值已移除，语义正确）；⑤ **Reviewer 建议复核**：#1 `d["abs_cosine"]` 硬键排序 KeyError——默认 min_score=0.4 下无 abs_cosine 候选（0.0）在过滤步骤即被移除，不可达 sort，默认配置无影响（仅 min_score≤0 配置理论可达，建议后续改 `.get` 或加 config 校验）；#3 chat 路径 `_recall_memory` top_k=3（engine.py:290）K=5 档 chat 不可达（module-033 review #3 既有，非本模块回归，直接调 memory_service.recall 已真实可达 K=5）；#4 acceptance 汇总表 33 vs 实际 35 已修正；⑥ P3 RRF 3 项（1.4-1/2、4.1-3）评估不采纳标记不适用，记录 backlog；⑦ **实测观察（非缺陷）**：自然 dedup=True 流程下 5 条近义记忆会被 0.85 去重合并（正是去重目标），故 K=5 档多发生在"不同但均高质量"的候选集，实际对话路径更常触发 K=3/K=1；⑧ 测试数据已清理（`memory:%` 全库 0 行）。test-report.md + acceptance-criteria.md（35 项签署）已产出；技能：verification-before-completion / systematic-debugging（K=3 档位 vs 候选截断歧义归因 + dedup=True 下 K=5 构造探索）/ test-driven-development |
| module-034 | Reviewer | [REVIEW]（⚠️ **前次结论已被下一行升级取代**——阻塞项：/ai/rag/chat 双重调度会话持久化→确定性重复落库，见下行）全量审查：契约核对（三层 source 精确匹配 / 长期 `:%`→精确等值零回归 / save-recall 签名兼容 / 匿名降级 / 会话持久化 fire-and-forget 不阻塞 / 长短期注入段区分）；安全（三层按本身份隔离：长/短 `_layer_pattern` 精确 LIKE + 双保险转义，会话等值查询 / TTL 惰性过滤 fail-open / 日志无敏感）；架构（session_memory 复用 documents 无新表、依赖方向无循环、会话文档无向量不参与 FTS/向量/图检索）；独立复现 py_compile OK + test_session_memory 11/0 + test_memory 42/0（+13 short）+ test_identity 20/0 + extractor/stream 44/0 + 全量 **278/0**（与 Developer 自测一致）；验收 34 项中 31 项代码/单测核验 ✅、3 项真实 E2E 留 Tester（另发现 acceptance 汇总表接口类记 3 实际 4 项，按实际核对）。结论：⚠️ **有条件通过**（无阻塞；应修建议 #1 `/ai/rag/chat` 双重 `_schedule_session_persist`——engine.chat L254/268 + main.py L333 对同一轮两次调度，content_hash 幂等存在 TOCTOU 竞态可致会话重复轮次，建议 Tester E2E 前删一处；非阻塞 #2 短期行物理不清理 + 0.95 去重阈值近似重复累积 #3 持久化会话偏好可能丢更新鲜 request.history #4 save_session_messages 63 行超 50 #5 会话恢复 +3s 延迟 #6 TTL 时区差）。review-report.md 已产出；技能：understand-anything:understand-diff / verification-before-completion / requesting-code-review |
| module-034 | Tester | [TEST] **验收通过（36/36）**。① 全量回归 **278 passed / 0 failed**（254 基线 + 24 新增，3 个既有 Redis setex 弃用 warning 与模块无关）；② 新增单测 **24/24**（test_session_memory.py 11：保存 source/幂等 hash 去重/上限滚动/隔离恢复/失败返回空 + test_memory.py short 13：三层 source 精确匹配/短期去重 layer 隔离/TTL 过滤）；③ 身份回归 20/20、memory+extractor 81、stream 5、py_compile 5 文件 OK；④ **真实 E2E**（Tester 启动 Java 8081 + AI 8001 + 真实 PG/Redis/bge-m3/DeepSeek，结束已停）：注册/登录（user 10/11）、带 token knowledge 对话 → 日志 `extract_facts → save 长期 + save 短期`（memory:10:short: 落库）、recall_short 真实召回 1 条、匿名双 IP（203.0.113.71/72）隔离 PASS（ip1 session+short 落库、ip2 无）、`_resolve_session_history` 持久化优先 + 空/失败回退 request.history 单元级核验、长短期双段注入（历史记忆+最近上下文）核验；⑤ **Reviewer 阻塞 #1（双重调度）复现 + 修复复验**：首跑 AI 服务为修复前代码 → 真实复现 4 行/轮重复落库（duplicated_contents c=2）；team-lead 已修复 main.py（删除 L333 冗余调度）→ Tester 用**修复后代码重跑** → **2 行/轮、duplicated_contents=[]**（PASS）；⑥ 摘要失败降级真实验证（uid=11 extract_facts facts=0 不落库）；⑦ 测试数据已清理（user 10/11、ip 71/72/90 全部删除，残留校验 0）。test-report.md + acceptance-criteria.md（36/36 签署）已产出；技能：verification-before-completion / systematic-debugging（双重调度复现归因） / test-driven-development |
| module-034 | Tester(复验) | [TEST] **独立复验通过（与首轮 Tester 结论一致，加固验收）**。team-lead 修复后再次独立复验：① 全量回归重跑 **278/0** 一致；② **竞态复现**（真实 PG，模拟生产双调度 2 个 create_task，30 轮）修复前 **30/30 轮重复**（4 行/轮，content_hash 无唯一约束 TOCTOU 实证）→ 修复后单调度 **0/30**；③ 单实例 uvicorn 8001（Start-Process 分离式，规避 PowerShell 后台退出）真实 HTTP E2E：两轮知识对话 **4 行（2 行/轮）无重复**、日志每轮恰好 1 次 `会话持久化 new=2`；④ 偏好型 query → `extract_facts facts=2 → 短期/长期自动写入完成 saved=2`，short/long 层各 2 父块落库；⑤ 服务层真实 E2E **20/20**（会话保存/恢复/limit/幂等/隔离、save_short→recall_short 真实 bge-m3 召回、三层隔离、匿名无泄漏、TTL 父+子改旧过滤）；⑥ **归因修正**：首轮"多进程假象"澄清——工作区遗留 4 个并发 uvicorn 进程干扰，清掉后单实例复验确认修复有效；TTL 验证要点：recall 返回的 created_at 取**父块**，须父+子同改旧才触发过滤（真实保存父子同刻写入，非缺陷）；⑦ 测试数据残留校验 0。test-report.md 增补 §5 环境归因 + §6.1 竞态复现/修复闭环；技能：verification-before-completion / systematic-debugging（多进程假象归因）/ test-driven-development |
| module-033 | Tester(补跑) | [TEST] **真实 HTTP 端点 E2E 复验通过**（首轮服务未启动，补跑启动双服务：Java 8081 `APP_JWT_SECRET` 与 ai_service/.env `PW_JWT_SECRET` 同值注入未打印 + AI 8001 uvicorn 真实 PG/Redis/bge-m3/DeepSeek）。① 注册/登录 HS256 通过（user_id=8，token header 解码 alg=HS256）；② 带 token 保存记忆落库 source=`memory:8:`；③ 完全一致内容二次保存 **status=updated**（id 不变、parents 数不涨=3）→ >0.95 去重机制真实验证；同义改写 cosine≈0.88<0.95 按设计新增（阈值校准观察）；④ 登录 knowledge 对话（query 含偏好）→ 日志 `extract_facts facts=1 → save → 长期记忆自动写入完成 identity=8`，落库 memory:8:（自动提取链路真实 HTTP 跑通）；⑤ 闲聊/实时不提取（日志确认不触发 _persist_memory）；⑥ 无 token chat XFF=7.7.7.7 → 自动落库 memory:7.7.7.7:（匿名 client_ip 隔离真实验证），user 8 记忆不受影响；匿名 recall 仅返回本身份记忆、user 8 带 token recall 仅返回 user 8 记忆（无跨身份泄漏）；⑦ recall 返回 created_at + `[长期记忆 - 日期]：内容` 格式化注入；⑧ 测试数据已清理（memory:8:/memory:7.7.7.7: 共 12 行删除，残留校验 parents=0）。test-report.md §6.6 增补真实 E2E；技能：verification-before-completion / systematic-debugging（阈值观察归因） |
| module-033 | Tester | [TEST] **验收通过（40/40，4 项附条件非阻塞）**。① 全量回归 **254 passed / 0 failed**（215 基线 + 39 新增，3 个既有 Redis setex 弃用 warning 与模块无关）；② 新增单测 **39/39**（test_memory_extractor.py：提取/importance 过滤/失败降级/JSON 结构/去重（>0.95 更新、不同新增、失败降级）/动态K 三档+空候选/格式化注入/触发链路 chat+chat_stream）；③ 身份回归 20/20、memory+identity+stream 54/54、py_compile 5 文件 OK；④ **半真实 E2E**（真实 PG + 真实本地 bge-m3 + 真实 DeepSeek HTTP 200）：`_persist_memory` 自动写入链路真实跑通（extract_facts 返回事实 → save 落库 → recall 召回 → 格式化注入）、匿名 client_ip 两身份（9.9.9.9/8.8.8.8）隔离 PASS（含自动写入路径）、去重机制（identical content cosine 1.0→updated）✅；⑤ **Reviewer 关注项复核**：#1 动态K 真实 min-max 相对分 avg 恒<0.75 → 恒 K=1（K=5/3 档实际不可达，确认语义偏差）、#2 去重合并无脏数据/崩溃（子块向量不重建是已知取舍）、#3 chat 路径 top_k=3 截断 K=5 档（无逻辑错误）；⑥ **去重阈值校准观察**：真实 bge-m3 同义改写 cosine≈0.88<0.95 不触发去重（完全一致 1.0 触发），LLM 提取措辞不稳定 → 「二次同义对话→不膨胀」在措辞变化时不成立；机制正确属阈值校准问题，建议 module-034 下调阈值（约 0.85）或改绝对相似度口径；⑦ 时区观察：created_at 为 PG UTC（08-05）vs 标题本地日期（08-06），同天差 8 小时（环境既有非模块缺陷）；⑧ 验收项核对发现原统计 38 实际复选框 40（17/5/6/8/4），按实际 40 签署并修正统计表。test-report.md 已产出；技能：verification-before-completion / systematic-debugging（观察归因） / test-driven-development |
| module-032 | Tester | **真实 E2E 验收发现核心缺陷：不通过**。① 三栈回归全过：mvn 37/0、pytest 215/0、vitest 31 pass + 3 既有 ChatPage 基线失败（git log 确认 ChatPage.test.tsx 未被 module-032 触碰）；② 真实双服务 E2E（Java:8080 APP_JWT_SECRET 与 PW_JWT_SECRET 同值 + AI:8000 hermes venv uvicorn + 真实 PG）：注册/登录/me/重复用户名/匿名降级全过；③ **缺陷**：Java JwtUtil 对 64 字节 secret 经 jjwt 0.12.6 自动签 **HS512**（token header 解码实证），Python parse_jwt 仅接受 HS256 → pyjwt `InvalidAlgorithmError` 拒绝 → 带 token 保存记忆 source=memory:127.0.0.1:（应为 memory:1:）、B 登录召回 A 的记忆（跨用户隔离失败）；④ 根因：单测密钥 40 字节恰好 HS256 掩盖问题，生产 64 字节才触发；Reviewer 复现仅跑单测未做真实握手；⑤ 修复方向：Java 显式 `signWith(key, Jwts.SIG.HS256)` 或对齐 Python；⑥ 验收 40 项：32 通过 / 8 失败（1.3 两项 + 1.4 一项 + 2.1 算法契约 + 4.3 两项 E2E + 5.3 文档同步 2 项留收尾）；test-report.md 已产出。**环境坑**：PowerShell `Get-Content` 注入 ETS 属性致登录 500（脚本问题非模块缺陷）|

### 2026-08-02 收尾
- Planner（主会话）：记忆库同步（file-index/activity-log 补齐）、backlog 记录（重排分数校准、记忆库维护）

### 2026-08-10/11（module-050 幻觉检测模型对比 + WP5 目录细分）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-050 | Developer | ① **WP1 环境**：huggingface.co 502 且 hosts→127.0.0.1；hf-mirror 对 Python httpx 308→hf.co，Python 下载不可用 → curl resolve 直链下载 HHEM-2.1-Open（438MB）+ MiniCheck-RoBERTa-Large（1.36GB）+ flan-t5-base tokenizer；离线加载验证：MiniCheck 走 HF cache 布局（refs/main 无 BOM 坑）+ HF_HUB_OFFLINE + 剥 device_map（transformers 5.x CPU 坑，补装 accelerate）；HHEM 自定义远程代码 5.x 崩（all_tied_weights_keys + shared/embed_tokens 绑定键）→ get_class_from_dynamic_module + safetensors 手动加载 + embed_tokens 展开，predict() 分数与 README 参考值逐一吻合；② **WP2+3 对比**：eval/compare_factcheck_models.py 全量 100 对实测 **HHEM Accuracy 0.7700/F1 0.7527/Prec 0.814/Rec 0.700/0.364s每对 vs MiniCheck 0.5100/0.0392/1.000/0.020/2.855s每对**；kappa 0.0264、一致率 58%、P 平均绝对差 0.2392、不一致 42 条；**核心发现：MiniCheck 中文退化**（Rec 0.02 只抓回 1/50 supported，P(support) 中位 0.162；英文参考对判别正常排除 bug）→ 中文裁判倾向 HHEM；诚实边界声明（跨语言/claim=问题代理/注入文档非真实检索/512 截断）；③ **WP4 测试**：tests/test_compare_factcheck.py 12/12；④ **WP5 目录细分**：rag 拆 retrieval/graph/memory 三子包（state/models/schemas/engine/migrate_parent_child 留根）+ 12 根脚本移入 scripts/（sys.path 注入，python scripts/xxx.py 与 python -m scripts.xxx 均验证可运行）；**import 兼容关键**：存量 tests 的 mock.patch("rag.memory.hybrid_retriever") 等要求旧路径与真实模块同一对象 → rag/__init__.py 对 12 个旧模块名 sys.modules+包属性双注册（坑：子包 import * 覆盖同名属性，须走 sys.modules 取模块）；生产代码全部迁移新路径，grep 无残留旧引用；⑤ 全量 pytest **579 passed/0 failed**（567 基线 + 12 新增，rag 子包迁移与脚本移动后各复跑一次）；changelog.md 已产出；技能：systematic-debugging（离线加载三层适配）/ executing-plans |
| module-050 | Reviewer | 第一轮审查 **verdict: pass**：独立全量运行对比脚本**复现全部数字**（MiniCheck 0.5100/0.0392/1.000/0.020/2.891s vs HHEM 0.7700/0.7527/0.814/0.700/0.346s；kappa 0.0264、一致率 58%、P 平均绝对差 0.2392、不一致 42 条——与 changelog 逐一吻合，无伪造）；冒烟双侧（--skip-hhem/--skip-minicheck）跑通；模型文件完整核对（HHEM safetensors 438MB + MiniCheck 1.36GB HF cache 布局 + flan-t5-base tokenizer；README 参考值 0.0111/0.6474/... 存在）；MiniCheck max_support_prob 与 pred_label 语义源码核对一致；WP5 全仓库 grep 仅 main.py:100 一处残留旧 import（minor#1）；全量 pytest 579/0 独立复跑确认；3 minor 不阻塞（main.py:100 旧路径一行修复 / ADR-0010 状态未更新（文件在主 checkout 不在 worktree）/ 诚实边界第 4 条"512 截断"表述与 predict() 实际行为不符）；review-report.md 已产出 |
| module-050 | Tester | [TEST] **验收通过（579/579）**。① 全量回归 **579 passed / 0 failed**（104.30s，567 基线 + 12 新增，与 changelog 数字一致）；② 冒烟复跑 `python -m eval.compare_factcheck_models --limit 5` 跑通（exit 0）：MiniCheck 5 对 22.1s（4.415s/对）、HHEM 2.1s（0.411s/对）；不一致样本值与 changelog 全量逐对吻合（[1] Kafka ISR 0.294/0.907、[3] volatile 0.326/0.680——与全量前 5 一致）；诚实边界 4 条声明在输出中打印；③ 抽查实现：build_pairs=100 对/50 标注 supported 与数据源一致、_pre_chunk 中文句切正确、指标函数（Accuracy/F1/kappa）与测试断言口径一致；④ WP5 验证：scripts/ 12 脚本 + rag 三子包结构齐全；旧路径别名 sys.modules 同对象实测（rag.retriever is rag.retrieval.retriever）；`python scripts/backfill_search_tokens.py` 与 `python -m scripts.backfill_search_tokens` 双方式从 ai_service 可运行；git status 无存量 tests 改动、golden_sufficiency.py 未动；engine/agent/eval/main 的 diff 均为 import 迁移（无逻辑变更）；gitignore `/memory/` 锚定生效；⑤ 逐条 AC 26 项签署：24 通过 + 2 附条件（7.4 生产代码残留 main.py:100 旧路径懒加载 `from rag.embeddings import embedding_service`，经兼容层可用，非阻塞 minor；8.3 ADR-0010 状态行在 main checkout 非 worktree，合并时由主会话追加）；test-report.md 已产出 |

### 2026-08-11（module-051 HHEM 专职裁判）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-051 | Developer | [CODE] verify_answer 接入 HHEM 专职裁判（ADR-0010 P0-②）。① 共享加载器 `rag/retrieval/hhem_loader.py`（module-050 验证路径单一来源）+ `rag/retrieval/factcheck_judge.py`（HHEMJudge：延迟加载 + threading.Lock + asyncio.to_thread + 失败返回 None；compare_factcheck_models.py 改引用共享 loader）。② verify_answer 拆分：`_VERIFY_PROMPT` 纯拆句（字符串数组）/ 旧全量保留 `_VERIFY_LLM_PROMPT` 降级 / `_judge_by_hhem`（max 分映射三态 0.7/0.3 读配置 + evidence=max 文档号）/ `_judge_by_llm` / `_parse_claims`；旧格式 claims（带 verdict）直接采用跳过 HHEM——module-039 存量测试不改全绿；返回结构零改动。③ config：verify_judge_model 默认 "hhem" + 两阈值；降级链三层 + 开关 llm 零回归。④ `eval/golden_factcheck.py`：50 条（supported 20 + unsupported 20 继承充分性代理标注 + inferred 10 人工构造）+ kappa 三态/二值两口径 + eval_runs eval_type='factcheck' + --fixture。⑤ 真实模式实测（HHEM 就绪）：**kappa 三态 0.3252 / 二值 0.3220 < 0.7 未达门槛如实标注**（eval_runs id=15）；根因：中文分数压缩致 0.7 上界偏严 + HHEM 对部分覆盖判乐观；fixture 冒烟 kappa 0.9375。⑥ tests/test_factcheck_judge.py 32 项全 mock 不加载真实模型。全量 pytest **611/0**（579 基线 + 新 32）；changelog.md + ADR-0010 状态更新（P0-② 已实施：git-tracked 落点在 memory/project-context.md ADR 索引行；specs/adr/0010 为 gitignored 本地文件——worktree 副本状态行已就地更新，主 checkout 副本由主会话合并时追加）+ 三记忆文件更新；未 git commit |
| module-051 | Reviewer | [REVIEW] **结论：✅ 通过（pass，0 阻塞）**。独立复现：① 全量 pytest **611/0** 复跑一致（579 基线 + 新 32，117s）；② 真实模式 `golden_factcheck --no-save` 数字与 changelog 逐一吻合（kappa 三态 0.3252 / 二值 0.3220 / Acc 0.5600 / 11-2-15，"未达门槛如实标注"无伪造）；③ fixture 冒烟 kappa 0.9375 一致；④ 降级 prompt 无漂移：git show 对比 `_VERIFY_LLM_PROMPT` 与 module-039 `_VERIFY_PROMPT` 逐字节一致；⑤ py_compile 6 文件 OK；⑥ 前端零改动（ChatMessage/ChatPage/rag.ts 未触碰）+ engine.py/main.py 调用方零改动 + 开关 "llm" 零 HHEM import 实证；⑦ 旧格式兼容（带 verdict claims 跳过 HHEM）使存量 test_reflector 25 项不改全绿，全量实证。AC 8 节核验：§1-§7 全过；§8 文档基本完成（test-report 移交 Tester、main checkout CONTEXT.md ADR-0010 状态行合并时主会话追加，module-050 先例）。4 项 minor 非阻塞（#1 HHEM 判分无 wait_for 超时 + 交叉打分 5×5≈9s 总耗时可超 15s 哲学→建议 predict 加 wait_for 或限对数；#2 `_parse_claims` 旧格式 dict 带 verdict 缺 evidence 时前端 parseEvidenceRef 对 undefined 抛 TypeError→建议默认补 "N/A"；#3 活动日志"ADR-0010 状态更新"表述应注明落点在 project-context.md；#4 fixture 模式门槛行打印"达标"易误读→建议加 [fixture] 前缀）。review-report.md 已产出 |

### 2026-08-12（module-052 NLI 矛盾扫描前置决策 + module-053 RRF 三通道融合验证）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-052 | Developer | [CODE] NLI 矛盾扫描前置决策（ADR-0010 P1-③ 数据验证，只验证不动生产）。① **WP-0 环境**：hf-mirror curl resolve 直链下载 mDeBERTa-v3-base-xnli（557,652,046B fp16 + tokenizer 全家桶 → models/mdeberta-nli/）；transformers 5.14.1 离线加载无兼容坑（标准 DebertaV2ForSequenceClassification，注意 id2label=0 entailment/1 neutral/2 contradiction 与 XNLI 常规序不同，从 config 动态读）；3 对已知中文用例语义核对全对（entailment 0.9929/neutral 0.8932/contradiction 0.9992）；README 官方 XNLI zh 基准 **0.803**（与 Planner 探查 86.4% 不一致，以 README 为准）；资源实测：25 对批量 5.03s（0.201s/对短输入）/全量长输入 0.786s/对/峰值内存 1.95GB/磁盘模型账 6.21GB。② **WP-A 实测**：eval/compare_nli_models.py 100 对（SUFFICIENCY_DATASET 同源同构复用 build_pairs；人工三分类复核 E50/N50/**C0**——数据源无矛盾构造成分诚实标注）**mDeBERTa kappa 三分类 0.4711/二值 0.7600/Acc3 0.6800/Acc2 0.8800 vs HHEM(0.7/0.3 生产阈值) 0.1351/0.4400/0.3600/0.7200**；混淆矩阵 mDeBERTa entailment 46/50 vs HHEM 23/50，HHEM 48/100 判 contradiction（中文分数压缩 p25=0.058）；HHEM 阈值敏感性全网格最优 kappa3=0.2903（high=0.50/low=0.05）仍远低于 mDeBERTa → 差距内在非阈值 artifact。③ **WP-B 决策**：替换（推荐）——三态映射 entailment→supported/neutral→inferred/contradiction→unsupported；放行条件=矛盾构造样本集≥20 条 + 真实答案句子 + 真实检索文档的 kappa 复测 ≥0.7；阈值校准计划（低置信→inferred 阈值扫描/512 截断验证/交叉打分 5-20s 预算）；ADR-0010 状态行 + "P1-③ 选型结论"小节已更新（主 checkout 副本由主会话合并时同步）。④ tests/test_compare_nli.py 15 项全 mock（三分类标注/映射/指标/mock 模型/降级）；全量口径（共享 worktree，module-053 并行改造在途）**628/1**——唯一失败 test_golden_retrieval::TestMethodLengthLimit::test_retrieve_under_50（module-053 在途 retriever 方法长度自检，非本模块回归，grep 证实无 import 交集）；changelog.md + 三记忆文件更新；未 git commit |
| module-052 | Reviewer | [REVIEW] **结论：✅ 通过（pass，0 阻塞）**。独立复现全部核心数字：① 全量 100 对实测与 changelog 逐一吻合（mDeBERTa kappa 三分类 0.4711/二值 0.7600/Acc 0.6800/0.8800 vs HHEM 0.1351/0.4400/0.3600/0.7200；混淆矩阵 46/3/1+8/22/20 vs 23/15/12+1/13/36；不一致 54 条；HHEM vs 人工 64 条；HHEM 分数中位 0.3587）；② --smoke 3 参考对分数精确复现（entailment 0.9929/neutral 0.8932/contradiction 0.9992，防 embed_tokens 类坑确认无）；③ **HHEM 阈值敏感性独立网格复算（step 0.01）**：最优 kappa3=0.2903 精确一致、生产阈值 0.7/0.3 kappa3=0.1351 精确一致 → 差距内在非阈值 artifact 成立；④ tests/test_compare_nli.py 15/15 复跑通过；全量回归当前态 **645/0**（628/1 快照归因 module-053 在途 retrieve 66 行、现已收敛 47 行，归因正确）；⑤ git 核对生产代码零改动（module-052 仅 2 新文件 + 文档）、golden_sufficiency.py 只读、存量测试零改动；ADR-0010（gitignored）状态行 + P1-③ 选型结论已核对（三态映射/kappa 复测≥0.7/阈值校准/放行条件齐全）；记忆三文件核对（project-context 行 + 头部日期 + ADR 索引 / activity Developer 行 / file-index 两新行）。5 项 minor 非阻塞（#1 file-index"全量 629/0"应为 628/1 或当前 645/0 #2 HHEM 网格扫描无脚本落盘，建议按 module-047 threshold_scan 先例函数化 #3 资源实测受并行负载影响建议注明口径（复跑 0.388s/对 vs 0.201s/对、RSS 1.08GB vs 1.85GB）且 smoke rss 为推理后采样非真峰值 #4 "人工复核"措辞宜改为"由人工充分性标注派生 + 映射规则人工复核" #5 ADR 25 对交叉 5-20s 预算留实施模块落实批处理）。review-report.md 已产出 |
| module-053 | Developer | [CODE] 检索融合升级（RRF 三通道消融验证，只动 retriever/engine 开关层不动 reranker）。① **WP-0 DB 修复**：`scripts/migrate_module053.py`（幂等，查 information_schema 跳过）——documents 补 `last_mentioned_at`/`mention_count`（module-046 缺迁移，导致 graph_store.search_related 全列查询报列不存在）+ feedback 表建表（复用 FEEDBACK_DDL）；修后 search_related 真实返回 5 篇不再报错。② **WP-A 基线复测**：golden 112 题 **Hit@5=0.9714/Recall@5=0.9571/MRR=0.9270**（eval_runs id=17）与历史 id=13 一致；**口径钉死**：评估直调 retriever 不含引擎层图谱并行，历史 0.9714 为纯两通道口径；前置修复 module-050 目录细分回归——embeddings.py `_LOCAL_MODEL_DIR` 二级 dirname 解析到 rag/models/ 不存在致向量通道全断（首测退化 FTS-only 0.0381 id=16 无效），改三级 dirname（对齐 factcheck_judge 范式）恢复；eval 脚本 scores 增 fusion_mode/fusion_weights 落库 + compare 口径警告。③ **WP-B RRF**：`retrieval_fusion_mode` 开关（hybrid 默认/rrf/weighted）+ `rrf_constant_k=60` + 权重配置；retriever 层 `_execute_fusion` 三路并行（FTS/向量独立 session + 图谱 `_retrieve_graph_only`）→ RRF `score(d)=Σ1/(60+rank_i)`（1-based，缺路不参与）+ min-max 归一化 hybrid_score 兼容引擎 0.6 过滤（解决 module-035 记录的 RRF 原始分与过滤硬不兼容）；`round_num` 参数——fusion 仅 round 0 语义，round 1/2 单路混合；引擎 round 0 fusion 模式不再重复查图（防双倍 LLM 提取）；abs_cosine 融合前存档红线保持。④ **实测**：rrf **0.9905/0.9762/0.9341**（id=18，+0.0191 Hit@5，2 题翻盘 0 回退，增益来自图谱文档进 top-5）；加权 0.3/0.6/0.1 → = 基线（id=19）、0.25/0.5/0.25 → 0.9714/0.9619/0.9246（id=20，Recall 微升 MRR 微降）→ **加权否决，RRF 放行推荐启用**（PW_RETRIEVAL_FUSION_MODE=rrf 一键开启）；默认保持 hybrid（AC 零回归契约：rrf 默认会改引擎降级语义致 2 项存量降级用例失败，实测 2 failed，红线不改存量测试）。⑤ 引擎 rrf 分支真实 DB 冒烟：12.4s 返回 4 篇含 rrf_score、min_score 过滤兼容、父块映射正常。⑥ tests/test_rrf_fusion.py 16 项（RRF 公式已知值/三路融合/开关零回归/单路降级/abs_cosine 保留/加权/回退）。全量 pytest **645/0**（614 基线 + module-052 15 + 新增 16，零回归）；changelog.md + 08 简历文档 2.4 节更新（对比表 + 口径声明 + 面试话术）+ 三记忆文件更新；未 git commit |
| module-052 | Tester | [TEST] **验收通过（645/0 全量全绿 + 15/15 新增）**。① 全量回归 **645 passed / 0 failed**（115.08s；614 基线 + module-052 15 + module-053 16——changelog 628/1 快照的 module-053 在途失败已随其收敛（retrieve 66→47 行）消失，归因成立，无 module-052 回归）；② module-052 自身 `tests/test_compare_nli.py` **15/15** 通过（三分类标注 100 对派生 E50/N50/C0 一套两用/阈值映射边界/指标两口径/mock 模型不加载真实模型/缺失报错含路径/--skip 两侧降级）；③ 冒烟复跑 `python -m eval.compare_nli_models --limit 5`（真实模型，exit 0，无 DB 落库无需 --no-save）：mDeBERTa 离线加载成功、5 对 4.0s（0.801s/对 vs changelog 全量 0.786s/对一致）、5/5 entailment 全对；HHEM 5 对 2.0s（0.406s/对）、分数中位 0.400（vs 全量 0.359）——HHEM 中文分数压缩 + entailment 样本判 contradiction/neutral 行为与 changelog §2.3/2.5 完全一致（[0][2] contradiction、[3][4] neutral）；单标签子集 kappa=nan 为 sklearn 预期行为非缺陷；诚实边界 6 条声明输出齐全；④ 抽查实现：build_pairs 同源同构直接复用（compare_factcheck_models import）、THREE_CLASS_LABELS 从 SUFFICIENCY_DATASET 派生（程序化 + 人工复核）、cohen_kappa_score 三分类+二值两口径与测试断言口径一致、id2label 从 config 动态读（0=entailment/1=neutral/2=contradiction 非硬编码）、_require_model 缺失报错含路径、truncation=512 参数断言；git 核对 module-052 仅 2 新文件（compare_nli_models.py + test_compare_nli.py），生产代码零改动（改动均为 module-053 在途），golden_sufficiency.py 只读，存量测试零改动；⑤ 记忆硬核查：project-context.md module-052 行存在且格式对齐（含测试数字）+ 头部"最后更新 2026-08-12" + ADR 索引行 ✅；agent-activity-log.md Developer/Reviewer 行 ✅ 本行 Tester 追加 ✅；file-index.md 两新文件行（L64/65）+ spec 行 ✅（spec 行"全量 629/0"数字不准确已修正为 645/0，对应 Reviewer minor#1）；⑥ AC 逐条 23 项签署：§1-§7 全部通过（3 项 minor 非阻塞附注：资源实测受并行负载影响、file-index 数字修正、人工标注措辞）。报告：`specs/module-052-nli-contradiction-scan/test-report.md` |
| module-053 | Reviewer | [REVIEW] **结论：✅ 通过（pass，0 阻塞）**。独立复现全部核心数字：① 全量 pytest **645/0** 复跑一致（118.90s）；② eval_runs 表实查 id=13/16/17/18/19/20 与 changelog 逐一吻合（基线 17=0.9714/0.9571/0.9270 与 id=13 同；rrf 18=0.9905/0.9762/0.9341 含 fusion_mode 字段；加权 19=基线、20=0.9714/0.9619/0.9246；id=16=0.0381 环境故障记录声明有效）；③ **逐题翻转核验**：id=17 vs id=18 per_question 对比——恰好 2 题 miss→hit（Transformer Self-Attention、RocketMQ vs Kafka 选型）0 题回退，与 changelog 逐字吻合；⑤ 次数据真实性：5 次运行全部 105 题 degraded=0 无静默降级；④ DB 修复实查：documents 两列 + feedback 表 6 列存在，golden.json 112 题、skipped=7 与 DB 一致；⑤ 代码级核对：`_execute` 与引擎 hybrid 分支 diff 零改动（零回归）、`_fuse_rrf` k=60 1-based 排名公式与已知值测试吻合、abs_cosine 融合前存档 + 双命中透传保留、reranker.py 未触碰、round 0/round 1/2 语义参数+注释+测试齐、下游 `doc.get("hybrid_score", doc.get("score", 0.0))` 对 graph-only 文档无 KeyError；⑥ py_compile 7 文件 OK；存量测试零改动（仅新增 test_rrf_fusion.py 16 项）；⑦ 记忆硬核查：project-context 行 + 头部日期 2026-08-12 + activity Developer 行 + file-index 两新行 ✅；简历 08 文档 2.4 节对比表/决策/话术已更新 ✅。AC 9 节逐条核验全过（§9 test-report.md 移交 Tester——模块 053 Tester 未单独验收）。6 项 minor 非阻塞（#1 retrieval_fusion_mode 无枚举校验，非法值静默落 rrf 分支，建议 Literal 或白名单回退 hybrid #2 引擎 rrf 分支 embedding 故障无图兜底（与 hybrid 分支不对称），changelog 已声明未真实 HTTP E2E，建议上线前覆盖 #3 基线 id=17 scores 无 fusion_mode 字段（运行顺序所致），compare_runs 默认值兜底正确但隐式 #4 weighted 经 _execute_fusion 全流程无直测（仅纯函数覆盖，golden id=19/20 实测兜底） #5 共享 worktree 脏状态：module-033 changelog 未提交改动属并行会话产物，提交时按归属区分 #6 engine 口径差异（HyDE 图实体提取 vs 评估原 query）已在 changelog §3.3 如实声明）。review-report.md 已产出 |
| module-053 | Tester | [TEST] **验收通过（AC 9 节全过，0 阻塞）**。① 全量回归 **645 passed / 0 failed**（119.72s；614 基线 + module-052 并行 15 + 本模块新增 16，与 changelog/Reviewer 复跑一致；存量测试零改动，git status 仅新增 test_rrf_fusion.py）；② 新增单测 **16/16**（test_rrf_fusion.py：RRF 公式已知值 rank1=1/61 三通道已知排名精确断言/缺路不贡献/归一化 top=1.0、加权和/权重覆盖/非法权重回退、开关默认 hybrid 走 _execute 零回归/rrf round0 走融合/round1-2 单路混合、三路合并排序/向量失败降级/三路全空/abs_cosine 保留+双命中透传/融合异常回退 hybrid/图-only 结果保留）；③ **冒烟复跑（eval_runs 表实查，同源数字一致性）**：id=13/17=0.9714/0.9571/0.9270（hybrid）、id=18=0.9905/0.9762/0.9341（fusion_mode=rrf）、id=19=0.9714/0.9571/0.9270（weighted 0.3,0.6,0.1）、id=20=0.9714/0.9619/0.9246（weighted 0.25,0.5,0.25）、id=16=0.0381（degraded=105 环境故障记录无效声明成立）——与 changelog 对比表逐位一致；**逐题翻转独立复算**：17 vs 18 恰好 2 题 miss→hit（Transformer Self-Attention、RocketMQ vs Kafka 选型）0 回退，与 changelog 逐字吻合；5 次运行全部 105 题 degraded=0 无静默降级；④ **实现抽查**：`_fuse_rrf` k=60 enumerate start=1 1/(k+rank)、`retrieval_fusion_mode` 默认 "hybrid"（PW_ 前缀三配置在）、`_execute_fusion` 归一化/融合前 abs_cosine 存档、round 0 语义注释（retriever docstring + engine rrf 分支 + round 1/2 传 round_num）、reranker.py 零改动（git status 无）——全部与 changelog 一致；⑤ **DB/迁移**：documents 两列（last_mentioned_at timestamptz / mention_count integer）+ feedback 表 6 列（id/message_id/rating/comment/identity/created_at，对齐 FEEDBACK_DDL）实查存在；`python scripts/migrate_module053.py` 二次运行幂等（两列跳过 + feedback 已就绪）；⑥ **记忆硬核查**：project-context.md module-053 行格式对齐 + 头部"最后更新 2026-08-12" ✅；agent-activity-log.md Developer/Reviewer 行 ✅ 本行 Tester 追加 ✅；file-index.md migrate_module053.py + test_rrf_fusion.py 两新行 ✅（未改其他模块历史行）；简历 08 文档 2.4 节对比表/决策/口径防御已核 ✅。AC 9 节逐条签署全过（§6-2"图谱不可用标待环境"不适用——DB 修复后图谱真实可用；§9 test-report 本报告）。报告：`specs/module-053-rrf-fusion/test-report.md`。**模块标记 ✅ 完成** |
| module-054 | Developer | [CODE] 检索降级修复 + mDeBERTa 矛盾复测（specs/module-054-degradation-fix-and-nli-retest/）。① **WP-1 reranker 路径修复**：`rag/retrieval/reranker.py` `_LOCAL_MODEL_DIR` 二级→三级 dirname（对齐 embeddings.py 修法，module-050 目录细分回归）——真实加载冒烟 PASS（2.17GB 权重冷加载 10.4s，G1 查询相关文档 0.9996 排首、无关 0.0009/0.0000）。② **WP-2 方案 A**：retriever 向量化失败 → warning + `query_embedding=None`（不再 raise RetrievalException，hybrid/rrf/weighted；**vector_only 保持抛错**消融语义 changelog 声明）；`_execute`/`_execute_fusion` None 快路径（仅 FTS 或 FTS+图谱，不建向量 session 零额外调用，`_vector_search` 签名还原 list[float]）。真实验证：hybrid 故障 → 5 篇 FTS 结果无异常、rrf 故障 → FTS+图谱融合（graph/vector_score=0）、FTS 0 命中与 fts_only 对照一致。③ **WP-2 方案 B**：引擎 rrf 分支 catch 后补 `hybrid_retriever._retrieve_graph_only` 兜底（15s 超时，对齐 hybrid 图回退；防御层正常不触发零开销）——真实图谱验证 PASS（retrieve 抛异常 → 图兜底返回 Redis 持久化 doc id=445）。④ **WP-3 矛盾样本**：`eval/build_contradiction_dataset.py` 56 条构造样本（contradiction 32 = claim_vs_doc 16 + internal 16 + entailment 正例 16 + neutral 8，doc 为 SUFFICIENCY_DATASET 真实段落）落盘 `contradiction_dataset.json` + 标注指南 `contradiction_annotation_guide.md` + 加载/校验/golden_factcheck 双向转换 `contradiction_dataset.py`（question/claim/doc/verdict ↔ question/documents/label）。⑤ **WP-4 复测**：`eval/retest_nli.py`（--gen-real 生成 24 真实对：deepseek-v4-flash 真实答案句子 + DB golden 112 题 hybrid 检索片段，人工标注 E9/N13/C2 → real_retrieval_pairs.json；默认模式 80 对 mDeBERTa 打分 → kappa）——**kappa 三分类 0.4991/二值 0.6176 < 0.7 未达放行门槛，如实标注：降级双轨（NLI 只做矛盾扫描，不替换 HHEM 主裁判）**，结论写回 ADR-0010（eval_runs id=21 eval_type='nli_retest'）；失败模式：internal_contradiction 大量判 neutral（11/32→neutral）+ claim_vs_doc 反转判 neutral/entailment 各半。⑥ **WP-5 测试**：test_degradation_fix.py 9 + test_contradiction_dataset.py 10 = 19 新增全绿；方案 A 注释曾致 `retrieve` 53 行触发存量方法长度自检 → 压缩注释回 50 行（非改测试）。全量 pytest **667/0**（648 基线 + 19 新增）；changelog.md + ADR-0010 + 三记忆文件更新；未 git commit |
| module-054 | Reviewer | [REVIEW] **结论：✅ 通过（pass，0 阻塞）**。① 标注一致性抽查（WP-3/AC §3）：程序化核验 80 条 schema/分布（constructed 56 = C32 两类各 16 + E16 + N8；real 24 = E9/N13/C2）+ 逐条精读 internal 16 条与真实 24 对——claim_vs_doc 反转语义精确、entailment 原文对照、部分支持判 neutral 的严格口径贯穿一致（RocketMQ/MyBatis/Seata/FullGC）；雪花"三部分 vs 四段" contradiction 成立（穷尽性断言被 doc 否定）、类加载"5 vs 7 阶段"可辩护但最弱（5⊂7 子集，已建议"阶段数互斥"口径入指南）；**[M] AOF fsync internal_contradiction 口径不成立**（两半句主语为不同配置 no-fsync vs everysec，按指南规则 3 应判 neutral，contradiction_dataset.json:290）。② 代码级核对：WP-1 三级 dirname（对齐 embeddings.py 修法）测试断言防 rag/models 回归；WP-2 方案 A `query_embedding=None` 快路径零向量调用 + **vector_only 保持抛错**（_dispatch_mode 验证）+ 方案 B 引擎补图兜底 15s 超时嵌套降级，融合快路径无包 try 的 _retrieve_graph_only 内部全降级安全；③ 新增单测独立复跑 **19/19 通过**（test_degradation_fix 9 + test_contradiction_dataset 10）；④ 全量 pytest 独立复跑 **667/0（122.40s，5 存量 warning）**，与 changelog 一致；⑤ 复测结论核验：kappa 三分类 0.4991/二值 0.6176/Acc 0.6625（80 对）与 changelog + ADR-0010 逐位一致，降级双轨判定正确（0.4991 < 0.7 如实标注不硬推）；⑥ 文档核验：changelog/ADR-0010/project-context 行+日期+ADR 索引/file-index 8 新行/activity Developer 行 ✅，简历类未改 ✅；4 项非阻塞问题已入待办（[M] AOF 弱标 + changelog §4 分布笔误 E10/N12→实际 E9/N13 + changelog §7 用例数 16/3→实际 9/10 + 口径入指南）。report：`specs/module-054-degradation-fix-and-nli-retest/review-report.md` |
| module-054 | Reviewer | [REVIEW] **独立复核补充（轮次二）**：与首轮结论相互印证，追加独立证据——① **WP-1 真实加载冒烟独立复现**：CrossEncoderReranker 真实加载 2.17GB 权重，G1 查询相关文档 0.9996 排首、无关 0.0/0.0，冷加载 7.4s（非 mock）；② **eval_runs id=21 数据一致性核验**：scores（kappa_3class 0.4990724/kappa_binary 0.6176471/acc 0.6625/0.8375/gate 0.7）与 ADR-0010 表逐位一致；per_question 80 条（constructed 56 + real 24）vs 当前 real_retrieval_pairs.json **stored-vs-file mismatch = 0**——kappa 可复现、无事后改数；③ 全量 pytest 独立复跑 **667/0（120.77s）**；④ 与首轮 4 项问题（[M] AOF 弱标/分布笔误/用例数笔误/口径入指南）全部印证，无新增；⑤ 补充 minor：AC §1 聊天路径 E2E 冒烟未明确记录（仅直接 reranker 冒烟），建议 Tester 补真实 chat 端点 E2E。结论 **PASS（0 阻塞）**。report：specs/module-054-degradation-fix-and-nli-retest/review-report.md（§10 补充节） |
| module-054 | Tester | [TEST] **验收通过（AC 33 项全过，0 阻塞）**。① 全量回归 **667 passed / 0 failed**（142.49s；648 基线 + 本模块新增 19，与 changelog/Reviewer 复跑一致；存量测试零改动——git status 仅新增 test_degradation_fix.py 9 + test_contradiction_dataset.py 10，无存量修改）；② 新增单测 19/19（test_degradation_fix.py 9：reranker 三级路径断言+真实目录校验/方案 A hybrid+rrf 向量化失败降级+warning+向量检索零调用/vector_only 保持抛错/正常路径零开销/方案 B 图兜底+图兜底失败降级空+正常路径兜底零调用；test_contradiction_dataset.py 10：≥30 矛盾两类/正例对照比例/四键结构/verdict 合法/文档真实内容/标注指南落盘/to_factcheck_item 映射+roundtrip+JSON 存在）；③ **冒烟复跑（全部真实环境，非 mock）**：reranker 真实加载 2.17GB 权重 8.4s，G1 查询相关文档 0.9995 排首、无关 0.0002/0.0000；方案 A 真实 DB + embed_text mock 故障——hybrid 5 篇 FTS 兜底（vector_score=0.0）、rrf 5 篇 FTS+图谱融合（id=445 graph_score=1.0）、warning 日志确认、**vector_only 真实抛 RetrievalException**；方案 B 真实图谱——rrf 分支 retrieve 抛异常 → 引擎补图兜底返回真实图结果 2 篇（id=445 hybrid_score=1.0）；④ **复测数字一致性（eval_runs id=21 实查 + 独立重跑 retest_nli --no-save）**：kappa 三分类 0.4991/二值 0.6176/Acc 0.6625/0.8375、人工构造 0.4488/0.7101、真实检索 0.4700/0.4146、混淆矩阵（contradiction 34 判对 19：11 neutral+4 entailment）——与 changelog/ADR-0010 逐位一致，降级双轨结论成立；⑤ **AC §1 真实聊天路径 E2E 补跑（Reviewer 建议）**：rag_engine.chat 真实链路（真实 intent/检索/rerank/HHEM verify）72.0s 返回 message=ok、sources=3、真实 G1 答案——**无 RerankerException**，重排通道恢复正常；⑥ 记忆硬核查：project-context.md module-054 行格式对齐 + 头部"最后更新 2026-08-12（module-054 完成）" ✅；agent-activity-log.md Developer/Reviewer 行 ✅ 本行 Tester 追加 ✅；file-index.md 8 新文件行 ✅；ADR-0010 复测结论节（0.4991<0.7 降级双轨）✅；简历/文档类零改动 ✅。非阻塞 4 项（Reviewer 已列待办）：[M] AOF fsync 样本弱标（contradiction_dataset.json:290，两半句不同配置主语，按指南规则 3 应判 neutral，1/34 弱标 kappa 方向不变）+ changelog §4/activity 分布笔误 E10/N12 vs 实际 E9/N13 + changelog §7 用例数 16/3 vs 实际 9/10 + "阶段/段数互斥"口径未入指南——均不影响放行。报告：`specs/module-054-degradation-fix-and-nli-retest/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-12（module-055 提示词评估优化 + E2E 待办修复 + rrf 切默认）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-055 | Developer | [CODE] 提示词评估优化（ADR-0011 第一步）+ E2E 3 问题修复 + rrf 切默认（specs/module-055-prompt-eval/）。① **WP-1**：`eval/prompt_variants.py`（5 变体 baseline/v_brief/v_strict/v_fewshot/v_conservative × golden_sufficiency → 对比表 Accuracy/insufficient Recall/kappa/耗时；--variant/--limit/--no-save/--save 落 eval_runs eval_type='prompt_variant'/--fixture）+ `check_sufficiency(prompt=...)` 可注入（默认 _CHECK_PROMPT 零回归，自洽第二判同口径）；真实 LLM 冒烟 PASS；ADR-0011 补建（Planner 引用但文件缺失）+ 第一步状态更新。② **WP-2 intent 漏检**：诊断——E2E query 实时 classify=knowledge/conf 1.0（LLM 非确定性高置信误判是缺口，原 L2 低置信才触发）；修复——L2 无条件触发 + 规则表提前短路（零 DB）+ 停用词表两轮数据驱动扩充至 43 词（golden 扫描 20/50→0/50 误确认；正向 8/8）；golden_intent 真实评测 0.98（2 条 realtime→knowledge 系 LLM 自判非 L2，方向多检低风险）。③ **WP-3 HHEM 超时**：诊断——级联超时（HHEM 15 对负载 12s+ 贴近 15s + LLM 判分再 15s → verified_claims=0）；实测——15 对冷加载 9.04s/热 0.11s 每对/服务内冷加载 17-19s；修复——交叉对数上限（8×2）+ 预算 15→20s + 父块截断 500（实测 6 对 9.3s→2.4s、verdict 0.568→0.802 且消 512 token 溢出）+ lifespan 后台 fail-soft 预热；E2E verified total=8 非空。④ **WP-4 RRF abs_cosine**：诊断——融合路径存档代码正确（直调实测 0.70），真根因是图谱通道父块排 top-1 缺向量分数 + 旧"缺→0.0 保守标记"恒误触发（引擎 _retrieve 复现 id=239 top-1 MISSING）；修复——L3 只对实测低分标记；E2E chat top_abs_cosine=0.6664/0.7052、stream=0.8296 全部 suspected=false。⑤ **WP-5 rrf 默认**：config 默认 hybrid→rrf（保留 PW_RETRIEVAL_FUSION_MODE=hybrid 回退）；module-054 方案 A/B 已消解存量降级用例，**零存量断言改动**。⑥ 真实 E2E（uvicorn 8001 默认模式）：chat G1→knowledge/sources=3/verified=8；JVM→sources=4/verified=8（答案如实声明 KB 覆盖缺口）；你好呀→casual、现在几点了→realtime 均正确；stream retrieval 真实值 + 无误标记 + verified 事件正常。全量 pytest **688/0**（667 基线 + 21 新增 = prompt_variants 12 + intent 6 + factcheck 3）；changelog.md + ADR-0011 + 三记忆文件更新；未 git commit |
| module-055 | Reviewer | [REVIEW] **结论：✅ 通过（pass，0 阻塞 / 5 项 minor 非阻塞）**。① 全量 pytest **688/0** 独立复跑一致（159.96s，5 存量 warning）；定向三文件 **97/97**（prompt_variants 12 + intent_validation 47 + factcheck_judge 38，与 changelog 分项一致）；② 关键行为独立验证：无 PW_RETRIEVAL_FUSION_MODE 下 `settings.retrieval_fusion_mode=="rrf"`（WP-5 默认生效）；`_kb_terms("G1垃圾收集器的核心创新是什么？")=['G1','垃圾','收集器','核心','创新']`（专有术语保留）vs `_kb_terms("今天心情不太好")=['不太好']`（停用词扩充生效）；`test_default_prompt_zero_regression` 逐字节断言 prompt=None 与 _CHECK_PROMPT 一致（WP-1 零回归）；③ 代码级核对：L2 无条件触发（确认路径零 LLM 红线保持）+ 规则表提前短路（单测断言 fts/graph 不被 await）+ 43 词停用词数据驱动扩充（golden 扫描 20/50→0/50 记录）；HHEM 交叉对数上限（8 claims × 2 docs）在旧格式兼容判断之后、predict 之前生效，LLM 判分降级用全文不受截断影响；L3 判定与展示同源（engine.py:302），`is None` 判断语义正确（实测 0.0 仍标记、缺字段不标记）；HHEM 预热 `predict(["warmup"],["warmup"])` 签名匹配 + fail-soft；④ WP-5 零存量断言改动声明核实：git diff 仅 test_factcheck_judge/test_intent_validation 更新（行为升级两处均改名注明理由），test_degradation_fix.py（module-054 2 项降级用例）显式 monkeypatch 模式与默认值无关——声明属实，非掩盖；⑤ 记忆硬核查：project-context 行 + 头部日期 2026-08-12 + activity Developer 行 ✅（本行 Reviewer 追加）+ file-index 4 新行 + ADR-0011 状态"第一步已实施" ✅；简历/弹药/前端零改动 ✅；未 git commit ✅。AC 9 节逐条核验全过（§9 test-report.md 移交 Tester）。5 项 minor 非阻塞（#1 config.py 注释"按新语义更新断言"与 changelog"零存量断言改动"口径矛盾，疑似 module-053 旧注释搬运 #2 factcheck_judge 顶部 docstring 残留"15s 超时" #3 停用词扩充经 `_kb_terms` 单一路径连带影响 module-049 分诊 FTS 命中（方向一致未在 changelog 注明） #4 HHEM 预热 task 未持有引用（关闭期可能 "Task was destroyed" 告警） #5 module-033 changelog +47 行"附属发现"为工作树遗留物非本模块写入）。review-report.md 已产出 |
| module-055 | Tester | [TEST] **验收通过（AC 全过，0 阻塞）**。① 全量 pytest **688 passed / 0 failed** 独立复跑（152.56s，5 存量 warning）与 changelog/Reviewer 声明逐字一致；② **真实 E2E 冒烟（uvicorn 8001 默认模式，无 PW_RETRIEVAL_FUSION_MODE）**——3 问题场景全部修复：chat "G1垃圾收集器的核心创新是什么？" → steps.intent=knowledge/conf 1.0、sources=3（真实 G1 文档）、top_abs_cosine=**0.6664**、suspected=**false**、verified total=**8**（inf6/uns2，非空不再超时降级）；chat "JVM的内存结构是怎样的？" → knowledge、sources=4、top_abs_cosine=**0.7052**、false、verified=8（inf1/uns7）——与 changelog §7 逐位一致；"你好呀"→casual_chat（sources=0）、"现在几点了？"→realtime_not_implemented 均正确；stream G1 → intent 知识库、事件 step×4/token×529/verified×1/done×1、**verified claims 非空（supported/unsupported 正常）**、done 带真实 sources、0 error；③ **WP-4 stream 场景实测**：5 次 stream（G1×3/JVM/Redis/Kafka）retrieval top_abs_cosine 均 0.0 + **suspected 恒 false**——图谱通道父块（无向量分数）rrf 融合排首属预期新语义"缺字段如实 0.0 不误标记"（旧行为恒误标 true），与 changelog 描述一致；真实值场景由 chat 端 0.6664/0.7052 + changelog stream 0.8296 实证 + 单测 TestL3PostValidation（实测低分标记/整组缺失/top-1 缺失不标记）覆盖；④ **WP-1 冒烟**：`--fixture --limit 5`（baseline/v_brief/v_strict 同分属预期）+ 真实 LLM `--limit 3 --variant baseline,v_brief`（baseline 7.0s / v_brief 3.9s，deepseek 真实调用，对比表 Accuracy/kappa/耗时输出正常）；⑤ **WP-5 默认值验证**：无环境变量 `settings.retrieval_fusion_mode=="rrf"` ✅；⑥ **记忆硬核查**：project-context.md module-055 行（行 73 格式对齐）+ 头部"最后更新 2026-08-12（module-055 完成）" ✅、agent-activity-log.md Developer/Reviewer 行 ✅（本行 Tester 追加）、file-index.md 4 新行 ✅、ADR-0011 状态"第一步已实施" ✅；简历/弹药/前端零改动 ✅；未 git commit。非阻塞 3 项（沿用 Reviewer minor 记录）：config.py 注释口径矛盾（#1）、factcheck_judge docstring 残留 15s（#2）、stream 图谱父块排首时 0.0 属度量诚实非缺陷。报告：`specs/module-055-prompt-eval/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-12（module-056 L4 意图分类器上岗）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-056 | Developer | [CODE] L4 意图分类器达标启用（specs/module-056-intent-classifier-live/）。① **WP-1 人造数据集**：`eval/build_intent_dataset.py`（标注指南 docstring + build 校验：≥300/每类≥80/边界易混≥30/专有术语≥30/口语化≥20/E2E bug 类样本/与评测集字符串零重叠）+ `eval/intent_train_dataset.json` **337 条**（knowledge 132 = 边界易混 32 含 E2E bug 类"G1垃圾收集器的核心创新是什么？" + 专有术语 40 + 口语化 24 + 常规 36 / casual 105 / realtime 100）。② **WP-2 重训**：`train_intent_classifier.py` 训练源改 人造集→golden.json 112 题，**移除 load_golden_intent_samples（训练/评测分离防泄漏）+ 内置样本**；`IntentClassifier.fit` 返回新增 confusion_matrix；重训 449 条 **test split Accuracy 1.0000**（90 条全对角，旧 0.89）；落盘 models/intent_clf.joblib（不进仓库）。③ **WP-3 真实对比**：`golden_intent.py --compare-classifier` 新增（LLM=router 含 L2 / 分类器=裸 max 概率，同 100 条，对比表 + 落库 eval_type 参数化）——**LLM 1.0000 vs 分类器 1.0000**（每类 P/R/F1 双全 1.0，knowledge Recall 双 1.0；eval_runs id=23/24 实查确认；casual/realtime 30+20 零重叠纯泛化全对）；LLM 不可用启发式（闲聊/实时 0 命中）→ skipped 分类器单侧声明。④ **WP-4 启用**：达标线（Acc≥0.95 + know Recall≥0.95 + casual/realtime F1≥0.9）全过 → **`intent_classifier_enabled` 默认 true**（保留 PW_ 开关回退）；conftest 新增 autouse fixture 钉住测试环境 false（hermetic，存量 LLM 路径用例零改动）。⑤ **WP-5 测试+冒烟**：`tests/test_intent_dataset.py` 11 项（结构/平衡/边界/分离/回退三路径）；真实 HTTP（uvicorn 8001）：分类器路径 G1 E2E bug query→knowledge conf 0.9159 + 日志"意图识别(L4)"、你好呀→casual；模型改名回退路径→LLM 兜底 200（日志"分类器模型加载失败（回退 LLM 分类）"）冒烟后恢复。全量 pytest **699/0**（688 基线 + 11 新增）；changelog.md + ADR-0003 状态更新（L4 已启用 + 数据量）+ 三记忆文件更新；未 git commit |
| module-056 | Reviewer | [REVIEW] **结论：⚠️ conditional（附条件通过）——1 项 P1 必修 + 4 项 minor 非阻塞**。独立核对（非仅采信 changelog）：① 数据实查 337 条（132/105/100、边界 32 含 E2E G1、术语 40、口语化 24、无重复、与 golden_intent 评测集字符串零重叠）；② 重训复现 `--no-save` Accuracy **1.0000**（90 条全对角）与 changelog 逐位一致；③ eval_runs id=23/24 实查 DB（双 1.0000、per_question 100 条齐全、commit=eb40e165）；④ **执行序还原（关键诚实性验证）**：config.py mtime 15:43:49 > eval_runs created_at 15:43:19 —— 对比运行在启用开关翻转之前，"LLM 侧 1.0000"确为真 LLM+L2 运行非分类器自我对比；⑤ 进程内实测分类器：G1 E2E bug query→knowledge **0.9159**（与 changelog 冒烟数字逐位一致）、你好呀→casual、现在几点了→realtime、你们网站都有哪些功能？→knowledge、内存老是溢出咋办→knowledge；⑥ 语义重叠量化：评测 knowledge 50 题对训练集余弦 >0.95 共 23 题（仅 1 题字符串全等）——changelog"计划内重叠"声明方向保守无隐瞒；⑦ 全量 pytest **699/0** 独立复跑一致（178.87s）。**P1 必修**：`eval/golden_intent.py::run_compare_classifier` "LLM 侧"走 `router_agent.classify`，启用默认 true 后（模型在位）router 静默走 L4 路径（实测 reason="L4 classifier …"）——**任何重跑（含 Tester 验收重跑）"LLM 侧"会自我污染为分类器 vs 分类器**；须在 LLM 侧前钉住 `settings.intent_classifier_enabled=False`（本次记录数字不受影响，执行序已证）。4 项 minor：router.py docstring"默认仍用 LLM"过时 / intent_classifier.py docstring 训练源描述与分离口径矛盾 / changelog §2.3 重叠口径建议量化（23/50 >0.95）/ train 脚本打印"设置 PW_=true"提示过时。AC §1-7 + §8（除 test-report 归 Tester）全过；记忆硬核查（project-context 行/日期/activity Developer 行/file-index 5 行/ADR-0003）✅；简历/弹药/前端零改动 ✅；未 git commit。报告：`specs/module-056-intent-classifier-live/review-report.md` |
| module-056 | Developer | [FIX] Review conditional 逐条修复（specs/module-056-intent-classifier-live/changelog.md §9）。**P1 自污染**：`eval/golden_intent.py::run_compare_classifier` LLM 侧运行前钉住 `settings.intent_classifier_enabled=False` + 重置 `router_agent._intent_classifier/_classifier_tried`（防同进程早前按启用态加载），`finally` 恢复原开关与缓存；`record_eval_run` 配置快照补 `intent_classifier_enabled` 运行时字段（rag_config 表无此键，eval_runs 可回溯 L4 启用态）；模块 docstring + classify_intent docstring 注明默认运行测生产管线（L4 优先）口径。**minor**：router.py 模块/类 docstring"默认仍用 LLM"→"module-056 起默认启用"；intent_classifier.py docstring 训练源改人造集+golden.json（golden_intent 评测集不进训练）；train_intent_classifier.py 打印改"默认已启用；回退 PW_=false" + docstring"上线流程"→"启用现状"；changelog §2.3/§5.2 重叠量化（**1/50 字符串全等、23/50 余弦>0.95**，bge-m3 实测复现 Reviewer 数字）。**验证**：真实 router 路径钉住生效（启用态 reason=L4 classifier / 钉住态=真实 LLM 理由）；新增单测 test_golden_intent.py::TestRunCompareClassifier（LLM 侧钉住+恢复断言，1 项）+ record_eval_run 快照断言随契约演进更新；定向 23/23 通过；全量 pytest **700/0**（699 基线 + 1 新增，存量测试零改动）；纯脚本/docstring 改动不触产品代码路径；changelog §9 + project-context module-056 行/日期更新（本行 Developer 追加）；未 git commit |
| module-056 | Reviewer | [REVIEW-2] **结论：✅ 通过（pass，0 阻塞 / 0 新引入）**——第二轮审查核验 P1 + 4 项 minor 全部修复。① **P1 自污染修复三重复核**：代码级（golden_intent.py L394-400 钉住 `intent_classifier_enabled=False` + 重置 `_intent_classifier/_classifier_tried`，L439-442 finally 双恢复）✅；**真实进程验证**——启用态 `router.classify(G1 query)` reason=`L4 classifier {...}`（L4 路径），钉住态 reason=LLM 生成语义理由（真 LLM 路径），"LLM vs 分类器"不再自我污染 ✅；`record_eval_run` 快照补 `intent_classifier_enabled` 运行时字段（eval_runs 可回溯，LLM 侧钉住时如实记 False）✅。② 单测 `TestRunCompareClassifier`（钉住+恢复断言）+ `test_eval_runs_contract` 随契约演进更新（非掩盖）✅。③ **Minor 4/4 修复**：router.py 模块/类 docstring（L44-48/L168-171）✅；intent_classifier.py 模块+fit() docstring 分离口径（L17-19/L79-82）✅；changelog §2.3/§5.2 重叠量化 1/50 全等+23/50 余弦>0.95 ✅；train 脚本打印+docstring 启用现状 ✅。④ 无新引入问题核查：数据复验 337 条（132/105/100、边界 32 含 E2E、术语 40、口语化 24、全唯一、评测集零重叠）+ 标注一致性（全部分型 note 均 knowledge，随机 10 条抽查合理）；训练/评测分离红线保持（449 条=casual 105/knowledge 244/realtime 100，casual/realtime 零混入）；config 默认 true 达标注释未动摇。⑤ 全量 pytest 独立复跑 **700 passed / 0 failed**（175.81s）+ 定向 23/23 一致。⑥ 记忆硬核查：project-context 行 74 + 头部日期 ✅、activity Developer[CODE]/Reviewer/FIX 行 ✅（本行 Reviewer-2 追加）、file-index 3 行 ✅、ADR-0003 实查（specs/adr/0003-intent-validation.md L3/L6/L69-76）✅；简历/弹药/前端零改动 ✅；未 git commit ✅。观察（非本模块写入）：module-033 changelog 遗留物、faithfulness.json/golden_expanded.json 历史未跟踪产物。**Tester 放行提示**：验收在启用默认下重跑 `--compare-classifier`，LLM 侧 eval_runs 快照应含 `intent_classifier_enabled:"False"`（钉住证据）。报告：`specs/module-056-intent-classifier-live/review-report.md` §7 |
| module-056 | Tester | [TEST] **验收通过（AC 全过，0 阻塞）**。① 全量 pytest **700 passed / 0 failed** 独立复跑（149.17s，6 存量 warning）与 changelog §9/Reviewer 声明逐字一致；② **冒烟 1 分类器真实加载预测**：G1 E2E bug query→knowledge **0.9159**（与 changelog §6.1 逐位一致）、你好呀→casual 0.9414、现在几点了→realtime 0.8401，原始 softmax 精确和=1（产品层 round 4 位后 1.0001 为舍入噪声 <1e-3）；③ **冒烟 2 失败回退**：注入 load=False 桩分类器 + 真实 DeepSeek——reason=LLM 语义理由无 L4 标记，回退零影响；另验证**默认启用态**（无环境变量）`router.classify(G1)` 真实走 L4 路径（reason="L4 classifier…"、conf 0.9159）；④ **冒烟 3 重训复现**：449 条（105/244/100）test split Accuracy **1.0000**、混淆矩阵 [[13,0,0],[0,57,0],[0,0,20]] 与 changelog 逐位一致；⑤ **Reviewer P1 放行验证**：`--compare-classifier --no-save` 真实重跑（启用默认下）——LLM 侧 reason 为真实 LLM 语义理由（无 "L4 classifier"），钉住生效，对比非自污染；⑥ **数据实查**：337 条（knowledge 132/casual 105/realtime 100）、边界易混 32（含 E2E bug 类）、专有术语 40、口语化 24、query 唯一、与 golden_intent 评测集字符串**零重叠**（三类均 0）、golden.json 112 题并入（union 449）；⑦ **记忆硬核查**：project-context.md module-056 行（行 74 格式对齐）+ 头部日期"2026-08-12（module-056 完成 + Review 修复）" ✅、agent-activity-log.md Developer[CODE]/Reviewer/Developer[FIX]/Reviewer-2 四行 ✅（本行 Tester 追加）、file-index.md 5 新行 ✅；简历/弹药/前端零改动 ✅；未 git commit ✅。非阻塞 1 项观察（沿用 Reviewer）：realtime query "现在几点了" 分类器概率 0.8401 vs Reviewer 首轮 0.8231——同为 realtime 决策、嵌入推理确定性内差异（模型落盘时间不同），非缺陷。报告：`specs/module-056-intent-classifier-live/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-12（module-057 数据验证批）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-057 | Developer | [CODE] 数据验证批 5 个硬数字（specs/module-057-data-validation-batch/，开工前已读 project-context.md 全文）。**① 矛盾复测 v2**：`retest_nli.py` 新增句级拆解（。！？；!? 切子句 → 逐子句 vs 文档 + 子句两两互判 → 最严聚合：任一矛盾→contradiction）+ 阈值校准（低置信 max prob<t→neutral，0.5-0.9 步长 0.05 扫描，一次性打分纯 CPU 复用）+ 同口径旧集逐阈值曲线 + eval_runs 'nli_retest_v2'（id=25 初版/id=26 增强版含 same_set_scan + 过激聚合误杀分析）；`build_contradiction_dataset.py` 扩 30 条（追加在首版 56 之后保持 constructed[:56] 同集）：contradiction 53（claim_vs_doc 30 + internal 23 含 8 条多句"前真后假"）+ entailment 22 + neutral 11 = 86 条，指南同步。**实测（id=26）**：全量 110 对 kappa **0.4311**（t=0.80）、同口径旧 80 对 **0.3754 < 基线 0.5167（-0.1413）**——最严聚合误杀 9 条 + 真实检索子集 0.4700→0.0957，**改进不成立**，降级双轨维持，结论如实写回 ADR-0010（新"kappa 复测 v2"节 + 下一轮方向：中文专用/更大 NLI、微调、两阶段拆句+保守矛盾门控）。**② 图谱消融**：`--ablate` 同跑 graph_only 0.7429 → hybrid(rrf 三通道) 0.9810 = **+0.2381**（按类 java_gc +0.4444/agent +0.5714/redis 0）；单模式落库 id=27（0.7333）/id=28（**0.9905**）——--ablate 不落库（module-038 既有行为），delta 由两次单模式派生，LLM 实体提取运行间波动 ±0.01 如实注明。**③ 改写真实增益**（id=29，rrf 口径 n=104，8 skipped 含 1 rewrite_failed）：Hit@5 **+0.0096** / Recall@5 +0.0048 / MRR **-0.0353**（improved 3/worsened 10；不充分子集 n=6 MRR -0.1944）——基线近饱和改写无净收益，生产改写链路保留不改默认。**④ RRF k 扫描**：`eval/benchmark_rrf_k.py` 新建（三通道候选每题只收集一次，逐 k 纯 Python 融合，拐点自动加密）——k=20-100 **全平坦 Hit@5 0.9810**（两通道 0.9714），**图谱净增益 +0.0096**，k=60 保持不改默认（无需测试适配）；id=30 'rrf_k_scan'。**⑤ 飞轮冒烟**：`eval/flywheel_smoke.py` 新建（自动拉起/停止 uvicorn 8001）——5 条自造知识库问题真实 chat 全部 200 → POST /ai/feedback 5 次（rating 交替 ±1，2 条带 comment）全 ok → feedback 表 identity=203.0.113.66 共 **6 行**（message_id/rating/comment/created_at 正确）+ rating=0→422 + 同 message_id 二次提交落库 2 行（**后端无幂等，防重在前端已评态 localStorage rag_feedback_rated**，如实记录不改生产）；message_id 用构造标识 990000+i（AI 层直连无 Java 消息主键，声明）；**6 行保留为飞轮种子**。**⑥ 测试**：`tests/test_nli_improve.py` 32 项（句切/低置信/聚合/拆解管线/阈值扫描/样本集 ≥50 internal≥20 且首 56 同集，假 scorer 注入不加载模型）+ `tests/test_benchmark_rrf_k.py` 8 项（fuse_rrf 公式/归因）；全量 pytest **740/0**（700 基线 + 40 新增，557s）；changelog.md（5 硬数字 + 口径声明 + 诚实边界）+ ADR-0010 + 三记忆文件更新；前端/简历/弹药零改动；未 git commit |
| module-057 | Reviewer | [REVIEW-1] **结论：✅ 通过（pass，0 阻塞 / 0 新引入）**——第一轮审查，完整阅读 plan/AC/changelog/ADR-0010 复测 v2 节 + 全部变更文件（retest_nli.py / build_contradiction_dataset.py / benchmark_rrf_k.py / flywheel_smoke.py / test_nli_improve.py / test_benchmark_rrf_k.py / contradiction_dataset.json / 标注指南）。**DB 独立核验（5 硬数字）**：① id=26 'nli_retest_v2' per_question 重算——全量 110 对 kappa **0.4311**、同口径旧 80 对 **0.3754 < 0.5167（-0.1413）**、真实子集 **0.0957**、最严聚合误杀 **9 条**、矛盾 55 判对 29/判 neutral 22，与 changelog 逐位一致；② id=27 graph_only 0.7333（mode/fusion_mode=rrf）/ id=28 hybrid **0.9905** 落库字段正确，+0.2381 由 --ablate 派生（±0.01 波动已声明）；③ id=29 query_rewrite delta +0.0096/+0.0048/-0.0353、improved 3/worsened 10、skipped 8、不充分子集 n=6 MRR -0.1944 一致；④ id=30 rrf_k_scan 曲线全平坦 0.9810/两通道 0.9714/图谱净增益 +0.0096/k60 持平一致；⑤ **feedback 表实查 6 行**（identity=203.0.113.66、990001-990005、rating 交替 ±1、2 comment、990001 二次提交 2 行；rating=0 无污染行），8001 已停服。**样本集实查**：86 = 53/22/11、首 56 保持 module-054 同集、JSON 键集一致。**前端防重实查**：ChatMessage.tsx:57 localStorage 'rag_feedback_rated' 按 message_id 已评态（声明属实）。**全量 pytest 独立复跑 740 passed / 0 failed（166.62s）**。**记忆硬核查**：project-context 行 75 + 头部日期 + ADR-010 索引行 ✅、activity Developer 行 ✅（本行 Reviewer 追加）、file-index 5 行 ✅、ADR-0010 复测 v2 节 ✅。minor 5 项（changelog/file-index 单测计数 38/30 应为 40/32、eval_runs id=25 重复落库未说明、A2 delta 未持久化到 eval_runs、最优阈值未入 config（评估侧合理）、拐点加密仅补 k=25 + 图谱空候选 24/112 未落库）+ 观察 3 项（module-056 遗留未提交改动 router.py/test_golden_intent.py/module-033 changelog、历史未跟踪产物 faithfulness.json/golden_expanded.json、A2 头条数字取 --ablate 未落库与 id=27/28 差 ±0.01 已声明）。**Tester 放行提示**：全量 pytest 复跑 + WP-A5 链路复验（可另造身份避免与种子数据混淆）。报告：`specs/module-057-data-validation-batch/review-report.md` |
| module-057 | Tester | [TEST] **验收通过（AC 全过，0 阻塞）**。① 全量 pytest **740 passed / 0 failed**（158.64s，44 warning 与基线一致）独立复跑与 changelog §7 逐字一致；② **硬数字 DB 复算（5/5）**：id=26 per_question 独立重算 kappa 三分类 **0.4311** / 同口径旧 80 对 **0.3754（-0.1413）** / 真实子集 **0.0957** / 矛盾 55 判对 29 / 最严聚合误杀 **9**（与 changelog 逐位一致）；id=27 graph_only 0.7333→id=28 hybrid 0.9905（fusion_mode=rrf 字段实查）+ --ablate 派生 +0.2381 口径注明；id=29 query_rewrite delta +0.0096/+0.0048/-0.0353（improved 3/worsened 10、skipped 8）；id=30 rrf_k_scan 曲线全平坦 0.9810/两通道 0.9714/图谱净增益 +0.0096/best 20=k60 持平；**feedback 表实查 6 行种子**（identity=203.0.113.66、990001-990005、rating 交替 ±1、2 comment、990001 二次提交 2 行）。③ **真实管线冒烟**：`retest_nli --limit 20 --no-save` 真实 mDeBERTa 拆解+阈值扫描+门槛判定跑通（limit 子集全 contradiction 故 kappa 0.0、Acc 0.65，矛盾→neutral 失败模式复现一致）；`benchmark_rrf_k --limit 5 --no-save` 真实三通道收集+k 扫描跑通（k 平坦、图谱空候选 4/5 如实标注、无落库）；**飞轮链路独立复验**（另造身份 203.0.113.99，规避种子混淆）：真实 chat 200（500 字回答）→ feedback +1 200 ok → 重复提交 200（同 message_id 2 行，后端无幂等复证）→ rating=0 422 → 落库字段正确 → 测试行已清理（种子 6 行未动）、8001 已停服。④ 样本集实查：86 = 53/22/11、首 56 保持 module-054 同集、internal 23（旧 15 单句逗号混合 + 新 8 多句"前真后假"）、JSON 键一致、指南已更新。⑤ **接口零改动**：生产代码零 diff（git status 仅 eval/测试/记忆/文档；router.py+test_golden_intent.py 为 module-056 遗留）、rrf_constant_k=60 未改、test_rrf_fusion 断言保持。⑥ **记忆硬核查**：project-context 行 75 + 头部日期 + ADR-010 索引 ✅、activity Developer/Reviewer 行 ✅（本行 Tester 追加）、file-index 5 行 ✅、ADR-0010 复测 v2 节 ✅。非阻塞 minor 沿用 Reviewer 5 项（计数 38/30 应为 40/32、id=25 重复落库未说明、A2 delta 未持久化、最优阈值未入 config、拐点加密仅 k=25）——均为文档/落库完善类，不影响数字与结论。报告：`specs/module-057-data-validation-batch/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-13（module-058 检索链优化 + 工具治理 P1）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-058 | Developer | [CODE] 检索链优化 + 工具治理 P1（specs/module-058-retrieval-chain-opt/，开工前已读 project-context.md 全文；WP-A 拼标题用户决策推迟；原 059 并入为 WP-E）。**① WP-B prompt 顺序**：`_GENERATE_PROMPT` 区块顺序改 sections→检索到的文档→用户问题（docs 前移前缀缓存铺路，sections 内容/格式一字不改）；`tests/test_prompt_order.py` 新 6 项；存量 test_memory.py 1 项顺序预期按验收 §1 许可更新。**真实探测**（scripts/probe_prefix_cache.py，deepseek-v4-flash）：单文档 637 token 未达 DeepSeek 硬盘缓存门槛（cached 恒 512 固定 prompt 头）；**多文档 3001 token 同 docs 二次生成 billed miss 3001→57-60（-98%，cached 0→2944）缓存命中**；verify 口径核实（module-051 拆分后 LLM 只拆句、docs 不进 LLM prompt，无前缀可复用，收益面在 generate_answer 同 docs 重复生成）。**② WP-C 可观测性**：`src/observability.py` 新建（contextvar 观测上下文：trace_id/timing/usage/cache 计数，PW_REQUEST_LOGS 默认 true 关闭零埋点零落库）+ main.py trace_id 中间件（request.state）+ `persist_request_log`（chat/chat_stream/agent/agent-lg 四端点接线，流式 finally 落库，fail-open）+ engine.py 阶段计时（intent/triage_rewrite/retrieve/rerank/reflection/generate/verify）+ _retrieve 缓存命中计数 + retriever.py `_timed_channel` 并行主路径三通道各自计时 + llm/client.py `_extract_usage`/`_record_usage`（OpenAI/langchain/Anthropic 兼容，流式不采集）+ `REQUEST_LOGS_DDL` init_db 幂等（对齐 048 feedback 模式）+ RequestLog ORM；`tests/test_observability.py` 新 14 项（trace 贯穿/计时/token 累积/缓存计数/幂等落库/fail-open/端点接线/开关零埋点）。**真实 trace 样例**（scripts/probe_request_trace.py）：真实 chat 全阶段计时（intent 3.9s/retrieve_fts 6.5ms/vector 123.7ms/graph 1.7s/rerank 20.9s 冷加载/generate 10.2s）+ usage deepseek 4561/1562 + request_logs 落库查回全字段；E2E 期间自动落库 4 行（agent/agent-lg 含三通道计时+usage，422 失败请求同样留痕）。**③ WP-E 工具阶段切分（ADR-0012 方案 A）**：tool_registry.py AgentTool.group + `to_llm_schemas(group=None)` 默认全量 10（检索组 7/生成组 4，re_search 双组，10 工具 name/description/args_schema 一字不改）；react.py ReactContext.phase + 公共辅助 `schemas_for_phase`/`advance_phase`（判定以"已调用过生成工具"为界、下一轮切 generation、generation 内 re_search 不回退单向前进）；langgraph_react.py llm_call/execute_tools 复用同一辅助防漂移；config `tool_phase_split`（PW_TOOL_PHASE_SPLIT 默认 true/false 回退全量）；conftest autouse 钉住测试环境 tool_phase_split=false + request_logs_enabled=false（对齐 056 模式）；预算=0/预算耗尽兜底路径逐字不变；`tests/test_tool_phase_split.py` 新 14 项。**④ 两循环真实 E2E 冒烟**（uvicorn 8001，默认 true）：/ai/rag/chat/agent（search_knowledge→recall_memory→search_fts→search_knowledge，0 生成工具 0"尚未检索"防御串，4/4 预算耗尽兜底 1590 字）+ /ai/rag/chat/agent-lg（search_knowledge→search_fts→search_vector→search_knowledge，1451 字）——LLM 未实际调生成工具，阶段轮次推进由单测覆盖（诚实边界）；E2E 会话行已清理，request_logs 样例/E2E 行保留为观测种子。**⑤ 全量 pytest 774/0**（740 基线 + 34 新增）；changelog.md（实现决策/取舍/实测数字/口径声明/诚实边界）+ ADR-0012 状态行更新（✅ P1 已实施，并入 module-058）+ 三记忆文件 + CONTEXT.md 只增不删 + 08 文档 2.8 面试口径更新点；未 git commit |
| module-058 | Reviewer | [REVIEW-1] **结论：✅ Conditional（2 项 major 修复后重审）**——完整阅读 plan/AC/brief/059 brief/ADR-0012/changelog + 全部变更文件（observability.py / main.py / engine.py / retriever.py / client.py / tool_registry.py / react.py / langgraph_react.py / database.py / models.py / config.py / 三个新测试文件 / conftest）。**独立验证**：① 全量 pytest 复跑 **774 passed / 0 failed（154.01s）**与 changelog 逐字一致；② 新测试逐文件收集计数：test_tool_phase_split **18** + test_observability **10** + test_prompt_order 6 = 34（文档写 14/14 口径错误，总数对）；③ **request_logs 表 DB 实查 4 行**：id=1 probe 行与 changelog trace 样例逐位一致（timings 9 键 + usage deepseek 4561/1562）；id=3/4 agent/agent-lg 行佐证 MAJOR-2（usage 键含 'llm'）；**无 422 行**（佐证 minor #1）。**MAJOR-1**：trace_id 未贯穿日志——AC §2「挂 request.state + 日志 extra」的日志 extra 未实现（get_trace_id 定义但零调用、无 logging filter，无法用 trace_id 关联日志行；database.py COMMENT「贯穿日志与落库」与实现不符）→ 建议 logging.Filter 注入 extra + 1 单测。**MAJOR-2**：chat_with_tools token 用量标签恒为 "llm" 非供应商名（client.py:197/233 `_record_usage("llm", raw)`）——AC §2「fallback 链各供应商」在 agent/agent-lg 端点失真（工具调用轮次无法按供应商归属，DB 实查佐证）→ 建议标签改用 provider 标识 + 1 单测。**minor 3 项**：① changelog「1 行 422 失败请求也落库」与事实不符（DB 无 422 行、代码上 422 在端点前抛出不可能落库；行 id=2 agent/空 timings/空 usage/error=False 来源未说明）→ 建议改口径并说明 id=2 来源；② 测试分文件计数 14/14 应为 18/10（changelog/project-context/file-index 三处）；③ 观察：工作树携带 module-056 遗留未提交改动（router.py/test_golden_intent.py/module-033 changelog）+ CONTEXT.md 早期会话追加，提交时与 module-058 分离。**通过项**：WP-B 顺序/标签零漂移、probe 数据自洽、verify 口径诚实；WP-C contextvar 非全局 + 幂等 DDL + fail-open + 流式 finally、E2E 行证实流式 contextvar 传播；WP-E 归组 7/4 与 ADR-0012 逐字一致、两条循环共用辅助防漂移、预算路径未动、conftest 钉住模式正确、10 工具零改动；记忆三件套 + ADR-0012 状态行 + CONTEXT.md 只增 + 08 文档 2.8 只追加 ✅。报告：`specs/module-058-retrieval-chain-opt/review-report.md` |
| module-058 | Developer（修复轮） | [FIX] Reviewer conditional 意见逐条修复（specs/module-058-retrieval-chain-opt/changelog.md §7）：① **MAJOR-1 trace_id 贯穿日志**——observability.py 新增 TraceIdFilter（filter 从 contextvar 取 get_trace_id() 注入 record.trace_id，消除死代码）+ install_trace_id_filter()（幂等，根 logger + handler 双挂——Python logging 祖先 filter 不作用于传播 record，故必须挂 handler）；main.py 日志格式加 [%(trace_id)s] + basicConfig 后安装；② **MAJOR-2 chat_with_tools 用量按供应商**——LLMClient._provider_label()（DeepSeek→deepseek / ModelScope 系→self._label / Claude→claude），client.py:197/233 两处改传（DB 实测旧 'llm' 桶证据，修复后按供应商落桶）；③ **MINOR-1 changelog 422 误记修正**——DB 实查 4 行无 422（FastAPI 422 在端点前抛出不可能落库），改"流式结束/断开与端点异常经 finally 留痕"口径，来源不可确证的 id=2 异常样例行已删除，保留 3 行可确证种子；④ **MINOR-2 测试数口径修正**——实测 test_tool_phase_split 18（初版文档误写 14）+ test_observability 10；修复轮新增 6 项（TestTraceIdInLogs 3：record.trace_id 注入/无请求空串/install 幂等 + TestChatWithToolsUsageLabel 3：deepseek/qwen/claude 三路径供应商标签），最终 **40 新增 = 6 + 18 + 16**；⑤ **MINOR-3 观察确认**——router.py/test_golden_intent.py/module-033 changelog/CONTEXT.md 系先前会话遗留未触碰（提交时与 module-058 分离）。测试要点：TraceIdInLogs 用例如根 logger 默认 WARNING 滤 INFO 需显式 setLevel 放行（独立调试确认）。**全量 pytest 780/0**（740 基线 + 40 新增）；changelog §7 修复记录 + 三记忆文件更新；未 git commit |
| module-058 | Reviewer（第 2 轮） | [REVIEW-2] **结论：✅ Pass（上轮 2 major + 2 minor 全部修复，无新阻塞；新发现 1 项 minor 不阻塞）**——核查 conditional 修复：① **MAJOR-1 trace_id 贯穿日志已修复**（observability.py TraceIdFilter 从 contextvar 注入 record.trace_id + install_trace_id_filter 幂等双挂根 logger/handler；main.py basicConfig 格式 [%(trace_id)s] + 其后安装顺序正确；get_trace_id 死代码消除；database.py 旧 COMMENT 已移除；TestTraceIdInLogs 3 项全绿）；② **MAJOR-2 chat_with_tools 用量按供应商已修复**（_provider_label：deepseek/qwen/zhipu/modelscope/claude 映射，_chat_with_tools_openai/bind 两处改传，fallback 链内各客户端天然分桶；TestChatWithToolsUsageLabel 3 项三路径断言无 'llm' 桶；存量 test_agent_tools 零改动）；③ **MINOR-1 已修复**（changelog §3.1/§6.4 改 finally 留痕口径 + 422 不落库；id=2 异常行已删除——DB 实查 request_logs 现 3 行 id=1/3/4 与声明一致，id=3/4 含 'llm' 键系修复前历史种子行即 bug 实据，保留合理）；④ **MINOR-2 已修复**（changelog/project-context/file-index 三处统一 6/18/16=40 新增、780/0；逐文件计数独立复核一致）；⑤ MINOR-3 遗留改动观察复核未触碰（router.py/test_golden_intent.py/module-033 changelog/CONTEXT.md 均先前会话内容）。**独立验证**：全量 pytest 复跑 **780 passed / 0 failed（148.11s，42 warnings）** 与 changelog 逐字一致；新测试逐文件计数 6/18/16。**新 minor 1 项**：ADR-0012 第 5/61 行状态数字未随修复轮更新（仍"774/0"与"测试 14 项"实为 18 项）→ 建议改 780/0 + 18 项；观察（非阻塞）：TraceIdFilter docstring 对"祖先 logger filter 不作用于传播 record"的技术表述不准（CPython Logger.filter 沿祖先链检查，根 logger 挂载已足够），实现双挂行为正确仅注释偏误。报告：`specs/module-058-retrieval-chain-opt/review-report.md`（第二轮） |
| module-058 | Tester | [TEST] **验收通过（AC 全过，0 阻塞）**。① 全量 pytest **780 passed / 0 failed（153.66s，43 warnings 与基线同源）**独立复跑与 changelog §5 逐字一致（740 基线 + 40 新增）；② **新测试逐文件计数**：test_prompt_order 6 + test_tool_phase_split 18（5+9+3+1）+ test_observability 16（3+3+1+1+2+3+3）= 40，断言内容与 changelog §4.2/§7 声明逐项对应（检索组 7 无生成工具/调 generate_answer、verify_answer 后下一轮切 generation 4/re_search 不回退/开关 false 全量 10/预算路径不变/langgraph 同步/_SYSTEM_PROMPT 全 10/TraceIdFilter 注入/usage 供应商桶）；③ **实现抽查全过**：react.py schemas_for_phase/advance_phase（_GENERATION_GATE_TOOLS={generate_answer,verify_answer}，单向前进）、langgraph_react.py llm_call+execute_tools 复用同一辅助、tool_registry group 归组 7/4 与 ADR-0012 逐字一致（re_search 双组）、config 开关默认 true、conftest autouse 钉住 false+request_logs=false、reflector _GENERATE_PROMPT 顺序 sections→检索到的文档→用户问题（标签格式一字不改）、engine.py 七阶段计时+_retrieve 缓存计数、retriever._timed_channel 三通道并行主路径、client.py _extract_usage 三形态+_provider_label、REQUEST_LOGS_DDL 幂等对齐 048、main.py 中间件+四端点接线（含流式 finally）；④ **DB 实查**：request_logs 3 行种子（id=1 probe 与 changelog §3.2 逐位一致：timings 9 键+usage deepseek 4561/1562；id=3/4 含修复前 'llm' 桶实据）→ **Tester 真实 E2E 复验新增 4 行（id=5-8）**：agent×3 + agent-lg×1 全部 usage 仅 deepseek 键（**MAJOR-2 修复的生产侧证明**）、三通道计时齐全、error=False；⑤ **两循环真实 E2E 冒烟（uvicorn 8001，PW_TOOL_PHASE_SPLIT 默认 true）**：/ai/rag/chat/agent tool_trace=search_knowledge→recall_memory→search_knowledge→search_knowledge（第 1 条，含 PowerShell 编码乱码环境注记）与 search_knowledge×4（curl UTF-8 重跑，正常 1098 字线程池参数答案 [6][7][8] 引用、5 sources）；/ai/rag/chat/agent-lg tool_trace=search_knowledge→search_fts→search_vector→search_graph（1432 字 G1 GC 停顿预测答案、5 sources）——两循环全部检索组工具、**0 生成工具、0"尚未检索"防御串、0 error 事件、tool_count 4/4 预算耗尽兜底**；E2E 会话行已清理（7 行 memory:127.0.0.1:session: 删除），request_logs id=5-8 保留为新增观测种子；⑥ **WP-B 冒烟抽查**：probe_prefix_cache.py 判定逻辑与 changelog 数字自洽（cached +2944 > 500 阈值、miss 3001→60、verify 口径声明）；⑦ **记忆硬核查**：project-context module-058 行（780/0+40 新增+修复记录）+ 头部日期 2026-08-13 ✅、activity Developer/Reviewer/修复轮 4 行 ✅（本行 Tester 追加）、file-index 6 行 ✅、CONTEXT.md 只增不删（module-058 追加节 ✅）、ADR-0012 状态行"✅ P1 已实施（并入 module-058）"✅；⑧ **非阻塞 minor 1 项（沿用 Reviewer 第 2 轮新发现）**：ADR-0012 第 5/61 行数字仍 774/0 与"测试 14 项"，与最终 780/0/18 项不一致（状态行实质正确，建议主会话收口时改）。报告：`specs/module-058-retrieval-chain-opt/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-13（module-060 verify 异步化）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-060 | Developer（续作） | [CODE] verify 异步化（specs/module-060-verify-async/，开工前已读 project-context.md 全文；网络中断续作——先 git status + 文件完整性检查再补齐，未盲目重写）。**① WP1 后端任务基础设施**：`src/verify_tasks.py` 新建——submit_verify_task（先插 verify_results pending → `asyncio.create_task(_run_verify)` fire-and-forget（对齐 module-033 _schedule_persist）→ 返回 task_id；开关关/落库失败返回 None fail-open）+ _run_verify（perf_counter → reflector.verify_answer 复用 15/20/15s → UPDATE done+failed，任务内全捕获）+ get_verify_task **读 DB 为准**（pending/done/failed/None）；内存池只持执行期中间态、done callback 释放、DB 结果不清理（飞轮数据源）；`verify_results` 表（VERIFY_RESULTS_DDL 幂等对齐 048/058）+ VerifyResult ORM + `verify_async_enabled`（PW_VERIFY_ASYNC 默认 true）。**② WP2 chat_stream + 轮询端点**：main.py chat_stream 开关 true 不再同步 await verify → done 带 verify_task_id + verified:false + **不再发 verified 事件** + 连接早于 verify 关闭；开关 false 现状同步路径逐字一致（逃生口）；新端点 GET /ai/rag/chat/verify/{task_id}（pending/done/failed/404，查询异常也 404 fail-open）；request_logs 零改动。**③ WP3 前端**：types（verifyTaskId + VerifyTaskResult）/ ragService（chatStream done 解析 verify_task_id + fetchVerifyResult 404 归一化 failed）/ ChatPage（doSend 后 loading 立即结束 + verifying 状态 + startVerifyPolling 2s/30 次上限，done 更新 verifiedClaims，failed/404/超时停止 fail-open + **生命周期清理**：新发送/重试/切会话/新建/卸载 clearInterval）/ ChatMessage（verifying prop + "正在验证…"提示）。**续作补齐**：handleRetry 半完成编辑（旧消息 patch 无 verifying/无轮询/无清理）补齐、ChatMessage render 未透传 verifying 补齐、MessageDTO 缺 verifying 字段补齐、前端测试缺失补齐、mock.patch 模块名 bug（"main_module." → "main."）修复。**④ 测试**：`tests/test_verify_tasks.py` 新 17 项（submit/get/状态流转/TTL 释放/开关/pending 落库失败 fail-open/轮询端点状态机/DDL 幂等/chat_stream 开关两分支）；conftest autouse `default_verify_async_disabled` 钉住 false（对齐 056/058）；前端 test_verify_tasks 相关：ragService +6 / ChatPage +4 轮询（+3 存量环境性修复：conversationService mock + 挂载等待 + M18 过期断言 '知识库'→'Agent 模式'）/ ChatMessage +4 verifying 态。**⑤ 结果**：全量 pytest **795 passed / 2 环境性失败**（真实 Redis 用例——本机 Redis 3.2 不支持 redis-py 8.1 RESP3 HELLO，非本模块回归；Redis 已启动实测仍 HELLO 失败）；前端 vitest **58/0** + tsc/vite build PASS；**真实 E2E 待环境**（本机无 PostgreSQL——服务 init_db 依赖无法启动，全盘/注册表/WSL/Docker 均无；方法学+命令见 changelog §7）；changelog.md（实现决策/取舍/测试结果/口径声明/诚实边界）+ ADR-0013（✅ 已实施）+ 三记忆文件 + CONTEXT.md 只增；未 git commit |
| module-060 | Reviewer | [REVIEW] **结论：✅ Pass（无 major，5 项 minor 不阻塞 → 进 Tester）**。**独立验证**：① 全量 pytest 复跑 **795 passed / 2 failed（231.57s）**与 changelog 逐字一致——2 项失败复现定位根因=**Redis 未运行**（redis-server 无进程 + localhost:6379 TcpTestSucceeded=False + redis-py 8.1 "Timeout connecting"），module-060 零触碰 cache.py/llm_chain.py/Redis 代码（diff 核对）→ 环境依赖非本模块回归，如实标注"待环境"；② 前端 vitest 复跑 **58/0** + tsc exit 0 + vite build 成功；③ 新测试逐文件计数：test_verify_tasks 17（4+4+1+5+3）+ ragService +6 + ChatPage +4 + ChatMessage +4 全一致。**通过项**：WP1 submit 先插 pending 再 create_task + 池防 GC + done callback 释放 + 开关关返回 None；_run_verify 全捕获 + verify_answer 内部超时返回 empty_result → done+空 claims（前端不显示 fail-open 一致，status=failed 仅真异常）；get 读 DB 为准四态；DDL 幂等对齐 048/058 + ORM 字段对齐；WP2 chat_stream 开关 true 不再同步 await + done 带 task_id/verified:false + 无 verified 事件（测试断言事件序列 + verify_answer 零调用）、false 逐字一致（事件序列 step×4→token×2→verified→done）、clean_answer 保留、identity XFF→client_ip/trace_id 从 state；轮询端点 pending/done/failed/404 + 查询异常 404；request_logs 零改动；WP3 loading 立即结束 + verifying + 2s/30 上限 + done 更新 verifiedClaims + failed/404/超时停止 + 生命周期清理（新发送/重试/切会话/新建/卸载 clearInterval + verifyTimerRef 竞态防护）；handleRetry 续作补齐与 doSend 一致；记忆三件套 + ADR-0013 状态行 + CONTEXT.md 只增 ✅。**minor 5 项（不阻塞）**：① 前端轮询 `patchLastAssistant` 未受清理保护——清理后 in-flight fetch resolve 会把旧任务 verifiedClaims 挂到最新消息（AC「竞态防护」仅覆盖 stopTimer）→ 建议捕获目标消息 index 再 patch；② 遗留调试测试文件 `__tests/_debug_m060.test.tsx`/`_debugm2.test.tsx`（未跟踪、console.log + expect(true)）污染 58 计数 → 收口前删除；③ AC「全量 780+N 全绿」本环境未字面满足（795/2 环境性）→ Tester 复跑确认同根因，持续标"待环境"；④ 文档「异常/超时→failed」措辞与实现出入（verify_answer 内部超时→done+空 claims，行为更合理）→ changelog/AC 补充措辞；⑤ 观察：router.py/test_golden_intent.py/module-033 changelog 系先前会话遗留未触碰。报告：`specs/module-060-verify-async/review-report.md` |
| module-060 | Tester | [TEST] **验收通过（AC 全过，0 阻塞；2 项环境性附注）**。① 全量 pytest **795 passed / 2 failed（225.00s）**独立复跑与 changelog §5.4 逐字一致——2 项失败 `test_cache.py::test_prefix_invalidation_real_redis` + `test_llm_chain.py::test_set_get_roundtrip_real_redis` 复现根因=**Redis 未运行**（Get-Process redis-server 无进程 + Test-NetConnection localhost:6379 TcpTestSucceeded=False + redis-py 8.1 "Timeout connecting to server"）；git diff 核对 module-060 零触碰 cache.py/llm_chain.py（cache/Redis 代码零改动）→ 环境依赖非本模块回归，持续标"待环境"（780 基线原全绿系 module-058 当时 Redis 在运行）；② 新测试 `test_verify_tasks.py` **17/17** 通过（submit 返回 uuid+先插 pending+池持有释放 / 异常→failed / 开关关 None / pending 落库失败 fail-open / get 读 DB 四态 / DDL 幂等 / 轮询端点状态机 pending/done/failed/404+查询异常 404 / chat_stream 开关 true done 带 task_id 无 verified 事件+verify_answer 零调用、false verified→done 顺序）；③ 前端 vitest **58 passed / 0 failed**（23.24s，9 文件）+ `npm run build`（tsc strict + vite）**PASS**——模块 060 相关新测试 ragService +6 / ChatPage +4 / ChatMessage +4 全绿；④ **实现抽查全过**：main.py chat_stream 开关 true（599-614：done `{sources, verified:False, verify_task_id}`、不 yield verified 事件、连接早于 verify 关闭、clean_answer 保留、identity/trace_id 从 state 透传）+ 开关 false 现状同步路径（616-623 逐字一致）+ 轮询端点 GET /ai/rag/chat/verify/{task_id}（641-678：pending/done/failed/404+查询异常 404 fail-open）；src/verify_tasks.py 全文核对（submit 先插 pending→create_task fire-and-forget→池防 GC→done callback 释放；_run_verify 全捕获+15/20/15s 复用；get DB 为准）；config `verify_async_enabled=True`（PW_VERIFY_ASYNC，L169）；conftest autouse `default_verify_async_disabled`（L73-85 钉住 false）；database VERIFY_RESULTS_DDL+ensure_verify_results_table+init_db 追加（幂等）；models VerifyResult ORM 字段对齐（task_id UNIQUE/claims JSONB/verified_in_ms）——全部与 changelog 一致；⑤ **记忆硬核查**：project-context.md module-060 行（行 77 格式对齐含测试数字）+ 头部"最后更新 2026-08-13（module-060 完成）" ✅、agent-activity-log.md Developer/Reviewer 行 ✅（本行 Tester 追加）、file-index.md 新行（verify_tasks.py L97 / test_verify_tasks.py L98 / specs L99 / ADR-0013 L100 / 前端 L112-116）✅、ADR-0013 状态行 ✅、CONTEXT.md module-060 追加节（L190-197 只增不删）✅；⑥ 真实 E2E 冒烟**待环境**（本机无 PostgreSQL，服务 init_db 无法启动；changelog §7 方法学+命令已备，异步行为由 17 单测 mock DB 覆盖——诚实标注）；⑦ 非阻塞 minor 2 项（沿用 Reviewer）：**调试测试文件残留** `frontend/src/__tests/_debug_m060.test.tsx` + `_debugm2.test.tsx`（`__tests` 单下划线误命名目录、未跟踪、console.log+expect(true)，污染 vitest 58 计数为 56 真实+2 调试——收口前删除）；前端轮询 `patchLastAssistant` 未受清理保护（Reviewer #1 竞态，非阻塞）。报告：`specs/module-060-verify-async/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-13（module-061 记忆纠错）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-061 | Developer | [CODE] 记忆纠错（ADR-0007 P0+P1，specs/module-061-memory-correction/，开工前已读 project-context.md 全文）。**① WP1 评测闭环**：`eval/memory_conflict_dataset.py` 新建——30 条五类记忆矛盾标注集（改口 10/迁移 4/过时 3/升级冲突 3/正例中性 10）+ `contradiction_metrics` P/R/F1 + 达标判定（contradiction Recall≥0.8 且 Precision≥0.8）；**真实 mDeBERTa baseline（eval_runs id=31 'memory_conflict'）Accuracy 0.60 / contradiction Precision 1.0000（tp=10 fp=0）/ Recall 0.5000（fn=10）/ F1 0.6667——未达门槛（Recall 0.5<0.8）→ 开关默认关（不预设成功，数据说话；mDeBERTa 矛盾判别短板与 module-054/057 结论一致，但记忆级短句场景 Precision 极高值得注意）**。**② WP2 P0 升级留后悔药**：`_promote_memory` 升级**不删除短期副本**（后悔药）+ 长期新条目 superseded=false/updated_at=now + 幂等保留；documents 表加 superseded/updated_at 两列（`MEMORY_SUPERSEDED_DDL` init_db 幂等 ALTER + `scripts/migrate_module061.py` 本地先决已执行，DB 校验两列在）；召回侧过滤 superseded（`_expand_to_parents` + `_evolve_recall`，`_is_superseded` 用 `is True` 防 MagicMock 真值误伤存量测试）。**③ WP3 P1 冲突消解**：`rag/memory/nli_loader.py`（镜像 eval 已验证 transformers 5.x 加载路径单一来源，顶层零重依赖）+ `nli_judge.py`（MemoryNLIJudge：延迟加载 557MB + threading.Lock + to_thread + 20s 超时 + 失败 None 降级，对齐 factcheck_judge 模式；**子包 __init__ 注册解决 `rag.memory.nli_judge` 导入**——module-050 别名兼容机制实测：rag.memory 被旧路径别名覆盖为普通模块，新子模块必须 init 导入注册 sys.modules）；`_merge_duplicate` 分流（去重命中 → `_judge_conflict` 判 contradiction → 旧父块 superseded=true+updated_at 不删除 + 返回 None → save 正常新增不拼接共存 / entailment·neutral·NLI None·开关关 → 追加零回归；标记+新增分两步 fail-open）。config `memory_conflict_enabled`（PW_MEMORY_CONFLICT）默认 false；conftest autouse 钉住 false。**④ WP4 测试+文档**：`tests/test_memory_correction.py` 新 **27 项**（P0 留副本/superseded 过滤/幂等 + P1 三分类分流/矛盾全流程/NLI None 降级/开关关零回归 + nli_judge 封装 + 评测基线一致性，mock NLI 不加载真实模型）；**存量 test_memory.py 2 项升级断言按验收许可更新**（AC §2 升级不删短期副本，module-058 先例——`test_promotes_to_long_and_deletes_short`→`..._keeps_short`、`test_promotion_idempotent_skips_duplicate_copy`→`..._keeps_short_copy`，唯一存量改动已如实标注）；全量 pytest **824/0**（797 基线 + 27 新增）+ py_compile 11 文件 OK；changelog.md + ADR-0007 状态行（P0+P1 已实施）+ 三记忆文件 + CONTEXT.md 只增；未 git commit |
| module-061 | Reviewer | [REVIEW] **结论：✅ Pass（无 major，4 项 minor 非阻塞 → 进 Tester）**——对照 AC 8 维逐条核查。**独立验证**：① 全量 pytest 复跑 **824 passed / 0 failed（172.46s）**与 changelog 逐字一致；目标+存量 `test_memory_correction.py + test_memory.py` **103 passed**；② **eval_runs id=31 DB 实查**（eval_type=memory_conflict, commit=7c215814, scores={accuracy 0.6, precision 1.0, recall 0.5, f1 0.6667, tp 10, fp 0, fn 10, gate_passed false, dataset_size 30, evaluated 30, skipped 0}）与 changelog 逐位一致——未达门槛如实标注，开关默认关（不预设成功）；③ documents 表 superseded/updated_at 两列 DB 实查在（boolean NOT NULL default false / timestamp NOT NULL default CURRENT_TIMESTAMP，迁移已执行）；④ nli_loader 与 eval/compare_nli_models 加载路径逐项核对（HF_HUB_OFFLINE + dtype=float32 + id2label 从 config：0=entailment/1=neutral/2=contradiction）+ eval `--fixture --no-save` 冒烟跑通（30/30 无 skip）；⑤ py_compile 10 文件 OK。**通过项**：P0 升级留短期副本（delete 已移除 + superseded=False/updated_at 标记 + 幂等，存量 2 项改名断言按 AC §2 许可）；P1 三路径分流（矛盾→SUPERSEDED+None→正常新增 / 一致→追加 / NLI None/开关关→零回归）；`_is_superseded` `is True` MagicMock-safe；nli_judge 延迟加载+Lock+to_thread+20s 超时+失败 None；conftest autouse 钉住 false + config 默认 false；召回过滤覆盖 `_expand_to_parents`（父块级）+ `_evolve_recall`（ref 级）两路注入路径；记忆三件套 + ADR-0007 状态行 + CONTEXT.md 只增 ✅。**minor 4 项（非阻塞）**：① `_merge_duplicate` 对已 superseded 父块无守卫——开关开时新事实与 superseded 旧记忆去重命中且 NLI 判一致 → 内容追加进 superseded 父块从此不可召回（建议顶部 `if _is_superseded(parent): return None`）；② `_expand_to_parents` 仅父块级过滤，legacy 单文档（parent_id=None）superseded 不设防（当前写路径不可达，理论健壮性）；③ changelog "标记写库失败→按旧行为追加" 与实际不符（实际异常→None→按新增处理，均 fail-open）；④ `updated_at` ORM timezone=True vs DDL TIMESTAMP 无 tz（与既有 created_at 同款 cosmetic）。**有意取舍已核**：标记+新增分两步（非 plan 6.2 同事务）fail-open 理由充分、superseded 过滤在召回层非检索 SQL（共享检索器零风险）——均有声明。**遗留观察（非本模块）**：router.py/test_golden_intent.py/module-033 changelog 系 module-056 遗留未提交改动。报告：`specs/module-061-memory-correction/review-report.md` |
| module-061 | Tester | [TEST] **验收通过（0 阻塞，全部 AC 通过）**。① 全量 pytest 复跑 **824 passed / 0 failed（195s）**——797 基线 + 27 新增，与 changelog 一致；`test_memory_correction.py + test_memory.py` **103 passed**（新 27 + 存量 76）；存量唯一改动为 2 项升级断言按 AC §2 许可改名（test_memory.py line 1352/1383 实查）。② 冒烟+数字一致性：eval `--fixture --no-save` 30/30 无 skip；**eval_runs id=31 DB 实查与 changelog §2.2 逐位一致**（accuracy 0.6/P 1.0/R 0.5/F1 0.6667/tp 10/fp 0/fn 10/gate_passed false）；**真实模型生产封装冒烟**（nli_judge.predict 真实加载 557MB）：矛盾对→contradiction、一致对→entailment、空输入→None，降级契约成立。③ 实现抽查：P0 `_promote_memory` 无 delete 残留（grep 0）+ 长期 superseded=False/updated_at=now + 幂等；superseded 过滤 `_expand_to_parents`(L1036)+`_evolve_recall`(L706/720) `is True` MagicMock-safe；P1 `_merge_duplicate` 三路径（矛盾→SUPERSEDED+None→正常新增 / 一致→追加 / None·开关关→零回归）+ `_judge_conflict` 异常→None；nli_judge 延迟加载+Lock+to_thread+20s 超时+失败 None；config 默认 false + conftest autouse 钉住 false；documents 表两列 DB 实查在；ADR-0007 状态行 + CONTEXT.md 只增（记忆纠错领域节 L199-203）实读。④ 记忆硬约束：project-context module-061 行 + 头部日期 2026-08-13、activity Dev/Rev 行在、file-index 6 条新文件行在——缺项 0。Reviewer 4 minor 复核均非阻塞（开关默认关 MINOR-1 不可达）。**验收通过**。报告：`specs/module-061-memory-correction/test-report.md` |

### 2026-08-13（module-062 记忆进化 2：P2 类型化衰减 + P3 冷记忆降权 + WP4 矛盾检测启用）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-062 | Developer | [CODE] 记忆进化 2（ADR-0007 P2/P3，specs/module-062-memory-evolution2/，开工前已读 project-context.md 全文）。**① WP1 类型判断双方案**：`eval/build_memory_type_dataset.py` 人造 120 条（preference 40/fact 40/event 40，与评测集零重叠校验强制）+ `scripts/train_memory_type_clf.py`（bge-m3+LR → `models/memory_type_clf.joblib`，test split Accuracy **1.0000**）+ `eval/memory_type_dataset.py` 30 条同集对比：**clf 1.0000 / LLM 1.0000 双达标同分（eval_runs id=32/33，真实 deepseek 跑 LLM 侧）→ 取 clf**（`memory_type_mode="clf"`，零成本/确定性/离线，LLM type 作兜底）；`_EXTRACT_PROMPT` 加 type few-shot（`{"content","importance","type"}`，缺失/非法→默认 fact，向后兼容）。**② WP2 类型化衰减**：documents.type 列（`MEMORY_TYPE_COLUMNS_DDL` init_db 幂等 ALTER + `scripts/migrate_module062.py` 本地先决**已执行**，DB 两列实查在）+ `_evolve_recall` 按 type 半衰期（`_type_half_life`：**preference 30 天慢 / event 1 天快 / 其余=memory_short_half_life 3 天存量零回归**）+ `PW_MEMORY_TYPE_DECAY` 默认 true。**③ WP3 冷记忆降权**：documents.last_recalled_at 列（同批 ALTER）+ 长期层 `_apply_cold_decay`（按 last_recalled_at or created_at 距今天数加权：<30 天 ×1.0 → 平滑渐降至 ×0.3 下限，**不删除可回溯** + 召回命中 fire-and-forget 刷新升温，顺序=检索→降权→动态 K 截断）+ `PW_MEMORY_COLD_DECAY` 默认 true；短期层不降权（已有衰减，声明）。**④ WP4 矛盾检测对比启用**：`eval/build_memory_conflict_train.py` 自建 **142 条**（contradiction 82/non_conflict 60，与评测集零重叠——初版发现 22 处复用评测集措辞已全部改写）+ `scripts/train_memory_conflict_clf.py`（新旧两条分别嵌入→拼接+差值+绝对差值 4096 维 + LR → `models/memory_conflict_clf.joblib`，test split **contradiction Precision 0.90/Recall 0.95**）+ `memory_conflict_dataset.py --judge clf/nli` 同 30 条对比：**clf Precision 0.9048/Recall 0.95（eval_runs id=34）vs mDeBERTa Precision 1.0000/Recall 0.5（id=35 复现 module-061）——按用户决策"Precision≥0.8 者启用、双达标取 Precision 高者、保守方向宁可漏检也不错标" → mDeBERTa(nli) 启用：`PW_MEMORY_CONFLICT=true` + `PW_MEMORY_CONFLICT_JUDGE=nli`**（覆盖 module-061 默认关；clf Recall 更高可 `PW_MEMORY_CONFLICT_JUDGE=clf` 一键切换，config 预置；Recall 提升入 backlog）。**⑤ 测试+文档**：`tests/test_memory_evolution2.py` 新 **70 项**（extract_facts type/分类器推理/resolve_memory_type 三模式/类型化半衰期/冷降权系数·下限·存量·开关·DB 失败·重排·刷新/recall 集成/save 写 type/_persist_memory 类型注入/裁判切换 clf·nli·回退/评测集指标纯函数/训练集零重叠/DDL 幂等/配置）；conftest autouse 钉住（memory_type_mode='none'/cold_decay off/judge='nli'/conflict off——对齐 056/058/060/061 模式）；**存量 test_memory_extractor.py 3 处精确结构断言按验收许可补 type 字段**（extract_facts 返回结构加 type 为增量，AC §6 明确旧调用方取 content/importance 不受影响）；全量 pytest **895/0**（825 基线 + 70 新增）+ 真实模型冒烟（clf 类型 喜欢咖啡→preference/明天去北京→event、nli 矛盾对→contradiction、clf 矛盾 矛盾对→contradiction + 一处中性对误判如实记录）；changelog.md + ADR-0007 状态行（P2+P3 已实施 + WP4 启用）+ 三记忆文件 + CONTEXT.md 只增；未 git commit |
| module-062 | Reviewer | [REVIEW] **结论：⚠️ Conditional（1 项 major → 回 Developer 修复后重审；6 项 minor 非阻塞）**——对照 AC 8 维逐条核查。**独立验证**：① 全量 pytest 复跑 **895 passed / 0 failed（172s）**与 changelog 一致（825 基线 + 70 新增，test_memory_evolution2.py collect 70 确认）；② **eval_runs DB 实查 id=32/33/34/35**（type clf 1.0000 / type llm 1.0000 / conflict clf P 0.9048 R 0.95 tp19 fp2 fn1 / conflict nli P 1.0000 R 0.5 tp10 fp0 fn10，git_commit=8e014907）与 changelog **逐位一致**；③ documents 表 type（varchar default 'fact'）/last_recalled_at（timestamp 可空）两列 DB 实查在 + models/memory_type_clf.joblib + memory_conflict_clf.joblib 在盘；④ 训练/评测字符串零重叠断言通过（类型 120 vs 30、矛盾 142 vs 30）；⑤ 记忆硬约束（project-context module-062 行+头部日期、activity Developer 行、file-index 9 条、ADR-0007 状态行、CONTEXT 只增）全在。**通过项**：WP1 双方案真实对比（clf/LLM 同集同 1.0 取 clf，memory_type_mode 注入 + 三模式降级）、WP2 类型化半衰期（preference 30/event 1/其余 3 零回归，`_memory_type_of` isinstance MagicMock-safe，公式结构不变）、WP4 矛盾对比启用（用户 Precision≥0.8 规则 → nli 启用，clf 回退 nli，fail-open 链完整）、降级链全 fail-open、诚实边界充分（同分布小集/中性误判/口径声明）。**MAJOR-1（阻塞）**：`_apply_cold_decay` naive/aware 时间差——`last_recalled_at` 列 DDL 为 TIMESTAMP 无 tz，`_refresh_last_recalled` 写 aware UTC 后 PG 落库读回 naive，`now(aware)-ref(naive)` 抛 TypeError 被 fail-open 吞成 ×1.0 → **被召回过一次的记忆后续冷降权恒不生效，WP3 生产功能不成立**（真实 DB 往返已实测复现；`_evolve_recall` memory.py:810-811 已有 `tzinfo is None → replace(timezone.utc)` 同款规范化，`_apply_cold_decay` 漏了）；单测全用 tz-aware mock 未覆盖 naive 形态。修复建议：`_cold_ref_time`/`_apply_cold_decay` 补 `ref.replace(tzinfo=timezone.utc)`（对齐既有模式）+ 可选 DDL 改 TIMESTAMPTZ + 补 naive 输入单测。**minor 6 项（非阻塞）**：① env 名与文档不一致（文档 `PW_MEMORY_TYPE_DECAY`/`PW_MEMORY_COLD_DECAY`/`PW_MEMORY_CONFLICT` vs 实际 `*_ENABLED`，实测短名无效——module-061 既有误记延续）；② `_promote_memory` 长期副本 type 丢失（默认 fact，当前长期层不按 type 衰减无行为影响）；③ `memory_conflict_dataset.py` gate_passed 仍是旧双门槛，启用 nli id=35 落库 gate_passed=false（口径不一致可复现性缺口）；④ memory_type_clf.load() 每次调用重载磁盘模型（模型极小可忽略）；⑤ preference 30 天半衰期 vs 30 天硬上限交互（差异化只在 0-30 天窗口，建议口径声明）；⑥ `_apply_cold_decay` identity 参数未使用。报告：`specs/module-062-memory-evolution2/review-report.md` |
| module-062 | Reviewer | [REVIEW] **结论：✅ Pass（第 2 轮复审，MAJOR-1 已修复 + 无新引入；6 项 minor 非阻塞保持 + 1 项新观察）**——上轮 conditional 的唯一阻塞项已核查通过。**独立验证**：① 全量 pytest 复跑 **897 passed / 0 failed（185s）**（895 基线 + 2 新增 naive 用例，test_memory_evolution2.py collect 72 确认，与 changelog §9 逐字一致）；② **MAJOR-1 修复落地**：`_cold_ref_time`（memory.py:156-157）返回前对 naive 补 `ref.replace(tzinfo=timezone.utc)`——对齐 `_evolve_recall`（memory.py:818-819）既有 module-046 同款规范化，单点源头修复；`_apply_cold_decay` 的 aware-aware 减法不再抛 TypeError，`except` 降级恢复为只兜真正异常的 fail-open；③ **修复测试锁定**：`TestColdDecay::test_naive_db_roundtrip_still_decays`（naive 90 天前 → 按 UTC 解释降权 0.36）+ `TestColdRefTime::test_naive_ref_normalized_to_utc`（naive 输入返回 tz-aware 且墙钟不变）——恰好补上上轮测试缺口；④ **DDL 取舍核实**：保持 TIMESTAMP 未改 TIMESTAMPTZ，changelog §9 如实声明（代码规范化已完整修复 + module-046 先例 + ALTER COLUMN TYPE 时区偏移风险），可接受；⑤ 修复为单点最小改动，其余 module-062 代码与上轮逐字一致无新引入。**通过项复核**：WP1 双方案 / WP2 类型化半衰期 / WP3 冷降权（真实 DB 往返语义成立）/ WP4 矛盾启用 / 降级链全 fail-open / 诚实边界（changelog §9 新增修复记录节）/ 存量零改动保持。**minor（非阻塞）**：6 项上轮保持（env 名与文档不一致 / _promote_memory 长期副本 type 丢失 / gate_passed 旧双门槛 / clf 每次重载模型 / preference 30 vs 硬上限 30 交互 / identity 参数未使用）+ 1 项新观察 MINOR-7（修复新增 2 测试后 project-context/file-index 测试计数 895/70 未同步为 897/72，changelog §9 声明无新文件行正确但既有行数字过时）。报告：`specs/module-062-memory-evolution2/review-report.md`（第 2 轮） |
| module-062 | Tester | [TEST] **验收通过（AC 全部通过，0 阻塞）**。① 全量 pytest 复跑 **897 passed / 0 failed（179.7s）**（825 基线 + 72 新增 test_memory_evolution2.py collect 确认，与 changelog §9 / Reviewer 复跑一致）；② 冒烟复跑与 changelog 数字一致性：`build_memory_type_dataset.py` 120 条（40/40/40）+ `build_memory_conflict_train.py` 142 条（contradiction 82/non_conflict 60）build 校验通过；`memory_type_dataset --fixture --no-save` Accuracy 1.0（管线演示）；**eval_runs DB 实查 id=32/33/34/35 与 changelog 逐位一致**（type clf/llm 双 1.0；conflict clf P 0.9048/R 0.95/tp19 fp2 fn1、nli P 1.0/R 0.5/tp10 fp0 fn10，git_commit=8e014907）；③ 实现抽查与 changelog 一致（双方案对比取 clf / memory_type_mode / 类型化半衰期 preference 30·event 1·其余 3 / 冷降权 ×0.3-1.0 不删除 + naive→UTC 规范化修复 / PW_MEMORY_CONFLICT=true + JUDGE=nli）；④ 真实模型冒烟：type clf 喜欢咖啡→preference/明天去北京→event/Java 开发→fact，conflict clf 矛盾对→contradiction + 中性对误判（与 changelog §5.3 如实记录一致）；⑤ 记忆硬核查：project-context module-062 行 + 头部日期 2026-08-13 ✓、activity Dev/Rev/Test 三行 ✓（本条为 Test）、file-index module-062 新文件行 9 条 + 目录行 ✓、ADR-0007 状态行 P2+P3+WP4 ✓、CONTEXT.md 只增（+8 行无删除）✓、存量 test_memory.py 零改动 + test_memory_extractor.py 仅 3 处按验收许可补 type 字段（git diff 实证）✓；⑥ 非阻塞观察（不阻塞合入）：test_conflict_enabled_default_off 注释"不预设成功"已过时（WP4 已达 Precision 门槛生产默认 memory_conflict_enabled=True，测试断言为 conftest 钉住的 hermetic 值 False 正确）；7 项 minor 保持 Reviewer 记录（env 名不一致/_promote_memory type 丢失/gate_passed 旧双门槛/clf 每次重载模型/preference 30 vs 硬上限 30/identity 参数未用/MINOR-7 计数漂移 895→897）。报告：`specs/module-062-memory-evolution2/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-14（module-063 多轮对话意图路由升级）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-063 | Developer | [CODE] 多轮对话意图路由升级（ADR-0015，specs/module-063-multi-turn-intent-routing/，开工前已读 project-context.md 全文）。**① WP-A 会话级路由**：`RouterAgent.classify(query, history=None, tool_history=None)`（history 取 `[-6:]` 最近 4-6 轮，**空/None history 行为与改动前逐字一致零回归**——897 基线存量测试零改动全绿）+ `_build_prompt` LLM few-shot `_MULTITURN_CONTEXT`（有 history 拼最近 4 轮上下文："省略句/指代句结合上下文、完整句按字面"防多轮闲聊误归因）+ L4 `IntentClassifier.predict_proba(query, prev_user_query=None)`（list 拼接最近一轮 user query 向量 2048 维，训练同构参考 memory_conflict_clf；**诚实边界：当前落盘 intent_clf.joblib 单 query 1024 维训练 → config `intent_classifier_multi_turn`（PW_）默认 false，置 true 需先重训配对样本，未重训置 true 维度不匹配→router 捕获回退 LLM fail-open 零回归**）+ **L4 路径补 L2 确定性信号**（golden_multi_turn 真实测量暴露：L4 单句无历史把"怎么解决呢"误判 casual 0.65，FTS 术语"解决"命中本应拉回却被 L4 短路 → 对 intent≠knowledge 同走 `_deterministic_confirm` 修正 knowledge，与 LLM 路径同款零 LLM 安全网；意图保持 11/12→**12/12**；存量 L4 测试全 knowledge 高概率零改动）。**② WP-B 短句意图继承（规则层零 LLM）**：`_strip_particles`（`_PARTICLE_WORDS`=哦/呢/呀/啦/请问/那个/嘛/吧 8 个）+ 去语气词后长度<6 且 `_deterministic_confirm` 无新特征 → `_short_inherit` 继承上一轮 intent（无状态从 history 推演 + `_classify_prev` 递归链式继承深度上限 `_INHERIT_MAX_DEPTH=3`）；有特征（FTS/图谱/规则）正常路由防话题漂移（"今天天气" rule_veto 挡住）。**③ WP-C 分诊式改写喂路由**：engine.chat 把 module-049 分诊式改写**提前到路由前**——改写成功且保真通过（used_rewrite）时 classify 改写后 query + 检索，失败/回退原 query（保守零回归）；`mode=="precise"` 且非闲聊/实时规则词（`router_agent._rule_hits` 守卫）短路 knowledge（省一次路由调用）；**非流式完整实现、流式 `_retrieve` 内部改写仍只喂检索**（流式路由用原 query+history，WP-A 上下文+WP-B 继承覆盖，如实声明）。**两条路径接 history（纪律 §八.2）**：engine.chat :237 + main.py chat_stream Step1 + rag/graph/graph.py classify_intent 三处同步。**④ WP-D 工具历史信号 + 评测**：`tool_history` 含 search_knowledge/generate_answer（`_KB_TOOL_NAMES`）且短 query → 强制 knowledge（轨迹不可得 tool_history=None 跳过）；`eval/golden/golden_multi_turn.py` 新建 12 条多轮追问对 + 三指标（自包含清晰度/意图保持/检索提升）+ --fixture/--no-save + eval_type='multi_turn' 落库。**真实评测（deepseek+DB）**：意图保持 **12/12**、单句对照 11/12（"为什么"无特征单句漏检正是本模块解决场景）、检索提升 **+0.4363**（raw_overlap 0.2364→rewritten_overlap 0.6727，prev 检索作锚点代理口径）、自包含 11/12（"为什么"改写单次返回 None 非确定性）。**⑤ WP-E 回归 + 真实 E2E**：全量 pytest **951/0**（897 基线 + 54 新增：test_multi_turn_routing.py 35 + test_golden_multi_turn.py 19，存量零改动）；真实冒烟（DB+Redis+bge-m3+HHEM+deepseek）：路由 4 轮（知识库→knowledge L4 / "为什么"→knowledge 短句继承 / 天气→realtime / 哈哈→casual）PASS + 全链路 engine.chat 两轮（round2 "为什么" message=ok 5 sources 走检索链路）PASS；changelog.md（WP 逐项 + 三指标数字 + 口径声明 + 诚实边界）+ ADR-0015 状态行 ✅ + 三记忆文件 + CONTEXT.md 只增；未 git commit |
| module-063 | Reviewer | [REVIEW] **结论：✅ Pass（无 major → 进 Tester；6 项 minor 非阻塞，其中 1 项关键 minor 需团队自觉接受）**——对照 AC 8 维逐条核查。**独立验证**：① 全量 pytest 复跑 **951 passed / 0 failed（160.9s）**与 changelog 一致（897 基线 + 54 新增，两新文件 `--co` 收集 54 确认）；② **存量测试零改动实证**：`git diff --stat -- ai_service/tests/` 为空，仅新增 2 未跟踪文件；③ **eval_runs DB 实查无 multi_turn 行**（最新 id=39 sufficiency 8-13）——真实评测 `--no-save` 未落库与 changelog §七.4 一致（数字为单次 LLM 快照不可 DB 复现，能力落库已就绪）；④ 记忆硬约束全在（project-context module-063 行+头部日期 8-14 / activity Developer 行（本条为 Reviewer）/ file-index 4 行+目录 / ADR-0015 状态行 ✅ / CONTEXT 只增无删 / changelog 注明开工前已读 project-context）；⑤ 配置默认核验（query_rewrite_enabled=False / intent_classifier_multi_turn=False / intent_classifier_enabled=True）——WP-C 改写喂路由与 L4 拼接默认关零回归。**通过项**：纪律六条全过（存量零改动 / 两路径接 history 三处 engine+main+graph 实证 / 三值不动 / 保真回退保留 / history[-6:]+LLM 4 轮截断 / 语气词 8 个）；WP-B 条件链逐条验证（"为什么"停用词无特征可继承 / "那图谱"图谱实体命中正常路由 / "今天天气"+"哈哈" rule_veto 不继承）；WP-C precise 短路 rule_hits 守卫（"你好"不短路有单测）；L4 拼接 2048 维维度异常 fail-open（except :341 回退 LLM）；递归深度上限 3 有界。**minor 6 项（非阻塞）**：①（关键）L4 路径补 L2 确定性信号改变**单轮** L4 行为（intent≠knowledge 且 FTS/图谱命中→knowledge），技术性偏离 AC §1"空/None history 逐字一致"——系本模块 eval 实测暴露的正确性修复 + 单测锁定 + 与 LLM 路径对齐，建议保留但团队需自觉接受；② WP-C `prepare` 提前到路由前 → query_rewrite_enabled=True 时 realtime/casual query 白跑改写管线（默认关零影响，建议加廉价预筛）；③ 真实评测未落库不可复现（建议复跑落库或标注一次性快照）；④ AC §1"L4 特征拼接训练同构"仅推理侧实现、训练侧未做、默认关 fail-open（能力就绪诚实声明）；⑤ task-brief §九.4 交付物"08 文档意图路由节加多轮段"未更新（docs/简历/08-项目经历-逐词深挖.md 无改动，面试口径仅落 changelog §八）；⑥ `_last_user_turn` 对 content=None 得 "None" 真值边角。报告：`specs/module-063-multi-turn-intent-routing/review-report.md` |
| module-063 | Tester | [TEST] **验收通过（AC 全部通过，0 阻塞；2 项部分通过与 Reviewer minor 对应非阻塞）**。① 全量 pytest **951 passed / 0 failed（175.28s）**（897 基线 + 54 新增，两新文件 `--co` 收集 54 = test_multi_turn_routing 35 + test_golden_multi_turn 19，与 changelog/Reviewer 一致）；② **存量测试零改动**：`git diff --stat -- ai_service/tests/` 为空仅新增 2 未跟踪文件；③ **冒烟复跑独立复现**——fixture 模式 12 pairs/Skipped 0/自包含 1.0/意图 0.9167 管线通过；**真实模式（deepseek+DB+本地 bge-m3/L4）独立复跑**：意图保持 **12/12**、单句对照 **11/12**、自包含 **11/12**（三指标与 changelog 逐字一致）；检索提升 **+0.3818**（raw_overlap 0.2364 与 changelog 逐字一致，rewritten 0.6182 vs 0.6727 属 LLM 改写非确定性区间）——关键 AC 指标独立验证不依赖采信；"为什么" raw=casual→routed=knowledge 短句继承真实生效；eval_runs 无 multi_turn 行（--no-save 未落库，数字为单次快照如实声明）；④ **实现抽查全过**：classify(query, history=None) 默认空历史零回归（单测 None/[]/缺省三路径完全一致）/ `_short_inherit` 去语气词<6 无特征继承 + rule_veto 防话题漂移 / WP-C 改写前移喂路由保真回退保留（engine.py:247-268）+ precise 短路 rule_hits 守卫 / **两条路径接 history 三处实证**（engine.chat :267/:270 + main.py chat_stream :457 + graph.py classify_intent :89）+ 流式改写不喂路由如实声明 / 意图三值不动 / L4 拼接 2048 维 fail-open / 工具信号轨迹不可得跳过 / config 默认（query_rewrite_enabled=False + intent_classifier_multi_turn=False 零回归契约 + sufficiency 0.55 红线未动）；⑤ **记忆硬核查全在**（project-context module-063 行+头部日期 8-14 / activity Dev+Rev 行（本条为 Test）/ file-index 4 行+目录 / ADR-0015 ✅ / CONTEXT 只增 +13 行零删）；⑥ 非阻塞观察随 Reviewer：MINOR-1（L4+L2 对齐偏离"逐字一致"——正确性修复建议团队自觉接受）/ MINOR-3（真实评测未落库，Tester 已独立复跑验证）/ MINOR-5（08 文档面试口径未更新，建议主会话补）/ MINOR-2/4/6。报告：`specs/module-063-multi-turn-intent-routing/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-14（module-064 多格式文档解析 + 数据清洗 + 去重）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-064 | Developer | [CODE] 多格式文档解析 + 清洗 + 去重（ADR-0014，specs/module-064-document-parsing-cleaning/，开工前已读 project-context.md 全文）。**① WP1 AnyDoc 解析层**：`pip install anydoc` 无此包 → 改装 **firecrawl-anydoc==0.1.8**（Firecrawl 开源 AnyDoc 引擎 PyPI 绑定，成功）+ python-docx/openpyxl 轻量回退；`rag/retrieval/document_parser.py` 统一入口 `parse_document(bytes, filename)`（格式识别**读字节魔数** `anydoc.format_from_bytes`，md/txt/csv 扩展名兜底；AnyDoc 主解析 + PDF PyMuPDF 回退（存量 main.py:911 提取 `_parse_pdf_pymupdf`）+ docx/xlsx/csv 轻量回退 + pptx/epub 无回退明确报错；错误变体映射中文提示 Unsupported/Malformed/Encrypted）；main.py upload 端点重写（8 格式白名单 + 中文错误码 + 标题/来源推导）；前端 UploadPanel+KnowledgePage `accept` 开放 8 格式 + 新增 `ragService.uploadDocumentFile`（FormData 传原始字节，旧 JSON uploadDocument 保留）。**② WP2 五步清洗（白名单哲学）**：`document_cleaner.clean` 把 MD 切 code/math/table/body 四区域按类型作用——代码块只去控制符+行尾（不剥缩进）、表格结构保留、正文内联（行内代码/URL/`$..$`）占位符 **⟦N⟧ 先保护再清洗**（防 NFKC 改写代码；初版 \x00 被控制符过滤抹掉的坑已修）；五步=格式清理（页码/控制符/NFKC 标点）/冗余过滤（纯符号噪声段）/结构恢复 ⭐（**合并 PDF 断行段落**：空行分段+块级信号不合并+CJK/连字符连接规则）/语义修复（OCR_TYPO_MAP 留空待补）/分块准备（`#`→`##` 对齐 chunker + 标题前补空行）。**③ WP3 无损归一化**：`normalize` NFKC/去零宽/统一空白（保留行首缩进防坏代码块）/表格保持 MD/超长截断兜底（chunker 已限 4000/300 防嵌入截断）。**④ WP4 PDF 图片三层（全默认关）**：`image_pipeline` PW_IMAGE_OCR / PW_IMAGE_CAPTION / PW_PDF_ENGINE=mineru 分层路由 + 占位符替换 + 图片价值过滤接口（image_value_filter 面积+OCR+VLM 评分）+ fail-open（模型全未装三层降级关）；扫描版无文本层如实附"图片未解析"提示。**⑤ WP5 原件留存**：落盘 uploads/{sha256[:16]}_{净文件名} + documents 新列 original_path（init_db 幂等 ALTER + migrate_module064.py 本地先决已执行 4 列+2 索引）。**⑥ WP6 去重三级**：L1 `doc_content_hash`（文档级 sha256 完全相同直接丢弃）；L2 文档级 embedding 余弦≥0.95（bge-m3 绝对余弦口径，复用 module-035 同款：**根父块 embedding 列复用存文档向量**——父块不参与向量检索零新增 Vector 列；`_expand_to_parents` 加 is_canonical.is_(True) 检索抑制只出 canonical）；L3 SimHash-LSH 接口预留（几千+ 才启用）；Boilerplate 先剥离（strip_boilerplate 页脚/免责声明正则）。**⑦ WP7 管线+测试**：`document_ingest.py` 编排 parse→图片→clean→normalize→L1 去重→原件落盘→L2 标簇→add_document（各层失败 fail-open 不阻断入库）；新增测试 **66 项**（test_document_parser 21 + test_document_cleaner 26 + test_document_dedup 15 + test_document_ingest 4，全 mock AnyDoc/embedding/DB）；conftest 钉住 doc_dedup_semantic_enabled=False；**真实 DB 冒烟 md/pdf/docx/xlsx 全管线入库通过**（chunks=2/original_path 落库/is_canonical=True，测试文档已清理不污染 124 篇知识库；冒烟真实暴露并修复 2 bug：compute_doc_embedding 未 await 协程落库错 + 占位符 \x00 被控制符过滤抹掉）；**全量 pytest 1017/0（951 基线 + 66 新增，存量零改动）**；前端 build PASS + vitest **56/56**；changelog（WP 逐项 + 66 测试 + 真实 DB 冒烟 + 诚实边界）+ **ADR-0014 补建并标 ✅ 已实施**（原缺失，按 task-brief §二 六项决策补）+ 三记忆文件 + CONTEXT.md 只增；未 git commit |
| module-064 | Reviewer | [REVIEW] **结论：⚠️ Conditional（2 项 major → 回 Developer 修复后重审；5 项 minor 非阻塞）**——对照 AC 8 维逐条核查。**独立验证**：① 全量 pytest 复跑 **1017 passed / 0 failed（172.10s）**与 changelog 一致（951 基线 + 66 新增，逐文件核对 21+26+15+4=66）；② 存量测试零改动（git diff main -- ai_service/tests/ 仅 conftest 追加 1 个 autouse fixture）；③ 记忆硬约束全在（project-context module-064 行+头部日期 8-14 / activity Developer 行（本条为 Reviewer）/ file-index 10 行 / ADR-0014 ✅ / CONTEXT 只增 15 行零删 / changelog 注明已读）。**MAJOR-1：L2 语义去重 Boilerplate 剥离不对称**——document_ingest.py:173 入库侧存储文档向量直接 embed(normalized)（未剥离），document_dedup.py:127 查询侧先 strip_boilerplate 再 embed——"Boilerplate 先剥离"（AC §6.4/ADR 决策 6 坑①）只对查询侧生效，候选向量被套话污染，可能误判语义重复（检索抑制误隐藏）或漏判，随文档积累放大；建议步骤 8 同口径剥离或复用 find_semantic_duplicate 剥离后向量。**MAJOR-2：WP4 image_pipeline 零直接单测 + changelog §五"单测 ✓"声明不实**——AC §4 四项（三层默认关/图片价值过滤/含图不报错+扫描版提示/开关+占位符替换单测）全未覆盖，现有测试仅 test_document_ingest 打桩 process_pdf_images 恒返回 md；建议新增 test_document_image.py。**minor 5 项**：① 测试数口径 64/66 不一致（ADR-0014 状态行/验收节 + project-context ADR 索引 + CONTEXT.md 写 64，实际 66）；② 真实 DB 冒烟脚本 .ua/smoke_*.py 未留存无法独立复现（建议 changelog 标注一次性本地产物）；③ normalize 全局 NFKC 对代码块/行内代码生效，与 clean 占位符保护部分抵消（ADR 允许全局 NFKC 不违反，建议文档声明）；④ add_document 内部 title/content_hash 命中被标记 dup_kind="exact" 语义名不符（无功能影响）；⑤ 同请求双次全文档 embed（步骤 7+8，修 MAJOR-1 时可复用）。报告：`specs/module-064-document-parsing-cleaning/review-report.md`。**模块标记 conditional（待修复后重审）** |
| module-064 | Reviewer（第 2 轮） | [REVIEW] **结论：✅ Pass（2 项 major 已全部修复，无新引入 major → 进 Tester）**——第 2 轮复审。**独立验证**：① 全量 pytest 复跑 **1036 passed / 0 failed（169.97s）**与 changelog 一致（951 基线 + 85 新增，逐文件核对 21+26+15+5+18=85；上轮 66 + 修复轮 19 = 85 自洽）；② 存量测试零改动不变。**MAJOR-1 修复核验**：document_ingest.py:176-178 改 `compute_doc_embedding(strip_boilerplate(normalized))` 与查询侧 find_semantic_duplicate 完全同口径（同输入同开关，False 两侧一致）；采用方案 A（入库侧剥离），方案 B（改 find_semantic_duplicate 返回契约消除双次 embed）按精准修改原则不做，MINOR-5 保留为已声明性能 minor；新测试 test_ingest_doc_embedding_strips_boilerplate 走真实 strip_boilerplate 锁定。**MAJOR-2 修复核验**：test_document_image.py **18 项**全 hermetic（extract_image_refs 5 + image_value_filter 5 + 三层默认关 2 + L1 fail-open 1 + L2 fail-open 1 + L3 MinerU 降级 1 + 扫描版 3），AC §4 四项全覆盖，断言与 image_pipeline.py 实现逐条核对一致；changelog §一/§五/§十三 已如实修正（初版"单测 ✓"声明过早）。**新 minor 2 项（非阻塞）**：① 新发现——find_semantic_duplicate 候选查询未过滤 is_canonical，而非 canonical 副本仍存 doc_embedding（document_ingest 步骤 8）→ 后续文档可与副本比对被过度折叠进簇（检索抑制误隐藏）；建议候选查询加 `Document.is_canonical.is_(True)` 或非 canonical 不存 doc_embedding；② 上轮 minor-1 数字滞后未清（CONTEXT/project-context ADR 索引写 64、ADR 状态行 66、实际 85）。报告：`specs/module-064-document-parsing-cleaning/review-report.md`。**模块标记 ✅ pass（待 Tester）** |
| module-064 | Tester | [TEST] **验收通过（AC 全部通过，0 阻塞）**。① 全量 pytest **1036 passed / 0 failed（177.21s）**（951 基线 + 85 新增，逐文件核对 21+26+15+5+18=85，与 changelog/Reviewer 第 2 轮一致）；② 存量测试零改动（`git diff main -- ai_service/tests/` 仅 conftest 追加 1 个 autouse fixture，5 新测试文件未跟踪）；③ 冒烟复跑：独立多格式解析冒烟（真实 AnyDoc 自造 md/pdf/docx/xlsx 四文件 parse_document 文本非空 + 不支持 .exe 明确中文提示，与 changelog §九 一致）；前端 vitest **56/56**；DB 迁移列实核（migrate_module064.py 幂等复跑输出 4 列 + 2 索引在）；④ 实现抽查 6 项全过（白名单 code/math/table/body 四区域 + 内联 ⟦N⟧ 占位符 / 无损归一化不改语义 / 三层默认关 fail-open / 原件留存 original_path 落盘 / 去重三级 canonical `_expand_to_parents` is_canonical 检索抑制 / 存量 PyMuPDF 回退）；⑤ 记忆硬核查缺项 0（project-context module-064 行 + 头部日期 8-14 / activity Dev+Rev 行（本条为 Test）/ file-index 13 行 / CONTEXT 只增）；⑥ 非阻塞观察随 Reviewer：数字口径漂移 66→85（project-context/ADR 状态行/CONTEXT 滞后）+ 非 canonical 作语义去重参考 + MINOR-5 同请求双次 embed + 冒烟脚本未留存。报告：`specs/module-064-document-parsing-cleaning/test-report.md`。**模块标记 ✅ 完成** |

### 2026-08-15（module-065 入库性能验证 + E2E 复测 + 公开基准 + minor 清理）

| 模块 | 角色 | 摘要 |
|------|------|------|
| module-065 | Developer | [CODE+数据] 能本地闭环的合并批（specs/module-065-ingest-perf-and-e2e/，开工前已读 project-context.md 全文）。**WP1 写入侧嵌入证伪固化**：新建 `eval/benchmarks/benchmark_embed_write.py`（可复跑，同探路口径）实测——循环 vs List 批量 10/50/200 条倍率 **1.0x/0.9x/1.0x 无加速**、多进程 2/4 **0.4x/0.3x 负优化**（173-211ms/块），**生产代码零改动**（无可行优化不做投机改动）；METRICS 待办⑥ 更新为证伪结论。**WP2 E2E 复测**：重启 uvicorn 8001（HHEM + reranker int8 量化预热日志确认）→ 探路脚本同口径 6 次"什么是G1 GC"：**TTFT 25.3→15.4s（-39%）/ 完整 28.1→18.9s（-33%）**（落预估 15-19s）；request_logs 分段 rerank **8.5-13.5→1.9-3.3s（平均 ~2.4s，P0-P3 ~4-5x）**；真实文档生产链路命中验证（"What are the three paradigms of RAG?" 完整 chat → 5 sources 含 3 个 rag_survey.pdf 块，RRF→粗筛→rerank 后仍命中）；6 次探针 verify 全异步后台（module-060 行为保持）。**WP3 公开基准首跑**：数据源探测 huggingface.co 502 不可达 / hf-mirror.com 可达（qrels 一次 502 瞬时抖动重试成功）；新建 `eval/benchmarks/benchmark_public_retrieval.py`（hf-mirror 直链下载缓存 eval/datasets/public/ gitignored + bge-m3 余弦暴力检索 + Hit@5/MRR/nDCG@10 trec 口径 + eval_runs 落库）：**C-MTEB EcomRetrieval 中文抽样 3,000/100,902 有效 26 查询（跳过 974）nDCG@10 0.9231**（id=44；qrels 每查询仅 1 条相关，97.4% 查询答案不在 3% 抽样内，26 条基数薄不可与官方比）+ **BEIR nfcorpus 英文抽样 1,200/3,633 有效 292 查询（跳过 31）nDCG@10 0.4853**（Hit@5 0.5685/MRR 0.4630，落在官方全量 bge-m3 参考值 ~0.45-0.5 区间，id=45）；**三轮口径修正（Review MAJOR-1 + Tester 修复轮）**：id=40/41 点积口径（嵌入未 L2 归一化，Reviewer 实测单条 norm=26.68）→ 逐行归一化修复 id=42/43（余弦但全查询计入，相关文档不在抽样内的查询按 0 分稀释）→ 真跳过语义 id=44/45（有效查询=相关文档∩抽样非空，跳过计数与 Tester 独立复算一致 26/31）；中间口径记录均已落库标注取代；全量 corpus 本机 CPU 单篇 1.7-2.2s（~1,800 字符）→ 全量 ~2h 不现实，抽样代理口径如实声明，`--corpus-sample 0` 全量入口留 GPU/集群。**WP4 minor 清理**：① `find_semantic_duplicate` 候选查询过滤 `is_canonical.is_(True)` + 单测（编译捕获断言 `is_canonical IS true`，test_document_dedup 15→16，存量零改动）；② 三处测试数口径 66/64 → **85/1036** 统一修正（project-context module-064 行 + ADR-0014 状态行 + CONTEXT.md 段内修订不删行）。**全量 pytest 1037/0（167.24s）= 1036 基线 + 1 新增**。changelog + 记忆三件套完成；服务已停回初始状态（E2E 测量完成后被停，无损失）；未 git commit |
| module-065 | Reviewer | [REVIEW] **结论：⚠️ Conditional（1 项 major → 回 Developer 修复后重审；4 项 minor 非阻塞）**——对照 AC 8 维逐条核查，独立验证不采信 changelog。**独立验证**：① 全量 pytest 复跑 `pytest tests/ -q` **1037 passed / 0 failed（135.35s，42 warnings）**与 changelog 167.24s 一致（1036 基线 + 1 新增，存量零改动实证）；② test_document_dedup.py **16/16**；③ **eval_runs 直查 DB**：id=40 ecom-zh（0.0240/0.0233/0.0235, n=1000）+ id=41 nfcorpus（0.4923/0.4152/0.4329, n=323）真实落库逐字一致；④ **WP2 证据链**：request_logs id 20-25 六次探针 rerank 1.9-3.3s（均 2403ms≈2.4s）/图谱 0.9-1.4s/反思 1.4-2.6s/生成 4.5-14s/HyDE 仅首次，与 changelog 分段逐项吻合 + verify_results id 9-13 一一对应旁证 + rag_survey 块 id=8282/8284/8304 存在 + 复测期 port 8001 监听；⑤ WP1 脚本独立复跑 --quick 可运行（CPU 争用下绝对值放大 3-4x，方向一致）；⑥ 记忆硬约束全在（project-context 头部 8-15 + module-065 行 + v0.65.0 + activity Dev 行（本条为 Reviewer）+ file-index 6 行 + CONTEXT 只增零删）。**MAJOR-1：benchmark_public_retrieval.py 标称"余弦"实为点积**——bge-m3 Q8 llama.cpp 原始输出未 L2 归一化（实测单条 L2 norm=26.68，embeddings.py 约定显式 _normalize），脚本 L223 docs_vec @ q_vec 直接点积，向量模长∝文本长度参与排名；eval_runs id=40/41 落库标签"bge-m3 Q8 本地 + 余弦暴力检索"错误，METRICS/changelog 沿用；建议：脚本归一化（对齐 embeddings.py 约定）+ 重跑两数据集更新数字 + 文档同步（或改标"点积"）。**minor 4 项**：① METRICS 待办⑤ 残留"nfcorpus 抽样待跑"与 id=41 矛盾；② WP1 脚本结论行写死与实测倍率解耦（我复跑在争用下批量 1.6-2.0x 快于循环）+ ms/块口径不一致（110-160 vs 实测 173-211）；③ scripts/test_models.py 被 pytest 收集致裸 pytest -q 1 collection error（**既有 module-050 遗留非本模块回归**，建议 conftest collect_ignore）；④ changelog WP1 两表循环 200 条数值不一致（42.29s vs 34.60s，独立测量机器态波动，建议注明）。报告：specs/module-065-ingest-perf-and-e2e/review-report.md。**模块标记 conditional（待修复后重审）** |
| module-065 | Reviewer（第 2 轮） | [REVIEW] **结论：✅ Pass（MAJOR-1 已修复，无新引入 major → 进 Tester）**——第 2 轮复审，逐项核验 changelog §九。**MAJOR-1 修复核验**：脚本 L217-233 corpus 矩阵逐行 L2 归一化（norm<1e-9 置 1）+ 查询向量归一化（对齐 embeddings.py _normalize），落库标签改"L2 归一化后余弦暴力检索"；**直查 DB 重跑结果**：id=42 ecom-zh 0.0240/0.0240/0.0240（旧 0.0240/0.0233/0.0235 基本不变——电商文档长度均匀）+ id=43 nfcorpus **0.5139/0.4185/0.4387**（旧 0.4923/0.4152/0.4329，Hit@5 +0.0216 / nDCG@10 +0.0058——长文模长偏置确实存在，修复后更贴近官方参考值）；旧 id=40/41 已落库 correction 标注"由新 eval_runs 取代"；changelog/METRICS 数字与 DB 逐字一致，delta 数学自洽。**minor 4 项全部核验**：① METRICS 待办⑤ 残留"待跑"已清改引新 id；② WP1 脚本结论行改本次实测数据驱动 + ms/块 口径统一 110-210（探路短文本 110-160 / 本脚本 141 字符实测 173-211）+ 我争用态复跑 1.6-2.0x 如实记录为瞬时现象，结论修正"无稳定可复现的批量加速"（更严谨）；③ scripts/test_models.py 既有遗留不动（认可）；④ WP1 两表 42.29 vs 34.60s 已注明独立测量。全量 pytest 1037/0（第 1 轮独立复跑）+ WP1/WP2/WP4 证据链不变。报告：specs/module-065-ingest-perf-and-e2e/review-report.md §七。**模块标记 ✅ pass（待 Tester）** |
| module-065 | Reviewer（第 3 轮） | [REVIEW] **结论：✅ Pass（最终）**——Tester 发现 WP3 抽样口径文档/实现不符，Reviewer 从本地 qrels 数据独立复核**全部证实**后 Developer 第三轮修复（changelog §十），逐项核验。**独立复核数据**：① nfcorpus qrels 均值 38.2 条相关/查询（中位 16），33% 抽样下 **31/323（9.6%）查询相关文档全落抽样外**——"0 跳过"是脚本设计使然（qids 按 qrels 预过滤 relevant 恒非空），"所有测试查询的相关文档均落在抽样内"推断不成立；② ecom qrels 每查询**恰好 1 条**相关（mean=1.0 std=0），3% 抽样下 **97.4%**（仅 26/1000）查询相关文档在抽样内——"~5 篇/查询 + ~86%"估算有误。**修复核验**：脚本实现真跳过（qids_effective = 相关文档 ∩ 抽样 corpus 非空，n_skip 计数打印，跳过查询省嵌入成本）——最终代码正确；文档不再写估算百分比改引实打印计数；注释同步。**重跑最终数字（DB 直查）**：id=44 ecom **有效 26 条 0.9231×3**（解读修正："答案在 corpus 内时找回能力高，但基数薄不可与官方比"）+ id=45 nfcorpus **有效 292 条 0.5685/0.4630/0.4853**（nDCG@10 落入官方参考区间 ~0.45-0.5）；口径演进三段标注（40/41 点积→42/43 余弦全计入→44/45 余弦+真跳过，各 id 不可直接比，旧记录已落库标注）；changelog §三/METRICS 公开基准段+待办⑤/project-context 行全部同步最终口径。测试影响：脚本不被测试导入（--co 1037 收集不变）。残留 1 措辞微瑕非阻塞（METRICS L224 旧 blockquote"性能大幅下降"未随 ecom 0.92 更新，可顺手润色）。报告：specs/module-065-ingest-perf-and-e2e/review-report.md §八。**模块标记 ✅ pass（待 Tester 验收）** |
| module-065 | Tester | [TEST] **验收通过（20/20 项通过，0 阻塞）**——Review ✅ Pass 后独立验收（开工前已读 project-context 全文）。**全量 pytest 1037/0（152.78s，43 warnings）= 1036 基线 + 1 新增，存量零改动**（开工前基线 1036/0 独立复跑核对一致）。**WP1 冒烟复跑**：benchmark_embed_write --quick 与 changelog 量级一致（批量 1.0x/0.9x、MP 0.4x、166-194ms/块，结论行数据驱动）。**WP2 独立 DB 核对**：request_logs id=20-25 六次探针 rerank 2015-3266ms（均 2404ms≈2.4s，与 changelog "1.9-3.3s 平均 ~2.4s"逐字吻合）+ 生成 4547-14032ms（4.5-14s）+ 优化前后对照（id=14/15 旧 13.0-13.5s）+ documents id=8282/8284/8304 存在（source=realdoc_test:2026-08-14）——TTFT 25.3→15.4s / 完整 28.1→18.9s 证据链成立。**WP3 Tester 发现 + 验证闭环**：固定种子复算 1,200/3,633 抽样下 31/323 nfcorpus 查询相关文档全落抽样外（"0 跳过"系 qrels 预过滤设计使然）、ecom 3,000/100,902 抽样仅 26/1000 可评估（qrels 每查询 1 条相关，落抽样外 97.4% 非 86%）→ 通知 Developer 修复（changelog §十 真跳过 + 口径修正）→ **重跑实跑有效查询数与 Tester 复算精确一致（26/292）**，id=44/45 数字与 changelog/METRICS 逐字一致；另 `--corpus-sample 1200 --no-save` 独立复跑旧语义与 id=41 逐位一致（0.4923/0.4152/0.4329）佐证可复现。**WP4 抽查**：is_canonical 过滤 + SQL 编译捕获单测 16/16；minor-2 三处 85/1036 修正（CONTEXT.md 段内修订零删行）。**记忆硬核查**：project-context 头部 2026-08-15 + module-065 行 + v0.65.0 + activity Dev/Review 三行 + file-index 5 行全在。报告：specs/module-065-ingest-perf-and-e2e/test-report.md。**模块标记 ✅ 完成（待 Developer 提交推送）** |

### 2026-08-17（module-066 Agent 级评估体系规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-066 | Planner | [PLAN] Agent 级评估体系（ADR-0017 落地）规划完成：① 读全上下文（planner.md 角色定义 + 记忆三件套 + task-brief + ADR-0017 + module-065 plan 格式参考）；② 代码事实核实——react.py react_loop L277-296 落库插入点（allowed 截断后 tool.run 逐个执行）、trace_id 来源 request.state/observability 上下文（ReactContext 无该字段）、request_logs DDL 模式（database.py L53-75 + init_db 幂等）、eval_runs 记录模式（golden_retrieval.py L61-70/L218-249）、pytest 实测收集 1038（**task-brief 事实 5 写 897/0 系 module-063 前旧快照，回归基线以当前 1037 全量为准**）；③ 产出 `specs/module-066-agent-evaluation/plan.md`（WP-A tool_call_logs 表+落库 / WP-B agent_tasks.json 任务集 30-50 条 6 类路径 / WP-C eval/agent_tasks.py 三层指标+agent_eval_runs 落库+确定性判定器 / WP-D 回归+文档收口；含 DDL、判定四规则、CLI 参数、行数口径豁免声明、风险 6 项）+ `acceptance-criteria.md`（6 类验收 40 项：功能 19 + 边界 6 + 异常 5 + 代码质量 5 + 测试 + 文档）；关键决策：chat 模式无轨迹如实标注、eval 脚本豁免 ≤200 行上限、CONTEXT.md 只增不删红线、不达标如实分类报告不隐藏；④ 三记忆文件更新（project-context：module-066 行 + ADR-017 索引行 + v0.66.0 + 头部日期；file-index：specs/module-066 目录行；activity-log：本条 [PLAN]）；未 git commit（协调者统一提交） |

### 2026-08-17（module-066 Agent 级评估体系开发）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-066 | Developer | [CODE] Agent 级评估体系实施（ADR-0017，specs/module-066-agent-evaluation/）。**WP-A tool_call_logs 落库**：database.py +TOOL_CALL_LOGS_DDL（ADR 决策 2 一字不改）+ ensure_tool_call_logs_table + init_db 挂接（幂等实测二次运行 OK）；config.py +tool_call_logs_enabled（PW_TOOL_CALL_LOGS 默认 true）；react.py +execute_tool_with_log/record_tool_call 共用辅助（计时包住 run / result_ok=工具不存在或 run 抛出才 false / preview 截断 200 / args 非 JSON 序列化兜底 {} / fail-open / 开关 false 零构造；trace_id 从 observability contextvar 读不改循环签名）+ react_loop 执行处接线（循环逻辑/事件格式零改动）；langgraph_react.py execute_tools 同构接线（复用辅助防漂移）；conftest +autouse 钉住测试环境 false（存量测试零改动）。**WP-B agent_tasks.json 36 条**（六类路径：knowledge 单轮 17/多轮 7/casual 3/realtime 3/重检 3/记忆 3；RRF 题 answer_points 真实检索验证 KB 覆盖——"倒数排名"10 篇/"分数量纲"2 篇）。**WP-C eval/agent_tasks.py**：判定器四规则确定性（覆盖顺序放宽/无多调 re_search 豁免/参数类型不判值/outcome=覆盖+answer_points 子串；chat 模式 Trajectory 层置 None 如实"无轨迹"）+ 三层指标（pass^k 全成功口径/工具正确率/平均步数/token/P50-P95 线性插值）+ CLI（--mode chat|agent/--sample 种子 42/--pass_k/--limit/--no-save/--fixture 假 LLM 回放零 LLM/DB）+ agent_eval_runs 表（eval_type=agent_eval，git_commit+快照+per_question）+ 评测身份 eval-066-anon 测后清理 + 失败分类（参数错/工具选错/工具漏调/路径绕/答案缺要点）。**WP-D**：全量 pytest **1075/0** = 1037 基线 + 38 新增（test_tool_call_logs 12 + test_agent_tasks 26，存量零改动）；真实冒烟：fixture 全量 36 条 pass^1=1.0 六类全过（确定性演示）；**真实 agent 冒烟（--limit 2）pass^1=0.0 / 工具正确率 0.0 / Grounding 1.0 / 平均步数 4.0 / 平均 token 9492 / P50-P95 21.8/30.6s——发现：默认 tool_phase_split=true 下 react_loop 检索阶段 schema 不含生成工具（module-058 设计使然）→ generate_answer 循环内不可达，LLM 4 轮全检索预算耗尽兜底生成，期望序列含 generate_answer 的任务按覆盖规则判 fail（全部归"工具选错"类）；tool_call_logs 真实落库 4 行验证（trace_id 关联/预览截断 200/duration_ms 1-1270ms）**；首次冒烟暴露表未建（服务端 init_db 未跑）→ 脚本真实模式启动时幂等 ensure 建表修复；chat 模式冒烟 + 阶段切分 A/B 见 changelog；changelog.md + CONTEXT.md（备份先行，补 ADR-0017 行 + module-066 索引行只增不删）+ ADR-0017 状态 ✅ 已实施 + 三记忆文件更新（project-context 行/日期/ADR 索引/§5 迭代状态 + file-index 8 行 + 本条）；未 git commit（协调者统一提交） |

### 2026-08-17（module-066 Agent 级评估体系审查）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-066 | Reviewer | [REVIEW] **结论：✅ Pass（4 项 minor 非阻塞 + 1 项建议改进，进 Tester）**。独立验证（不采信 changelog 数字）：全量 pytest **1075/0（145.36s）** 独立复跑一致；根目录收集 1075+1 ERROR（scripts/test_models.py module-050 遗留未触碰）一致；`--fixture` 独立复跑 pass^1=1.0 六类路径全过（P95 2.2 vs 3.0ms 计时抖动）；**直查 DB**：tool_call_logs 155 行全字段验证（截断 200/result_ok/duration 1-15017ms）+ agent_eval_runs id=1 per_question 与 changelog pass^3 报告逐字一致（pass^3=0.1、6/3/1 per-path、realtime at-303 过、9×工具选错）+ 种子 42 抽样独立复算与落库任务集一致 + answer_points KB 覆盖直查命中（倒数排名 10 篇/分数量纲 2 篇逐字吻合）+ eval-066-anon 清理 0 残留 + CONTEXT.md 只增不删 + 记忆三件套全在。**红线全守**（工具注册表零 diff/react 循环仅单行替换/无 LLM judge/request_logs 不动/存量测试零改动仅 conftest 加 autouse）。**minor 4 项**：① changelog pass^3 根因归因不精确——DB 显示落库 run 中 at-002 第 2 次尝试实际调用了 generate_answer（id=19，15s 超时），默认阶段切分下不可能 → 该 run 实为 PW_TOOL_PHASE_SPLIT=false 执行但 changelog 未记录配置，"同一根因不可达"概括越界（"结构性不可达"仅对默认配置成立，backlog 条目正确限定）；② agent-lg E2E "3 行"实为 4 行（c7ff1c5e trace）；③ 首跑 at-001 trace（含 extract_entities）DB 无对应行；④ 确定性 trace_id 跨运行复用致 grounding 读回混历史行（当前全 ok 无失真，潜在污染）。建议改进：--fixture --mode chat 非零 LLM footgun。报告：specs/module-066-agent-evaluation/review-report.md。**模块标记 ✅ pass（待 Tester 验收）** |
### 2026-08-17（module-066 Agent 级评估体系测试）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-066 | Tester | [TEST] **结论：✅ 验收通过（40 项 AC：39 过 + 1 项首跑指标未达标如实记录且 AC 降级行为满足）**。独立复跑（不采信 changelog 数字）：全量 pytest **1075/0（174.73s）** = 1037 基线 + 38 新增（test_tool_call_logs 12 + test_agent_tasks 26 全绿），存量测试零改动（仅 conftest +14 行 autouse 钉住开关，验收许可）；新增单测抽查全部覆盖 changelog 声明点（DDL 幂等/成功落库字段/截断 200/args 兜底/开关 false/fail-open/result_ok 三态/预算截断只记实际执行/langgraph 同构/trace_id contextvar + 判定器四规则/失败分类/指标聚合/fixture 全量确定性/chat 无轨迹/grounding 读回/记忆清理/落库/CLI）。**真实冒烟（独立执行）**：fixture 36/36 pass^1=1.0（P50/P95 1.0/3.0ms 逐字吻合）；真实 agent `--sample 2 --no-save` pass^1=0.5（at-002 过/at-008 工具选错分类），tool_call_logs +8 行独立查表（trace 关联/全表 preview max=200/duration 2-15016ms）；agent-lg HTTP E2E（uvicorn 8001）SSE 200 跑通（本跑 tool_count=0 零落库符合只记实际执行；c7ff1c5e trace 4 行验证 langgraph 落库）；agent_eval_runs id=1 直查与 changelog 逐字一致；eval-066-anon 清理 0 残留。**Tester 独立发现（与 Reviewer minor-① 同向且更强）**：默认配置（tool_phase_split=True）下本跑 at-002 实际执行了 generate_answer（id=159，15s 超时）——循环不校验工具名是否在暴露 schema 内，LLM 可调用注册表内非暴露工具 → "结构性不可达"表述过强，实为"schema 不引导调用"（可达性低）；该发现已写入 test-report.md §五观察 1，建议 backlog 修正归因。非阻塞：agent-lg 3 vs 4 行（minor-②）、确定性 trace_id 复用潜在 grounding 混历史行（minor-④）。报告：specs/module-066-agent-evaluation/test-report.md。**模块标记 ✅ 验收通过（待 Developer 提交推送后 team-lead 收口）** |

### 2026-08-17（module-067 MCP 集成规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-067 | Planner | [PLAN] MCP 集成（ADR-0018 落地）规划完成：① 读全上下文（planner.md 角色定义 + 记忆三件套 + task-brief + ADR-0018 + module-066 plan 格式参考）；② 代码事实核实——ToolRegistry（tool_registry.py：AgentTool name/description/args_schema/func/group，10 工具，检索组 7 含 re_search 双组 → **6 只读默认暴露须显式白名单非 group 过滤**）、main.py（app L175/lifespan L101/首个 mount 无先例）、config.py（pydantic-settings PW_ 前缀）、requirements.txt 为准无 pyproject（**mcp 未入 requirements 但环境已装 mcp 1.26.0**，FastMCP 实测 stateless_http/json_response 为构造参数、streamable_http_app() 零参、mcp CLI mcp.exe 可用而 python -m mcp 不可用）、pytest 实测收集 1076（**task-brief 事实 5 写 897/0 系 module-063 前旧快照，回归基线以 module-066 验收 1075 为准**）、conftest autouse 钉开关模式（fail-closed 检查须放 lifespan 不放 import 期防存量测试全炸）；③ 产出 `specs/module-067-mcp-integration/plan.md`（WP-A mcp_server.py build_server 动态注册 6 只读工具 + stdio 入口 / WP-B main.py 挂载 /ai/mcp + _mcp_auth_middleware 401 + lifespan fail-closed + PW_MCP_TOKEN + requirements 加 mcp==1.26.0 / WP-C 只读过滤+防御保留+边界声明+单测 / WP-D 回归 1075 基线+文档收口 README/CONTEXT/ADR-0018 状态/信息包；含 ctx 合成 4 字段、schema 三变体转换、截断 2000 第二道防线、风险 6 项）+ `acceptance-criteria.md`（5 类验收：功能 13 + 边界 5 + 异常 2 + 安全 6 + 代码质量 6 + 测试 3 + 文档 6，含验证命令表）；关键决策：显式 6 名白名单（re_search 不暴露）、FastMCP 1.26.0 构造参数适配、fail-closed 放 lifespan、走 AgentTool.run 复用 15s 超时降级语义、CONTEXT.md 只增不删红线；④ 三记忆文件更新（project-context：头部日期 + §5 迭代状态 module-067 进行中 + module-066 已验收待收口 + backlog 归因修正；file-index：module-067 目录行 + module-066 行归因修正；activity-log：本条 [PLAN]）；未 git commit（协调者统一提交） |
| module-067 | Developer | [CODE] MCP 集成实施（ADR-0018，specs/module-067-mcp-integration/）。**WP-A ai_service/mcp_server.py**：READ_ONLY_TOOLS 显式 6 名白名单（不按 group 过滤——检索组 7 含 re_search 双组状态类工具）+ build_server(registry, groups=None) 动态注册（ToolRegistry 零改动单一事实源；groups 显式传值按 group 过滤仅测试用）+ _make_tool_fn exec 构造带 type hints 闭包（JSON Schema→Python：string→str/integer→int/number→float/boolean→bool 未知→str 兜底；required 必填/带 default 默认/其余 Optional；**必填排前可选排后**——_VERIFY_SCHEMA 只 required answer 实测 SyntaxError 修复；**None 值参数剔除**防 int(None) 破坏 top_k 缺省）+ 执行走 AgentTool.run(args, ctx) 复用 15s 超时+降级（run 空串包装"（工具执行失败）"）+ 轻量 SimpleNamespace ctx（query/identity="mcp"/docs/memory/add_docs no-op）+ _truncate_result 2000 截断 + stdio 入口 + logging 零 print。**WP-B 挂载**：**FastMCP 1.26.0 实测适配 5 点**——stateless_http/json_response 构造参数（streamable_http_app 零参）/ 构造无 version 参数 / streamable_http_path="/"（默认 /mcp 挂载后成 /ai/mcp/mcp）/ **Starlette Mount 不转发 lifespan scope → 新增 mcp_http_lifespan 复刻 session_manager.run() 任务组语义（create_task_group+_task_group 注入+cancel，去单次 guard），main.py lifespan yield 前后进入**（否则每请求 "Task group is not initialized" 实测复现）/ **DNS rebinding 保护关闭**（默认仅放行 localhost Host，实测 test Host 421，远程部署会全拒；token 认证即安全边界）；app.mount("/ai/mcp", _mcp_auth_middleware(...)) + _mcp_auth_middleware（ASGI 包装：hmac.compare_digest 常量时间 + 每请求实时读 settings.mcp_token 改 token 立即生效 + 空 token 恒 401 双保险）+ **lifespan fail-closed**（PW_MCP_TOKEN 未设置启动即抛 RuntimeError——放 lifespan 不放 import 期，存量测试全 ASGITransport 零影响，grep 零 TestClient 实证）+ config mcp_token + requirements mcp==1.26.0。**WP-C**：4 个非只读（generate_answer/verify_answer/re_search/note_to_self）零暴露 + 工具内部防御保留 + 明确不做（client/工具治理迁移/完整问答/SSE）。**WP-D**：新增单测 **27 项**（tests/api/test_mcp_server.py：注册遍历/只读过滤/显式 groups/description 透传/schema 三变体+未知 type+default/截断 3/执行 8（结果/ctx query+identity/memory 可写/失败包装/None 剔除/缺必填 ToolError/类型错误 ToolError/_make_ctx）/HTTP 认证 6（无·错·对 401-401-200 initialize 握手 + 307 规范 URL + 空 token 恒 401 + 改 token 即时生效）/lifespan fail-closed raise）**全量 pytest 1102/0** = 1075 基线 + 27 新增（存量零改动；scripts/test_models.py 1 项 module-050 遗留 ERROR 未触碰）。**真实冒烟全过**：stdio python 官方 MCP client 6 工具全列出（generate/re_search 不在列）+ search_knowledge 真实检索（真实 PG + 真实图谱实体提取 entities=3 返回 2 篇命中文档）；HTTP 真实 uvicorn 无/错 token 401 + 正确 token initialize 200（serverInfo personal-knowledge-kb）+ 无尾斜杠 307→401 链；**fail-closed 启动即抛 RuntimeError 拒绝启动实测**；mcp dev 可启（Inspector GUI 沙箱不可开，如实记录）；规范 URL /ai/mcp/（尾斜杠）；本地 .env 需补 PW_MCP_TOKEN 才能启动。changelog.md + ADR-0018 状态 ✅ 已实施（验收标准逐条核对 + 适配点标注）+ CONTEXT.md（备份先行，补 ADR-0018 行 + module-067 索引行只增不删）+ README（PW_MCP_TOKEN + 功能段 MCP 能力）+ 07-项目经历-精简版.md（核心工作第 6 条 MCP 集成）+ 三记忆文件更新；未 git commit（协调者统一提交） |
| module-067 | Reviewer | [REVIEW] **结论：✅ Pass（7 项 LOW/INFO 非阻塞，0 阻塞）**。独立验证（不采信 changelog 数字）：全量 pytest **1102 passed + 1 ERROR（161.29s）**——ERROR 为 scripts/test_models.py::test_model fixture 收集错误（文件 git diff 零改动，module-050 遗留，module-066 review 同口径）；新增单测 27/27（47.39s）；增量 1102−27=1075 与 module-066 基线吻合。**真实冒烟独立复跑**：真实 uvicorn（PW_MCP_TOKEN=review-smoke-067）无/错 token **401** + 正确 token initialize **200**（serverInfo personal-knowledge-kb）+ 无尾斜杠 **307**；无 token 进 lifespan **RuntimeError 启动拒绝**实测；build_server 真实 registry 实查恰 6 工具（query 必填/top_k anyOf[integer,null]/description 逐字透传）；search_knowledge 真实检索返回 score=1.0 命中文档；零 properties schema 边界实测正常；mcp_server.py 163 行/135 功能代码（≤200 达标）、零 print。红线全守：tool_registry/react/engine/conftest 零 diff，main.py 仅挂载+中间件+import+lifespan 检查，config 仅 mcp_token，requirements 仅 mcp==1.26.0；CONTEXT.md +2 行只增不删。**WP 核对全过**：WP-A 白名单 6 只读/exec 参数模型（必填排前可选排后 + None 剔除）/AgentTool.run 复用/轻量 ctx/截断 2000；WP-B 挂载+hmac 常量时间+实时读 token+空 token 恒 401 双保险+fail-closed 放 lifespan（grep 零 TestClient 实证）；WP-C 4 非只读零暴露+内部防御保留+不做 client/治理迁移/完整问答/SSE；WP-D 文档收口全（changelog 诚实：Inspector GUI 不可开/mcp dev 可启/规范 URL 尾斜杠）。**非阻塞发现**：① mcp_http_lifespan 未复刻 SDK run() 的 _server_instances.clear()（对比 SDK 源码逐行）；② 访问 FastMCP 私有属性 _session_manager/_task_group（== 锁版本缓解，升级需复核）；③ DNS rebinding 保护关闭 token 为唯一边界；④ _make_ctx 含未被暴露工具使用的 history/scratchpad/add_note 冗余字段；⑤ MCP 统一 identity="mcp" 共享记忆命名空间（与 web 用户隔离）；⑥ fail-closed 为全服务级（本地 .env 需补 PW_MCP_TOKEN）；⑦ plan "00-信息包"文件不存在落地 07-简历精简版（合理替代）。报告：specs/module-067-mcp-integration/review-report.md。**模块标记 Reviewer ✅ Pass（待 Tester 验收）** |

### 2026-08-17（module-068 Agent 阶段推进死锁修复规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-068 | Planner | [PLAN] Agent 阶段推进死锁修复规划完成（specs/module-068-agent-phase-fix/plan.md + acceptance-criteria.md）。① 代码事实核实（读 react.py/tool_registry.py/config.py/langgraph_react.py/test_tool_phase_split.py/test_agent_tools.py）——**advance_phase(ctx, executed_names)**（react.py:154-163）推进条件=调过生成工具，调用点两处（react.py:389 + langgraph_react.py:160），**存量单测 test_advance_phase_unit 单列表直接调用 → 签名扩展须向后兼容（executed_results=None 缺省）**；检索组 7（search_*/extract_entities/recall_memory/re_search 双组）→ 命中判定清单 6 个不含 re_search；**执行工具处 react.py:382 result=await execute_tool_with_log 返回结果字符串可用，当前只收集 executed_names 需扩展**；⚠️ **判定坑（task-brief 未覆盖）**：_format_docs 空结果返回"（无检索结果）"（tool_registry.py:151）、recall_memory 空返回"（无相关历史记忆）"（:218）均非空字符串，bool(result) 会误判命中——判定须排除空结果标记 + extract_entities 解析 JSON entities 非空；预算截断点 react.py:357 allowed=tool_calls[:max(0,budget-tool_count)]，budget 源 config.py:104 max_agent_tools=4；**阶段预算必须联动 tool_phase_split 开关**（conftest 钉住 false 保存量全量工具测试不被"检索≤3"截断）；基线 **1102/0** 确认。② 产出 plan.md（WP-A 死锁修复：_retrieval_hit 纯函数 + 签名兼容扩展 + ctx.retrieval_rounds 兜底 3 轮强制切参数化 PW_AGENT_RETRIEVAL_MAX_ROUNDS / WP-B 预算按阶段：检索 ≤3 + 生成 ≤2 总 5，config agent_retrieval_budget/agent_generation_budget + max_agent_tools 4→5 总兜底，复用截断点 min(总剩余,阶段剩余)，ctx.phase_count 按执行时阶段计数 / WP-C 066 评测重跑对比首跑 pass^1=0.0·工具正确率 0.0·chat 0.6667·pass^3=0.1 如实记录 / WP-D 回归 1102 基线 + changelog/CONTEXT 只增不删/三记忆，无新 ADR；风险 5 项含判定字符串耦合记 backlog）+ acceptance-criteria.md（24 项 AC：核心路径 15 + 性能 5 + 代码质量 4 + 验证命令表）；③ 三记忆文件更新（project-context：日期 + §5 module-068 进行中 + backlog 归因修正；file-index：module-068 目录行；activity-log：本条 [PLAN]）；未 git commit（协调者统一提交） |
| module-068 | Developer | [CODE] Agent 阶段推进死锁修复实施（specs/module-068-agent-phase-fix/）。**WP-A 死锁修复**：react.py 新增 _RETRIEVAL_HIT_TOOLS 6 清单（不含 re_search）+ _EMPTY_RESULT_MARKERS（空结果标记"（无检索结果）"/"（无相关历史记忆）"非空字符串坑）+ _retrieval_hit 纯函数（零 LLM 判断，extract_entities 解析 JSON entities 非空）+ advance_phase(ctx, executed_names, executed_results=None) 签名向后兼容扩展（缺省 None=旧逻辑，存量 test_advance_phase_unit 零改动）+ ctx.retrieval_rounds 防空转兜底（≥agent_retrieval_max_rounds=3 强制切，PW_AGENT_RETRIEVAL_MAX_ROUNDS 参数化）+ 两循环执行处同序收集 results。**WP-B 预算按阶段**：config 新增 agent_retrieval_budget=3/agent_generation_budget=2 + max_agent_tools 4→5（不删总预算字段兼容）；截断点改 min(总剩余,阶段剩余)，仅 tool_phase_split=true 生效（false 纯总预算存量逐字）；ctx.phase_count 按执行时阶段计数；langgraph 新增 phase_exhausted 路由防阶段额度耗尽回 llm_call 死循环（allowed 恒空）。**WP-D 测试**：新增 14 项 test_agent_phase_fix.py（命中切/兜底参数化/re_search 不回退/旧签名兼容/纯函数边界 8 例/langgraph 同构/阶段截断 4 例/总预算兜底/开关 false/phase_count 计数）；conftest autouse 钉住 max_agent_tools=4（生产 4→5 与存量 budget==4 断言矛盾的双轨化解）；**存量 test_tool_phase_split.py 6 处 schema 序列断言按新推进语义更新（plan 矛盾点 §五 如实记录，Reviewer 裁定）**；全量 pytest **1116/0** = 1102 基线 + 14 新增（scripts/test_models.py 1 项 module-050 遗留 ERROR 沿用）。**WP-C 评测重跑**：agent_eval_runs **id=3**（2026-08-17 09:16，默认配置 budget=5）：**pass^1=0.0（0/10）未提升如实记录**——结构性修复实证生效（单轮轨迹呈"检索 2 + 生成阶段 2"阶段截断形态 + 单测全绿）但 deepseek 行为性残余（生成阶段 schema=GENERATION_4 下仍持续输出 search_* 调用——_SYSTEM_PROMPT 全量 10 工具清单 + 执行层不校验 schema 暴露（at-002），generate_answer 从未入轨迹 → 覆盖失败）；失败分类 工具选错×8/工具漏调×1/答案缺要点×1（at-303 realtime id=1 pass→id=3 fail 系 LLM 答案方差）；**§三勘误**：id=2（08-16 23:58 落库）早于本模块代码且轨迹超默认预算口径，非本模块重跑不采信。changelog（WP-A~D + 矛盾点 §五 + WP-C 实测 §六 + 勘误）+ CONTEXT.md 只增不删 + 三记忆更新；未 git commit（协调者统一提交） |
| module-068 | Reviewer | [REVIEW] **结论：⚠️ 有条件通过（1 项 mustFix）**。独立验证（不采信 changelog 数字）：全量 pytest **1116 passed + 1 ERROR（162.65s）**（scripts/test_models.py 系 module-050 遗留收集 ERROR，scripts/ 零 diff）；定向 94/0（新增 14 + test_tool_phase_split/test_agent_tools 80）；**agent_eval_runs 直查 DB 三行**——id=3 scores 与 changelog §六逐字一致（pass_1=0.0/tool_correct 0.1/no_extra 0.2/args 1.0/grounding 1.0/avg 4.6/tokens 16556.6/P50 24543.5/P95 37367.0/per_path 全 0）；失败分类按 classify_failure 四规则独立复算一致（工具选错×8/工具漏调×1/答案缺要点×1）；eval-066-anon 清理 0 残留。红线全守（tool_registry/engine/agent_tasks.py+json/database/scripts 零 diff）；CONTEXT.md +8 行只增不删；test_advance_phase_unit 零改动。**WP-A/B 代码与 plan 逐字一致**（_retrieval_hit 边界 8 例 + advance_phase 三条件 + 截断点 min 联动 tool_phase_split + langgraph phase_exhausted 必要补充）。**plan 矛盾点 §五裁定：接受**（6 处 schema 序列断言随特性语义更新，仅断言值+注释，tool_count/兜底断言逐字保持，先例 061/062）。**mustFix #1（阻塞）**：changelog §三 勘误两处论证与硬证据矛盾——① PG TimeZone=Etc/UTC 实测，agent_eval_runs.created_at 为 UTC：id=2（23:58:57 UTC）= **2026-08-17 07:58 本地**，晚于 react.py 最后修改（07:36 本地），"早于本模块代码落地"不成立（§六 09:16 做了 +8 换算而 §三 未换算，两次口径不一致）；② "轨迹（最高 8 工具）超默认预算"不成立：多轮任务按轮重置预算（_run_agent_once 每轮新 ctx + react_loop(max_agent_tools)），8=4+4，id=1/id=3 同有 8 工具任务；③ 正确画像：id=2 系 module-068 代码落地后的运行，其 at-101 轨迹含 generate_answer 且 pass（三次运行唯一知识类 pass 实证，WP-A 通过标准直接证据），环境覆盖无法从 config_snapshot 确证，应并列报告（pass^1=0.2/0.0）而非错误排除。minor 3 项（search_graph 空实体标记"（图检索：未提取到实体）"未入排除清单 / 无生产默认 5 断言 / conftest 钉 4 属 hermetic 设计接受）。报告：specs/module-068-agent-phase-fix/review-report.md。**模块标记 ⚠️ Reviewer CONDITIONAL（mustFix 待 Developer 修复后进 Tester）** |

| module-068 | Reviewer | [REVIEW-2 复审] **结论：✅ 通过（PASS）——第一轮 mustFix 已修复验证，进 Tester**。Developer 修复轮仅改 changelog.md（代码与第一轮逐字一致）。mustFix ①②③ 逐项独立核验（不采信 changelog）：① **时间口径**——DB 实测 created_at 为 PG UTC（TimeZone=Etc/UTC），id=2=23:58:57 UTC = **08-17 07:58 本地**，晚于代码 mtime（config.py 07:35:49/react.py 07:36:07/langgraph_react.py 07:36:25 文件系统实测），"早于代码落地"撤回成立；② **轨迹超预算论证删除**——多轮按轮重置预算（per_question id=2 at-105/at-107=8 工具=两轮各 4），id=1/2/3 avg_tool_count 均 4.6（scores 直查）；③ **id=2 并列报告**——per_question 直查：at-101 pass=True/cov=True/6 工具含 generate_answer（检索 3 → GA → 补检 2 形态逐字吻合）+ at-303 零工具 pass → pass^1=0.2 ✓；config_snapshot 仅 10 个 RAG 键无 agent 侧配置（环境覆盖无法确证如实标注成立）；id=3 主口径 pass^1=0.0 不变。**§六 WP-A 证据**：tool_call_logs 按 created_at 窗口切分实测——id=2 窗口 27 条 eval 尝试（28 trace_id−probe-068-1）5 条含 generate_answer（at-101-1/2/3、at-002-3、at-016-2）✓ / id=3 窗口 28 条 2 条含（at-016-2、at-101-3）✓ / id=3 判定轨迹（attempt 1）10 条全缺 ✓ / id=3 at-016 4×search_knowledge no_extra=True cov=False=工具漏调 ✓。复验：定向 94/0 + 全量 1116/0（1 ERROR 系 scripts/test_models.py module-050 遗留）；红线零 diff（tool_registry/engine/agent_tasks.py+json/database/scripts/requirements）；CONTEXT.md +8 行只增不删；eval-066-anon 0 残留。第二轮新增 minor 1 项（test_verify_answer_switches_to_generation 断言更新后不再独立覆盖 verify_answer 触发切——集合成员静态可审，低，入 backlog）。报告：specs/module-068-agent-phase-fix/review-report.md（§七 第二轮复审）。**模块标记 ✅ Reviewer PASS 待 Tester** |

### 2026-08-17（module-069 PDF 回退路径升级规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-069 | Planner | [PLAN] PDF 回退路径升级规划完成（specs/module-069-pdf-fallback-upgrade/plan.md + acceptance-criteria.md）。① 代码实测核实（读 document_parser.py _parse_pdf_pymupdf L151-176：page.get_text() 裸文本无坐标无结构、config.py Settings PW_ 前缀模式、requirements.txt 当前无 pymupdf4llm）；② 深挖文档核实（01-文档解析与数据清洗.md 2.1.2 pymupdf4llm 决策 B 档 + 2.2.1 双栏中线重组代码 _extract_with_column_order 三坑：单栏误切/跨中线块/跨栏表格）；③ 测试 PDF 核实（主 checkout docs/项目深挖/原PDF-rag_survey.pdf 21 页英文双栏 1.66MB + 原PDF-图检索增强生成研究综述.pdf 12 页中文 3.1MB，worktree 内无需从主 checkout 复制）；④ grilling 自查 8 项全确认（WP-A/B 顺序依赖/三坑/pymupdf4llm 清洗层衔接/测试 PDF 路径/AGPL 许可/PW_ 前缀/conftest autouse 模式）。产出 plan.md（WP-A 双栏重组 _reorder_columns 纯函数 ~80 行 + WP-B pymupdf4llm 升级 ~30 行 + WP-C 测试文档 ~120 行）+ acceptance-criteria.md（功能 9+5+3 + 性能 2 + 代码质量 5 + 合规 2 + 验证命令 5）；三记忆文件更新；未 git commit |
| module-069 | Developer | [CODE] PDF 回退路径升级（specs/module-069-pdf-fallback-upgrade/，开工前已读 plan/AC/task-brief/ADR-0014/project-context.md）。**WP-A 双栏中线重组**：`document_parser.py` 新增 `_reorder_columns(page)` 纯函数（~60 行）——get_text("blocks") + 双栏检测左右各>=2 块 + 跨中线块置顶 + 跨栏表格 | 跳过 + 图片块过滤；`_parse_pdf_pymupdf` 双栏页面调 `_reorder_columns`。**WP-B pymupdf4llm**：requirements.txt 补 pymupdf4llm（AGPL-3.0 注释）+ config.py `pdf_fallback_md` 默认 true（PW_PDF_FALLBACK_MD）+ `_parse_pdf_pymupdf` 开关 true 走 pymupdf4llm.to_markdown() / false 走旧 get_text() + 延迟导入降级 + engine 字段区分。**WP-C 测试**：新增 15 项（TestReorderColumns 7 + TestPdfFallbackMdSwitch 5 + TestColumnReorderIntegration 3）+ conftest autouse 钉住 false + 全量 **1131/0**（1116 基线 + 15 新增，存量零改动）。changelog + 三记忆文件更新；未 git commit |
| module-069 | Reviewer | [REVIEW] **结论：⚠️ Conditional（1 项 P1 建议修复 + 3 项 minor 非阻塞）**——对照 AC 8 维逐条核查。**独立验证**：① 全量 pytest 复跑 **1131 passed / 0 failed（187.67s）**与 changelog 一致（1116 基线 + 15 新增）；② 定向 test_document_parser **36/36**（21 存量 + 15 新增）；③ py_compile 两变更文件 OK；④ pymupdf4llm version 1.28.2 已装；⑤ 红线零触及（document_cleaner/document_ingest/document_dedup/image_pipeline git diff 为空）；⑥ 存量测试零改动（仅 conftest 新增 autouse 钉住 pdf_fallback_md=False）。**WP-A 通过**：_reorder_columns 纯函数质量优秀——三坑全覆盖（单栏 >=2 阈值 / 跨中线块置顶 / 表格 `|` 跳过）+ 图片过滤 + 空块 + 全跨中线，7 项单测 + 真实双栏 PDF 集成。**WP-B P1 问题**：pymupdf4llm 可用时 `_reorder_columns(page)` 输出被丢弃——pymupdf4llm 处理原始 page 对象非 reorder 后文本（L257-277 `md_text = pymupdf4llm.to_markdown(pdf_doc, pages=[i-1])` 处理原始页面）。违反 plan "WP-A 重组前置"依赖——pymupdf4llm 的 `to_markdown` 自身有版面分析可能处理双栏，但 WP-A 结果未被使用。**影响**：pymupdf4llm 成功时 reorder 空转（功能不受影响），pymupdf4llm 失败时 reorder 作 fallback（正确路径）。建议文档同步说明 pymupdf4llm 自身处理双栏版面。**WP-C 通过**：15 项新增全 hermetic + conftest 钉住 + AGPL 许可完备。**minor 3 项**：changelog `engine="pymupdfdf"` 拼写多一 f / pymupdf4llm 路径每页调 _reorder_columns 但输出被丢弃浪费 CPU / 集成测试未验证 pymupdf4llm 输出阅读顺序。报告：specs/module-069-pdf-fallback-upgrade/review-report.md。**模块标记 ⚠️ Conditional（P1 待 Developer 文档同步后进 Tester）** |
| module-069 | Developer | [FIX] Review P1 修复：plan.md 澄清 WP-A/pymupdf4llm 交互（pymupdf4llm 自身处理双栏版面，_reorder_columns 仅作 fallback）+ changelog.md 新增设计决策 5 + Review 修复段（实际运行时行为三路径 + 设计取舍说明）+ plan.md 修正 engine 字段拼写 pymupdfdf→pymupdf（minor-1 顺带修）+ 全量测试 1131/0 验证通过 |
| module-069 | Reviewer | [REVIEW-2 复审] **结论：✅ 通过（PASS）——P1 已修复验证，进 Tester**。Developer 修复轮改 plan.md + changelog.md（代码与第一轮逐字一致）。P1 逐项核验：plan.md L60 已澄清"pymupdf4llm 内部自带版面分析可处理双栏；`_reorder_columns` 仅在 pymupdf4llm 不可用或失败时作为 fallback 生效"✓；changelog.md §五 设计决策 5 + §八 Review 修复段完整描述三条运行时路径（pymupdf4llm 成功→输出覆盖 / pymupdf4llm 失败→reorder fallback / pymupdf4llm 未安装→get_text 裸文本）+ 设计取舍（保留每页调用非条件化跳过因 pymupdf4llm 失败是运行时异常）✓；engine 字段拼写 pymupdfdf→pymupdf 已修正✓。复验：全量 **1131/0（201.59s）** + 定向 **36/36（92.44s）** + 红线零改动 + 存量零改动。4 项 minor 非阻塞（changelog §二 措辞对齐 / 冗余调用 / 未安装路径覆盖 / 测试 reload）。报告：specs/module-069-pdf-fallback-upgrade/review-report.md（§九 第二轮复审）。**模块标记 ✅ Reviewer PASS 待 Tester** |

### 2026-08-18（module-070 记忆矛盾检测规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-070 | Planner | [PLAN] 记忆矛盾检测——评测集扩展 + 双判共识决策规划完成（specs/module-070-memory-conflict-decision/plan.md + acceptance-criteria.md）。① 代码事实核实（读 memory.py `_judge_conflict` L540-577 / `_merge_duplicate` L462-538 / config.py L206-207 / memory_conflict_clf.py / nli_judge.py / eval/datasets/memory_conflict_dataset.py 70 条 / test_memory_evolution2.py 存量断言 / conftest L142·L195）——裁判签名：clf `load()→bool` + `predict→"contradiction"|"non_conflict"|None`、nli `predict→"entailment"|"neutral"|"contradiction"|None`（20s 超时内建）；`_merge_duplicate` 仅判 `== "contradiction"`（conflict_hint 自然落入追加分支，判定逻辑零改动）；**实测 pytest 收集 = 1142 与 task-brief 一致**；`models/memory_conflict_clf.joblib` + `models/mdeberta-nli` 均在盘。② **grilling 自查 8 项全确认**：双判共识 AND 语义决策表固化 7 行（双 contradiction→superseded / 单 contradiction→conflict_hint 并存 / 双非矛盾→nli 标签 / clf 缺失→nli 单判零回归 / nli 不可用→clf 单判对称回退 / 双 None→None 追加）；降级路径全枚举（clf load False·predict None·异常 / nli None·超时·异常 / 双 None / enabled=false 不进判定 / judge 非法值 pydantic Literal 拒绝）；4 条边界陷阱（scenario=边界×4 verdict=neutral 用户 2026-08-18 确认不可改）plan+AC 双声明；WP-A 数据只影响默认值选择不阻塞 WP-B（WP-B 全 mock hermetic）；存量断言兼容（TestJudgeConflictDispatch 5 项显式 setattr 不受 dual 分支影响 / TestConflictDataset 结构断言 70 条下仍过 / conftest autouse 零改动）。③ **新发现 3 项（task-brief 未覆盖）**：a) 评测集 70 条与训练集 1 条字符串精确重叠（"用户喜欢摄影"中性样本 hypothesis vs 训练集 premise）→ `test_train_dataset_balance_and_no_overlap`（test_memory_evolution2.py:826-838）必 FAIL，WP-A 措辞去重修复（"用户平时喜欢摄影"，verdict=neutral 不变、非边界陷阱样本）；b) `run_eval` VERDICTS 校验会拒 "conflict_hint"（`pred not in VERDICTS` → ValueError skip）→ dual_judge 内映射 neutral（contradiction P/R 主指标等价，accuracy 参考口径失真如实声明）；c) eval_runs 三方案（nli/clf/dual）区分需 scores 加 `judge` 字段（对齐 memory_type eval model 字段先例）。④ 产出 plan.md（WP-A 评测集收尾+三方案真实跑分对比 / WP-B `dual_verdict` 纯函数+`_judge_conflict` dual 分支+config `memory_conflict_judge` Literal 扩展默认保持 nli / WP-C 回归+文档收口；预估功能 ~60 行）+ acceptance-criteria.md（24 项 AC + 验证命令表）；关键决策：dual 默认值由 WP-A 数据定（brief 倾向 dual 默认 + nli 回退保留，changelog 记录对比表+理由）、无新 ADR（ADR-0007 状态行更新 + CONTEXT.md 只增不删 + METRICS.md 待办③ L236）；⑤ 三记忆文件更新（project-context：头部日期 8-18 + §5 module-070 进行中 + backlog 标注已转 module-070；file-index：module-070 目录行；activity-log：本条 [PLAN]）；未 git commit（协调者统一提交） |

### 2026-08-18（module-070 记忆矛盾检测双判共识开发）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-070 | Developer | [CODE] 记忆矛盾检测——评测集扩展 + 双判共识决策实施（specs/module-070-memory-conflict-decision/）。**WP-A**：① 数据集措辞去重（中性样本 hypothesis "用户喜欢摄影"→"用户平时喜欢摄影"，verdict=neutral 不变，非 4 条边界陷阱；python 实测 `eval_texts & train_texts = set()` 零重叠不变式恢复）；② eval 脚本 +`dual_judge`（clf load→predict + nli predict 各自 try/except→None → 引用生产 `dual_verdict` 纯函数单一来源防漂移；"conflict_hint"→"neutral" 映射避开 run_eval VERDICTS 校验——contradiction P/R 主指标等价，accuracy_3class 参考口径失真如实声明）+ `--judge dual` + `scores["judge"]` 落库区分三方案（对齐 memory_type eval "model" 先例）。**三方案真实跑分（70 条全评估 0 skip，eval_runs id=46/47/48）**：nli P 0.9167/R 0.55（**30 条口径 1.0 Precision 假象证实**，fp=2）/ clf P 0.8158/R 0.775（**人造分布缩水**，fp=7 最贵失败模式）/ **dual P 0.9412（fp=1 最高）/R 0.4（AND 共识数学性质 ≤ min 单判）**→ **数据驱动默认值决策 = "dual"**（config.py 已改；PW_MEMORY_CONFLICT_JUDGE=nli|clf 回退保留；clf 缺失自动回退 nli 单判 fail-open 零回归）。**WP-B**：memory.py +`dual_verdict(nli_v, clf_v)` 模块级纯函数（决策表 7 行唯一真源）+ `_judge_conflict` dual 分支（clf 先行 load 短路→nli 次行→dual_verdict；存量 clf/nli 分支逐字不动）+ `_merge_duplicate` conflict_hint 日志 elif（判定逻辑零改动）+ config Literal +"dual" 默认改 "dual"。**WP-C**：新增单测 10 项（TestJudgeConflictDual 9 全 mock：双 contradiction/单矛盾两向/双非矛盾 nli 标签/clf load False 断言未调用/clf 异常/nli None 对称回退/双 None/纯函数决策表全覆盖 + TestMergeConflictHint 1：conflict_hint 追加拼接+superseded 仍 False+日志断言；存量 5 项逐字不改）；全量 pytest **1152/0** = 1142 基线 + 10 新增（228.51s；config 默认改 dual 后复跑 1152/0 + 内存测试 110/0）；changelog.md（对比表 + 最终选择 dual + 理由 + 措辞去重记录 + conflict_hint 口径声明 + 诚实边界）+ CONTEXT.md（备份 %TEMP% 先行，+10 行只增不删）+ METRICS.md（记忆矛盾检测段 70 条三方案表 + 待办③ 标记完成）+ ADR-0007 状态行补 module-070（P1 双判共识 + 默认 dual）+ 三记忆文件更新；未 git commit（协调者统一提交） |
| module-070 | Reviewer | [REVIEW] **结论：✅ 通过（PASS，进 Tester）**。独立验证（不采信 changelog）：① 全量 pytest 复跑 **1152 passed / 0 failed（225.80s）**与 changelog 一致；② test_memory_evolution2.py **82 passed**（HEAD 基线 72 + 新增 10，diff 逐函数核对恰好 +10）；③ AC-9/10/11 数据集结构 + 零重叠 6 passed（70 条结构 / 4 边界陷阱 neutral / eval_texts & train_texts 空）；④ **DB 直查 eval_runs id=46/47/48 与 changelog 对比表逐字一致**（46 nli 0.6429/0.9167/0.55 tp22 fp2 fn18；47 clf 0.6/0.8158/0.775 tp31 fp7 fn9；48 dual 0.5286/0.9412/0.4 tp16 fp1 fn24；size=70 skipped=0 judge 字段区分三行）；⑤ **AND 共识逐样本验证**：nli fp=2→dual fp=1（nli 独判误标"用户现在大三|用户是本科生"被 clf non_conflict → conflict_hint → neutral 中和）；⑥ `dual_verdict` 决策表 7 行逐行核对全对 + 降级路径全覆盖（clf load False/predict None/异常、nli None/异常、双 None——9 项单测全 mock）+ `_merge_duplicate` conflict_hint elif 追加不标 superseded；⑦ 存量零改动红线（conftest 零 diff、TestJudgeConflictDispatch 5 项逐字）；⑧ 判定器确定性（纯函数 + 双模型推理零 LLM）；⑨ CONTEXT.md 只增不删 +10 行 / METRICS.md 待办③ / ADR-0007 / 三记忆文件全在。**5 项 LOW/minor 非阻塞**：测试数口径文档数字 70→80 应为 72→82（file-index + changelog §五 顺手修正）；eval `scores["judge"]` fixture 落库标签失真（L501）；双判唯一 fp 样本标注存疑（"嵌入式方向更好|原专业方向有优势"两独立裁判均判矛盾，非用户确认边界陷阱，入标注复核 backlog，即使改标 dual 默认决策不变）；eval dual_judge clf load False 无 warning 日志（生产有）；project-context §5 迭代状态 v0.69.0 陈旧（已随附修正）。报告：specs/module-070-memory-conflict-decision/review-report.md。**模块标记 ✅ Reviewer PASS 待 Tester** |

### 2026-08-18（module-071 幻觉检测 kappa 校准规划）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-071 | Planner | [PLAN] 幻觉检测 kappa 校准规划完成（specs/module-071-hhem-kappa-calibration/plan.md + acceptance-criteria.md）。① 代码事实核实（读 golden_factcheck.py / reflector.py `_judge_by_hhem` L518-590 / factcheck_judge.py / config.py L260-264 / golden_sufficiency.py / test_factcheck_judge.py / real_retrieval_pairs.json / conftest.py / METRICS.md / module-070 plan 格式参考）——**阈值参数化现状**：config `verify_hhem_threshold_high=0.7/low=0.3`（pydantic env_prefix=PW_，逃生口 PW_VERIFY_HHEM_THRESHOLD_* 已核实）；映射逻辑两处 inline（golden_factcheck.judge_factcheck L293-299 + reflector L580-585 同口径）；**分数只算一次可实现**：run_eval 已把每条 max_score 存 per_question（L373 round 4 位）→ 扫描 = 1 次 run_eval + 25 组纯后处理，零缓存层；标注集来源已核实：real_retrieval_pairs.json 24 条真实 claim（entailment 9/neutral 13/contradiction 2）+ `contradiction_dataset.to_factcheck_item` 转换路径存在（entailment→supported/neutral→inferred/contradiction→unsupported）+ SUFFICIENCY_DATASET 50/50（当前只用 20+20，剩 30+30）；**存量测试影响**：test_dataset_structure_50_three_classes（L478-487 断言 len==50/counts {20,10,20}）标注集扩充必破 → 验收许可更新（module-061/062 先例）；test_run_eval_end_to_end（L543 断言 thresholds high==0.7）达标改默认必破 → conftest autouse 钉旧值；基线 task-brief 1163/0 vs module-070 实跑 1152/0 → 以实跑为准；无 factcheck 标注指南文件（新增 eval/datasets/factcheck_annotation_guide.md，对齐 contradiction_annotation_guide.md）。② **grilling 自查 6 项全确认**（grilling skill 未安装于本机，改手工按同口径执行）：a) 分数只算一次（run_eval 已存 max_score，scan_thresholds 纯函数只消费之，mock 断言 judge 调用次数==样本数作回归锁；--fixture+--scan 无分数显式报错）；b) 标注集来源（real_retrieval_pairs 转换 + SUFFICIENCY 剩余 + 新构造 inferred，question 去重 + keywords 必填 + part 来源字段）；c) inferred 口径重写对存量影响（10 条 INFERRED_SAMPLES + 13 条 neutral 转换样本复核 + 变更清单；WP-A 先跑旧口径方向性阈值，WP-C 复扫确认漂移风险如实声明）；d) 达标才改生产配置决策点（≥0.7 改 config 默认 + 注释 + PW_ 逃生口保留；<0.7 不改 + 失败模式分类 + backlog，决策规则写死 Developer 无自由裁量）；e) conftest 需钉旧值（L543 断言实证，达标分支新增 autouse fixture 0.7/0.3）；f) 落库单行 factcheck_scan 防 25 行噪音（benchmark_rrf_k 先例）。③ 产出 plan.md（WP-A 阈值扫描 ~70 行 / WP-B 标注集扩充+口径 ~35 行+JSON / WP-C 重跑+决策 config 2 行 / WP-D 回归+文档 conftest 6+测试 ~150；功能合计 ~110 行 ≤200）+ acceptance-criteria.md（28 项 AC + 验证命令表）；关键决策：max_score_to_verdict 三态映射唯一实现、单行 factcheck_scan 落库、决策规则写死、唯一存量例外 test_dataset_structure_50_three_classes 验收许可、无新 ADR（ADR-0010 P1-④ 状态行更新）；④ 三记忆文件更新（project-context：头部日期 + §5 module-071 规划中 + 进行中模块行；file-index：module-071 目录行；activity-log：本条 [PLAN]）；未 git commit（协调者统一提交） |

### 2026-08-18（module-071 幻觉检测 kappa 校准开发）
| 模块 | 角色 | 摘要 |
|------|------|------|
| module-071 | Developer | [CODE] 幻觉检测 kappa 校准实施（specs/module-071-hhem-kappa-calibration/）。**WP-A 阈值扫描**：golden_factcheck.py +`max_score_to_verdict`（三态映射唯一实现，judge_factcheck 重构引用，与生产 _judge_by_hhem 逐字同口径）+`scan_thresholds`（25 组纯后处理只消费 per_question.max_score，排序规则写死：三态 kappa→二值→贴近 0.7/0.3；无可评估样本→[]）+ CLI `--scan-thresholds`（落库 1 行 eval_type='factcheck_scan' 对照表内嵌 scores；--fixture 组合显式报错）+`--threshold-high|low`（apply_threshold_overrides 覆盖 settings 即时生效）；**真实扫描旧 50 集（id=49）：最优 0.65/0.35 三态 kappa 0.3711（vs 生产 0.7/0.3 = 0.3252 与 module-051 基线逐字一致，+0.0459）——阈值校准方向性，不改配置**。**WP-B 标注集扩充**：factcheck_real_samples.json 新增（32 条 = real_retrieval_pairs 24 转换（entailment→supported 9 / neutral→inferred 10 / contradiction→unsupported 2 + 3 条 neutral 口径复核改判 unsupported：熊艺诚×2/微服务）+ 8 条新构造 inferred）+ factcheck_annotation_guide.md（inferred"部分覆盖"边界写死：≥1 核心断言被**直接**支持 + ≥1 未覆盖且无冲突，"相关背景≠支持"）+ INFERRED_SAMPLES 口径复核（8 条 inferred→unsupported 改判：G1 调优/Kafka 生产者/联合索引/AOP 失效/Netty 粘包/JWT 刷新/CAS ABA/HashMap 扩容；2 条保持：线程池拒绝策略/哨兵故障转移）+ build 按 question 去重（real_retrieval>constructed>sufficiency，强制唯一；移除 G1/AQS/联合索引/synchronized/ZGC 重复）+ load 校验升级（≥100/question 唯一/keywords 必填/JSON 缺失 ValueError）→ **数据集 50→136（supported 57/inferred 20/unsupported 59）**。**WP-C 重跑决策**：新 136 集 + WP-A 最优 0.65/0.35 → **三态 kappa 0.2981 < 0.7 不达标 → 不改生产配置**（eval_runs id=50/51 复跑一致确定性；新集全网格最优 0.65/0.40 = 0.3309 id=52——阈值校准证伪天花板 0.33-0.37）；失败模式 61 条：supported 误杀 24（多文档中文分数压缩 RDB/AOF 0.334/B+树 0.061）/ inferred 混淆 15（HHEM 把部分覆盖判 supported 10 条：Kafka ISR 0.964/RocketMQ 0.924/Redis 哨兵 0.822/CHM 扩容 0.653 等 + 判 unsupported 5 条：MyBatis 0.059/Seata 0.057/JWT vs Session 0.071/Kafka Rebalance 0.286 等——Review 修复轮按 DB id=50 per_question 订正 12/3 → 10/5）/ unsupported 漏判 22（口径复核改判样本被 HHEM 打 0.79-0.90 高分：JWT 刷新 0.904/AOP 0.896——module-051 归因②实证）；入 backlog（中文专用 NLI/微调、两阶段拆句+保守门控、飞轮数据定向补样本）。**WP-D**：新增单测 19 项（TestMaxScoreToVerdict 7 边界+唯一实现引用/TestScanThresholds 8 含 mock judge 调用次数==样本数"分数只算一次"回归锁/数据集校验 4 ValueError）+ 唯一存量例外 test_dataset_structure_50_three_classes → 100_three_classes（验收许可，module-061/062 先例）；红线零改动（factcheck_judge/reflector/config 未动）；全量 pytest **1182/0** = 1163 基线 + 19 新增（208.74s）；changelog.md（25 组对照表/变更清单 11 条/重跑数字/决策/诚实边界）+ CONTEXT.md（%TEMP% 备份先行，只增不删 +7 行）+ METRICS.md（幻觉检测节 136 题表 + 待办② 标记完成不达标如实更新）+ ADR-0010 状态行补 module-071 P1-④ + 三记忆文件更新；未 git commit（协调者统一提交） |
| module-071 | Reviewer | [REVIEW] **结论：⚠️ CONDITIONAL（2 项 mustFix，修复后 PASS）**。独立验证（不采信 changelog）：① 全量 pytest 复跑 **1182 passed / 0 failed（201.70s）**与报告一致（1163 基线 + 19 新增）；② 单测文件 57 passed（38 存量 + 19 新增）；③ 数据集独立调用 **136 条 counts {57,20,59}** + parts {real 24/sufficiency 95/constructed 17}（去重移除 6 条与 changelog 清单 G1×2/AQS/联合索引/synchronized/ZGC 一致）+ config 0.7/0.3 未变；④ **DB 直查 id=49/50/51/52 全部数字与 changelog 逐字一致**（49 best 0.65/0.35 kappa3 0.3711/0.7-0.3 0.3252；50/51 0.2981/0.3701/0.5515 thresholds 0.65/0.35 复跑逐字一致；52 best 0.65/0.40 0.3309）+ **从 per_question 独立重算 scan_thresholds 全部同值** + 25 组对照表全 25 行核对逐字一致 + factcheck_scan 恰 2 行单行落库；⑤ **失败模式总数 61 独立重算一致（supported 误杀 24 = 12+12 / inferred 混淆 15 / unsupported 漏判 22 = 15+7），但 inferred 混淆内部分解 10 supported + 5 unsupported ≠ changelog 声称 12 + 3**（任何阈值口径 0.65/0.35、0.7/0.3、0.65/0.40 均 10/5；判 supported 漏列 Redis 哨兵 0.822、CHM 扩容 0.653，判 unsupported 漏列 JWT vs Session 0.071、Kafka Rebalance 0.286；该数字同步进入 METRICS/CONTEXT）；⑥ 三态映射与生产 _judge_by_hhem L580-585 逐字同口径（>=含等号）；⑦ 红线零改动（factcheck_judge/reflector/config/conftest git diff 空）+ 存量测试唯一改动 = 结构断言测试按验收许可更新（module-061/062 先例）；⑧ fixture 冒烟管线跑通；⑨ CONTEXT.md 只增不删 +9 行 / METRICS 136 题表+待办② / ADR-0010 P1-④ 状态行 / 三记忆文件全在。**mustFix#1**：changelog §三（+METRICS/CONTEXT 同步）inferred 混淆分解 12/3 → 10/5 + 补全样本清单（按 DB id=50 per_question 证据）；**mustFix#2**：golden_factcheck.py 模块 docstring（L20-32）数据集构成 55/24（保持 3/改判 6/neutral 13）→ 实 59/20（保持 2/改判 8/neutral 保持 10 改判 3），自述算术 57+55+24=138≠136 与测试断言 {57,20,59} 矛盾。2 项 LOW 非阻塞：changelog 基线数字 1152→1163 口径陈旧（有"以实跑为准"声明）、功能代码 ~176 行超 plan 预估 110 约 60%。报告：specs/module-071-hhem-kappa-calibration/review-report.md。**模块标记 ⚠️ CONDITIONAL 待修复后复审（通知 Developer）** |
| module-071 | Reviewer | [REVIEW 二轮] **结论：✅ 通过（PASS，进 Tester）**。Developer 已按 mustFix ①/② 完成修复（changelog §六 + 变更记录 v2；docs-only 修正轮）。独立验证（全部重查不采信 changelog）：① **mustFix① DB 实证**——直查 id=50 per_question label=inferred 20 条：判 supported **10**（含补列 Redis 哨兵 0.822、CHM 扩容 0.653，实查分数一致）/ 判 unsupported **5**（含补列 JWT vs Session 0.071、Kafka Rebalance 0.286）/ 判对 5——与修复后 changelog §三逐字一致，10+5+5=20 封闭；METRICS/CONTEXT 同步 10/5 口径，grep 全库 `12/3`/`55/24` 零残留；② **mustFix② docstring 实证**——golden_factcheck.py docstring（L19-36）57/20/59 与实际 build 交叉核对：supported 57 = sufficiency 48 + entailment 9、inferred 20 = 保持 2 + neutral 保持 10 + 构造 8、unsupported 59 = 不充分去重 3 实入 47 + contradiction 2 + INFERRED 改判去重 1 实入 7 + neutral 改判 3，57+20+59=136；去重保留语义逐 question 核对（G1/AQS/联合索引→real 版、synchronized/ZGC→supported 版）；③ **docs-only 确认**——golden_factcheck.py 全函数相对一轮均匀 +4 行位移（docstring 扩充），逻辑零改动；红线零 diff（factcheck_judge/reflector/config/conftest）；④ 单测 **57 passed** + `load_factcheck_dataset()` **136** + config **0.7/0.3** + fixture 冒烟 136 条跑通 + 全量 **1182/0（211.19s）**。2 项新 LOW 非阻塞：docstring 诚实边界段"量级小（50 条）"过期（已顺手订正为 136 条口径）、activity-log [CODE] 行 12 条过期（已订正 10/5）。数据/决策零改动（0.2981 < 0.7 不改生产配置保持）。报告：specs/module-071-hhem-kappa-calibration/review-report.md §七。**模块标记 ✅ Reviewer PASS 待 Tester** |
